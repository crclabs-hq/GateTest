# bench-target

Synthetic Node.js/Express corpus used for the GateTest vs. Semgrep vs. ESLint
competitive benchmark. See `docs/COMPETITIVE-BENCHMARK.md` at the repo root
for the methodology and results. Every planted issue carries a `// PLANTED:`
marker so scoring is mechanical, not subjective.

This is a benchmark fixture, not a real application — do not deploy it.
