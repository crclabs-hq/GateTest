# GateTest Roadmap

Forward-looking work: evolution tiers, the Inclusive Agentic QA spec, open Known Issues, and the remote-MCP distribution plan.

> Split out of CLAUDE.md (the Bible) 2026-07-07 to keep every session's context lean.
> The Bible holds rules + current truth; this file holds the detail. Nothing was deleted.

## HYPER-AGGRESSIVE PRODUCT EVOLUTION ROADMAP (READ THIS EVERY SESSION)

**Authorization:** Craig 2026-05-12 — handed over a 20-item product evolution list and instructed: ship the 5 "Ship Now" items. The rest is recorded here so future sessions don't re-litigate it. Boss Rule items in this list ALWAYS require Craig's explicit go before any code lands.

### Tier 1 — SHIP NOW (revenue-moving, low-risk, pre-authorised)

**STATUS: ALL FOUR SHIPPED AND WIRED (2026-05-24 verified — files exist, both fix paths import them).**

1. **Contextual Grounding** — `lib/contextual-grounding.js` (204 lines). Wired into `website/app/api/scan/fix/route.ts:340` AND `src/core/ai-fix-engine.js:38`. Injects README + AGENTS.md + ARCHITECTURE.md into every fix prompt as PROJECT CONVENTIONS. Kills "Claude suggested Mongo when we use Postgres" failures.

2. **Shadow Scan Previews + Tiered Feature Redaction** — `lib/scan-redaction.js` wired into `website/app/api/scan/run/route.ts:45`. $29 customers see counts of issues in the 86 modules they didn't pay for, with redacted detail and upsell prompts.

3. **CVE-to-Fix Pipeline** — `lib/cve-to-fix.js` (449 lines) wired into `website/app/api/scan/fix/route.ts:301`. CVE-shaped findings auto-generate `package.json` / `requirements.txt` / `Cargo.toml` version-bump patches. Headlines vs Dependabot.

4. **Confidence-Aware Reporting** — `lib/confidence-gate.js` (197 lines) wired into `website/app/api/scan/fix/route.ts:1790`. Aggregates per-finding confidence; gate-blocks only when ≥ threshold.

5. **(Item 4 rolls into Item 2 here)** — Tiered Feature Redaction is the Shadow Preview's machinery. Same `scan-redaction.js` helper.

**Future sessions:** do NOT re-implement these. If you think they're missing, grep for the lib file first.

### Tier 2 — REQUIRES CRAIG'S EXPLICIT OK (Boss Rule)

| Item | Why blocked |
| --- | --- |
| Memory-as-a-Service (central DB for Nuclear users' MemoryStore) | New user-data store + retention policy — Boss Rule #9 |
| SOC2 / HIPAA "always-audit-ready" dashboard | Compliance comms + sales positioning — Boss Rule #9 |
| Multi-Agent Consensus (Claude + GPT-4o cross-check) | Adding OpenAI as a second external API — Boss Rule #7 |
| Agentic Self-Healing (auto-fix without dev seeing the failure first) | Modifies customer code before review — trust contract change, Boss Rule #9 |

### Tier 3 — PUSHED BACK (cost > benefit until $5K-$10K MRR)

- **Shadow Infrastructure Scans** — spinning up isolated envs per PR is expensive compute; would erode margins
- **eBPF Runtime Truth** — Linux-only, ops-heavy, customers aren't asking for it
- **Formal Verification / Z3** — academic-grade, audience of ~5 customers worldwide
- **GNN Taint Analysis** — research-stage, ~12+ months from productisable
- **Vector-Based Fingerprinting** — heavy infra lift, real moat but only after revenue justifies it

### Tier 4 — DEFERRED (sequenced behind earlier work)

- **Architectural Decision Memory** — depends on centralised Memory (Tier 2)
- **Automated Post-Mortems** — needs log-ingestion infra; later product
- **Interactive Fix Terminal** — UX upgrade; ship after auto-PR has 100 real-customer PRs of trust
- **WASM Sandboxing for deps** — ~2 weeks of focused work; valuable but not blocking
- **Differential Fuzzing** — pairs with mutation testing; ~1 week to ship

### Operating rules

1. **No new Tier 1 work begins until the previous one is committed + pushed + tested.**
2. **Tier 2 items NEVER get auto-started.** Craig must say "do X" explicitly.
3. **Tier 3 items stay parked until MRR justifies the cost.** Revisit at $5K MRR.
4. **This roadmap is the source of truth.** Any session adding a new feature first checks: is it on this list? What tier? If not on the list and it's significant, ask Craig before building.

---

## INCLUSIVE AGENTIC QA PLATFORM — MASTER BUILD SPECIFICATION v1.0.0 (READ THIS EVERY SESSION)

**Authorization:** Craig 2026-06-23 — *"Let's lock this vision down into a formal, highly actionable blueprint."* This spec sets the new product direction, persona architecture, feature roadmap, and tone guidelines. It supersedes prior positioning where GateTest was pitched purely as a "SonarQube killer." Both framings coexist: the technical engine stays aggressive; the UX and messaging layer becomes inclusive and conversational.

### Product Vision (Updated)

GateTest.ai is the world's first **Inclusive Agentic QA Platform**. We reject tool fatigue, cryptic error messaging, and exclusionary developer elitism. The mission is to protect codebases while treating every user — from absolute novices to veteran software architects — like family.

- **The Technical Engine:** Ultra-fast, zero-dependency intelligence layer that orchestrates standard open-source tools (Playwright, Vitest) rather than reinventing them.
- **The UX Layer:** Empathetic, Claude-powered interface that translates system failures into human-readable conversations, visual storyboards, and automated code patches.

### Multi-Tier User Persona Architecture

The system must dynamically adapt interface and messaging to the user's technical profile:

| Persona | Primary Pain Point | GateTest Solution | Interface Mode |
| --- | --- | --- | --- |
| **The Novice / Learner** | Cryptic, intimidating terminal errors that cause panic | Conversational, encouraging translations of stack traces with step-by-step guidance | **Co-Pilot Mode** |
| **The Consumer / PM** | Zero visibility into pipeline health without opening GitHub | Natural-language search dashboard with clear visual timelines and site health checks | **Visual Dashboard** |
| **The Expert Architect** | Bloated third-party dependencies, slow pipelines, and magic black boxes | Ultra-fast native Node.js runners, clear AST visibility, and raw code configuration toggles | **Expert Toggle** |

### Phase Roadmap & Authorization Status

#### Phase 1 — Advanced Workspace Diagnostics *(Pre-authorized)*

Building directly on the native glob-walker and suppression map from PR #240.

- [x] **1A** Workspace package alias suppression — blanket-suppress false positives for packages consumed via name aliases (PR #240, 2026-06-22). Zero-dependency, line-heuristic approach.
- [x] **1B** Name-level export tracing — **DONE (2026-06-23, commit 3df833c).** Craig authorized `acorn` (now `^8.17.0` in `package.json` dependencies). AST-level entry-surface analysis is fully operational: `src/modules/dead-code-extractor.js` `parseExportsWithAcorn()` walks the AST (falling back to a regex extractor when acorn is absent or a parse fails), and `dead-code.js` builds a precise per-package export surface via `buildPackageExportSurface()` / `populatePackageSurface()` so granular dead code **inside** active packages is found rather than blanket-suppressing the whole package.
- [x] **1C** Configuration-free monorepo discovery — **DONE (2026-06-23)**. Two-part ship: (1) `fix-workspace-hydrator.js` `CONVENTION_FILES` now includes `pnpm-workspace.yaml`, `pnpm-workspace.yml`, `lerna.json` so these are always fetched for the fix route. (2) `/api/scan/run/route.ts` now promotes ALL `package.json`, `pnpm-workspace.yaml`, `pnpm-workspace.yml`, `lerna.json` files to the front of the fetch queue (capped at 30) before the 50-file source cap is applied — so `buildWorkspaceMap()` in `dead-code-index.js` always finds sub-package `package.json` files in the materialised workspace, enabling full monorepo dependency-map construction without any user config file. 1 new test (`fix-workspace-hydrator.test.js`). No new dependencies.

#### Phase 2 — Unified Test Orchestrator *(Needs Craig's authorization — Boss Rule #7)*

Eliminating tool fatigue by wrapping industry-standard tools via Model Context Protocol.

- [ ] **2A** MCP Harnessing Layer — Claude controls native outputs from **Playwright** (UI/E2E) and **Vitest/Jest** (unit/API) via MCP bridges. Playwright is already an approved internal dependency; the MCP wiring into a unified orchestrator is the new piece requiring Craig's go-ahead.
- [ ] **2B** Parallel Execution Core — run static analysis, API checks, and browser UI tests concurrently on the local machine before pushing to git, cutting CI wait times.

#### Phase 3 — Agentic Self-Healing & Repair *(Pre-authorized where it extends existing --auto-pr; Boss Rule for new user-facing command surface)*

- [x] **3A** Diagnostic Bundle — **SHIPPED for the CLI/GitHub Action path (2026-07-20).** `lib/diagnostic-bundle.js` (`buildDiagnosticBundle` / `formatDiagnosticHeader` / `summariseDiagnosticBundle`) compiles whatever a detecting module captured beyond its one-line message — stack traces, grouped network-failure records, per-engine render diffs — into a "Diagnostic Bundle" Markdown header prepended to the Claude fix prompt (same pattern as `contextual-grounding.js`'s conventions header). Wired into `src/core/ai-fix-engine.js`: `aiFix()` accepts `opts.details`, and `injectAutoFixes()` now forwards `check.details` (already populated by `runtime-errors.js`/`console-errors.js`/`cross-browser.js` via `result.addCheck(name, passed, { details })`, previously dropped on the floor before it ever reached the fix engine) into every closure it injects. Empty when a module has nothing beyond the message (the common case for static-analysis modules) — no fabricated DOM/network sections; honest about the gap ROADMAP's original wording implied (no module produces actual DOM markup or a structured request/response log today, only stack text + grouped failure objects — see the "Captured Detail" section's generic renderer). 12 new tests (`tests/diagnostic-bundle.test.js` + 3 wiring tests in `tests/ai-fix-engine-surgical.test.js` proving the header does/doesn't appear in the actual prompt Claude receives). **NOT wired into the website `/api/scan/fix` route** — by the time a finding reaches that route it has already been flattened to a plain string (`ModuleLike.details: string[]` in `website/app/lib/issue-extractor.ts`) by the scan pipeline that persists/serializes results; the structured `check.details` object never survives that boundary. Wiring the website path is a materially bigger, separate change (extend the scan-result storage shape + `IssueInput` + issue-extractor, touching persistence consumed elsewhere) — left as an explicit follow-up, not silently declared done.
- [ ] **3B** One-Click Git Patching — pass the Diagnostic Bundle to Claude, generate a safe precise code modification, present as a clean git patch (`gate-test fix --apply`). The AI-fix engine and orchestrator already exist; the `fix --apply` CLI flag is new user-facing surface. Authorisation needed for the final "apply to user's repo" step.

### System Tone & Personality Guidelines *(Apply immediately to all new error messages)*

> **Core Directive:** GateTest must never sound robotic, punitive, or superior. It must communicate like a helpful, grounded, and slightly witty peer.

**Anti-patterns (never use):**
- ❌ `ERROR: Process exited with code 1. Uncaught TypeError: Cannot read properties of undefined (reading 'map').`
- ❌ `Build Failed. Your code broke 14 tests.`

**GateTest formatting patterns (always use):**
- ✅ *"Caught a small slip-up on line 14 of `deploy-planner`. The app expected a list of files but hit an empty box instead. Here's a quick 2-line patch to make it robust!"*
- ✅ *"Looking good! Your workspace alias setup is running beautifully. 25/25 tests passed safely."*

Apply these guidelines to: module warning/error messages, CLI output strings, PR body templates, website scan-status copy.

### Revenue & Privacy Enforcement *(Boss Rule — Craig must authorize)*

- **Usage-Based Scale:** Generous local-first tier for indie hackers, open-source maintainers, and students to build grassroots loyalty. Enterprise pricing scales on execution volume and infrastructure savings. **Pricing change = Boss Rule #3.**
- **Zero-Data Retention Policy:** Enterprise connections — code snippets passed to Claude via secure APIs must never be retained, stored, or used for model training. This removes the primary enterprise adoption barrier. **Implementing and marketing this is Boss Rule #9 (user data, public-facing comms).**

### Phase 1B Dependency Authorization Tracker

| Dependency | Purpose | License | Bundle impact | Status |
| --- | --- | --- | --- | --- |
| `acorn` | AST parser for name-level export tracing in dead-code.js | MIT | ~100KB, zero runtime dep | **AUTHORIZED + INSTALLED 2026-06-23 (`^8.17.0`, commit 3df833c)** |

**Acorn authorized + Phase 1B shipped:** `parseExportsWithAcorn()` in `src/modules/dead-code-extractor.js` walks the AST to build the precise exported-name surface per workspace package entry point, with a regex extractor as the acorn-absent / parse-error fallback. `dead-code.js` consumes it via `buildPackageExportSurface()` / `populatePackageSurface()`, suppressing only the exports that pass through the entry point and flagging the rest.

---

## 2026-08-18 DEEP AUDIT — WHAT SHIPPED, WHAT IS QUEUED (READ THIS EVERY SESSION)

Craig 2026-08-18: *"remove all limitations developers have with pre-existing
tools … audit at the highest/deepest level along with the website … we don't
want false positives … what are the 20 biggest advancements."* Four parallel
audits ran (competitor complaints across ~60 sources, engine false-positive
measurement on 8 real repos, website/sync audit, engineering/security audit).
Full report: `docs/audits/2026-08-18-deep-audit.md`.

**Shipped the same day (commits b0b45c25 → 5df15098):**
- **The free funnel no longer depends on any git-host credential.** Public repos
  are read via one anonymous archive download (`website/app/lib/repo-snapshot.js`);
  `/api/scan/preview`, the playground stream and `/api/scan/run` proceed with
  no token. Verified live: KI #101's product half is CLOSED without the box key.
- **Every hosted scan path runs the real engine on the whole repo.**
  `website/app/lib/scan-engine-dispatch.ts` is the ONE place engine choice
  lives; `loadRepoFiles` reads up to 4,000 text files in one request; the
  worker scans the pushed SHA at the new `deterministic` tier (full engine,
  AI modules skipped). Before: 23 TS modules on 50 files, HEAD, quick tier.
- **Blocking false positives cut ~90% on real repos** (express 7→0, flask 20→3,
  petclinic 270→9, NodeGoat 358→41 with every planted vuln still caught) —
  seo/authBypass/visual/a11y/subprocess/unitTests/undefinedRef/security/
  python/ruby/claudeCompliance/links/envVars/deployScriptValidator/
  performance/deployReadiness, each with positive + negative controls.
- **Security:** SSRF guard + rate limits on /api/scan/url|nuclear|server;
  Slack fails closed; cron header bypass removed; recipe writes need
  `GATETEST_RECIPE_STORE_TOKEN` (client confidence delta ignored); server-fix
  Forensic branch admin-only; chat limiter module-scoped; webhook status
  passthrough; ssrf-guard blocks 100.64/10 (Tailscale), 198.18/15, 224/4, 240/4.
- **Website:** playground upsell mis-priced ($29→tier=quick) fixed; subscription
  success page; single scan trigger + honest progress + re-run on failure; MCP
  button pauses when email is unconfigured; hosted MCP manifest generated from
  the registry (was 120); 15 stale count claims; root canonical moved; OG images
  render; compare-page facts (SonarQube LOC pricing / IaC; no container images);
  "Nuclear"→"Forensic" in $399 deliverables; carried "120/120 green" replaced by
  a measured nightly self-scan; wp/web phantom Continuous cards removed.

**Shipped later the same day against the sore points (Craig: "nailing the
sore points matters the most"), commits 99c59379 + af22bc79:**
- **Finding registry** (`src/core/finding-registry.js`): one defect = one
  finding across modules (file:line:class dedupe, owning module keeps it),
  ranked blocking → severity → confidence → exploitable class → concrete
  location; runner emits `summary.findings`/`findingSummary`, console folds
  duplicates, PR comment opens with "What matters (5 of N, ranked)" + folded/
  held-back counts, per-module wall collapsed. NodeGoat: 9 eval reports → 3.
- **Reachability-gated dependency alerts** (`src/core/dependency-reachability.js`):
  only critical/high advisories in imported production dependencies block;
  installed-unused warn, dev-only info, reason stated. NodeGoat: 137 → 15
  blocking. Windows: npm audit's `2>/dev/null` made it "could not run" — fixed.
- how-it-works "How we keep it quiet" section states all of it (sync rule).

**Craig actions surfaced by the audit (Boss Rule / box-side):**
1. `GATETEST_PRIVATE_KEY` on the box is still the doc placeholder → GitHub App
   auth dead (statuses, PR comments, private repos). KI #100.
2. `CRON_SECRET` repo secret unset → the off-Vercel tick workflow 401s / the
   queue is only drained by the box's systemd timer. `gh secret set CRON_SECRET`
   with the box's value.
3. `RESEND_API_KEY` unset → MCP keys never email; the button now pauses itself.
4. `GATETEST_RECIPE_STORE_TOKEN` (new) → recipe writes are 503 until set on the box.
5. Legal/privacy pages still name Vercel as infrastructure/sub-processor
   (`legal/privacy/page.tsx:258,495-501`, `legal/terms/page.tsx:255,629,761`)
   — counsel/Craig copy.
6. Decide: URL-scan "Continuous" ($19/$49 cards removed from /wp,/web) — real
   tier or not? And "Full Scan = all 121 modules" vs the honest "every module
   that applies to a repository" (URL/WordPress modules need a live site).
7. Install Playwright on the box or /api/web/scan keeps reporting *our*
   missing browser as a customer finding (`explorer:playwright`).

**Queued (pre-authorised, ranked — see the report's Top-20 for the why):**
- ~~FP residue list~~ **SHIPPED 2026-08-25 (Craig: "end to end"), all with
  positive + negative controls in `tests/fp-residue-2026-08-18.test.js` (33
  tests):** docs_src/doc_src/docs-src are example-data paths in confidence.js
  (fastapi tutorial secrets stop blocking); python deadCode parses
  parenthesized multi-line + backslash-continuation imports and skips
  decorated defs (registered by their decorator, incl. multi-line decorator
  calls); flakyTests real-clock fires only when the clock reading is ASSERTED
  (inline or via a tracked variable) — reading the clock for a fixture id is
  not flakiness; errorSwallow callback-err-ignored now also matches classic
  `function (err, ...)` callbacks (the legacy Mongo/fs form the arrow-only
  regex missed) and scans the brace-balanced body instead of a 5-line window
  (err handled on line 6+ no longer false-fires); dependencies maven skips
  missing-`<version>` warnings under a `<parent>` POM or a
  `<dependencyManagement>` entry and no longer scans dependencyManagement
  blocks as resolutions (petclinic); compat:browserslist requires a web
  signal (browser framework/bundler dep, `browser` field, or index.html) —
  Go repos/CLIs/pure-Node servers exempt, with a positive control that a
  React app still warns; aiHallucination reads the repo's own
  tsconfig/jsconfig `compilerOptions.paths` (JSONC-tolerant, root + one
  level deep) so custom aliases aren't "hallucinated packages";
  bashSafety findings now carry `message` + repo-relative `file` + `line`
  (was: `fix` only with an absolute path → `message: null` downstream);
  ruby `system-interp` downgrades to warning (stays visible, stops blocking)
  when EVERY interpolation is a provable integer (`#{pid}` from `fork`,
  `$$`, `Process.pid`, `.to_i`, digit literal) via a new per-pattern
  `downgrade` hook in universal-checker — mixed integer+string interpolation
  stays a blocking error. NOT touched: gin trivy SARIF perms (audit marks it
  real — a true positive, not residue); fastapi innerHTML ×2 was already
  folded by the finding registry (verified + pinned with a regression test).
  duplicateCode shipped in the same pass: windows need ≥2 lines of actual
  logic (string/number collapsing had made any two data tables "identical
  code"), and overlapping window-offset groups coalesce so a long duplicated
  region is ONE finding instead of one per offset.
- Cross-module dedupe (eval ×3, innerHTML ×2, secrets ×4, open-redirect ×2,
  SQLi ×3) — a finding registry keyed by file:line:class.
- Per-rule precision telemetry surfaced in PR comments + `--noise`
  (competitor complaint #1); PR-comment budget (5 inline, ranked).
- Reachability-gated dependency alerts (competitor complaint #2).
- Auth for /api/status secrets listing; DB-backed rate limits; queue lease
  column + dead-letter; callback retry + `pending` status at enqueue; per-
  fetch timeouts; installation rebinding check on the GitHub callback.
- ~~AI fix verification~~ **SHIPPED 2026-08-25 (same end-to-end run as the
  FP residue):** (1) scanner gate is MANDATORY + FAIL-CLOSED — when the
  caller passes no baseline, `cross-fix-scanner-gate` now derives one from
  each fix's own `original` content and scans it (re-running every module,
  which subsumes "run the originating module on the fixed file"); a runTier
  crash now WITHHOLDS the unverified fixes instead of the old fail-open
  accept-all (deliberate contract flip, pinned in tests). Remaining
  refinement, deliberately advisory-not-blocking: asserting the ORIGINAL
  finding disappeared post-fix (module-detail matching is too fuzzy for a
  hard rollback — a wrong rollback blocks a paid customer, Forbidden #25).
  (2) TS syntax gate: `.ts/.tsx/.jsx/.mts/.cts` fixes are now parsed with
  the `typescript@^5` the website already ships (createSourceFile +
  parseDiagnostics — transpileModule is error-tolerant and passed unclosed
  JSX), lazy-required with pass-through fallback off-website. (3)
  prompt-injection guard on code-writing prompts: `buildSurgicalPrompt`
  wraps the code slice + issue text in `wrapUntrusted()` with an explicit
  never-follow-instructions-in-data preamble (bare require — crash loud,
  never silently unguarded). (4) path allow-list on overwrite targets:
  new `website/app/lib/fix-path-guard.js` runs after test-generation,
  right before commit — structural checks for scanned paths (no traversal/
  absolute/backslash/control chars), plus for MODEL-INVENTED paths no
  `.github/`, no dotfiles, extension allow-list. (5) CLI orchestrator now
  swaps each hypothesis INTO the source path before `node --test` and
  restores in a finally — previously all three hypotheses inherited the
  on-disk original's test result and the testOk ranking bit was noise;
  pinned by a control where only the correct hypothesis passes the tests
  (`opts._callClaude` test seam added).
- Language depth beyond JS/TS (Python second tier, Go/Java/Ruby regex-only —
  label honestly or build); `smart-suite-selector` sends non-JS diffs to
  JS-only modules.
- DX: `--json`/`--format`, `--fail-on`, distinct exit codes, SARIF
  fingerprints + rule help URLs, one VS Code extension, pre-commit recipe.
- Website: per-page canonical/titles on billing/checkout/scan/*, compare-page
  citations + as-of dates, single URL-scan flow, hero repo/URL toggle.

## KNOWN ISSUES — QUEUED FOR FIX

| # | Issue | Severity | Status |
|---|-------|----------|--------|
| 3 | ~~Stripe test keys not yet swapped in~~ — **stale, closed 2026-07-27.** `GET gatetest.ai/api/status` reports `stripe.mode: "live"` with `STRIPE_SECRET_KEY` + `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` both set and no warning. Live keys are in place; the KI was never re-checked after the swap. | — | RESOLVED — verified live. |
| 4 | GitHub App not yet installed on test repo | MEDIUM | Craig action |
| 5 | **Vapron (Crontech) protection — RESOLVED, description was stale.** Verified 2026-07-20: Vapron (`ccantynz-alt/Vapron`) already has a fully bespoke `.gatetest.json` + `gatetest-gate.yml` (SHA-pinned Actions, more hardened than our generic template — that improvement was back-ported into `integrations/github-actions/gatetest-gate.yml` the same day). Nothing was "missing" — the repo was never re-checked after the bespoke integration was built, so this KI never got closed. See CLAUDE.md → PROTECTED PLATFORMS for the full writeup. | — | RESOLVED — already integrated, just undocumented. |
| 6 | **Gluecron.com protection — RESOLVED, description was stale.** Verified 2026-07-20: the generic clone-based CI gate is deliberately disabled (Craig, 2026-04-30) because it kept failing against a private dev repo; the real, live integration is a direct API webhook (`src/hooks/post-receive.ts` → `POST /api/hooks/gatetest`, HMAC/bearer-authed, own `gate_runs` table + notifications + audit log — spec in that repo's `GATETEST_HOOK.md`). Nothing was "missing" — same root cause as KI #5. | — | RESOLVED — already integrated via a different (better-suited) mechanism. |
| 7 | **MCP/GitHub scope claim — RESOLVED, description was stale.** Verified 2026-07-20: the authenticated `gh` CLI (account `ccantynz-alt`, `repo` + `read:org` scopes) already has admin-level push access to `crclabs-hq/GateTest`, `ccantynz-alt/Gluecron.com`, AND `ccantynz-alt/Vapron` — confirmed via `gh api .../permissions` on all three. There was never a scope restriction blocking cross-repo work; whatever prompted this KI's original wording didn't hold up under a direct check. | — | RESOLVED. |
| 22 | **GitHub App `installation_id` not persisted** — **RESOLVED**, description was stale. Pre-launch audit 2026-07-16 found `/api/github/callback/route.ts:17-66` now calls `storeInstallation()` → `website/app/lib/installation-store.js` (`installations` table, `UNIQUE(host, installation_id)`, upsert-on-conflict); fixed in commit `3a1658b` (undated in this doc previously). Residual: the persisted mapping isn't yet looked up from `/api/webhook/route.ts` at ingest time — correlation storage exists but isn't consumed yet. Not urgent (nothing depends on it today), flagged for whoever wires multi-org billing correlation next. | — | RESOLVED (storage side); lookup-at-ingest-time is a small open follow-up, no severity assigned since nothing consumes it yet. |
| 23 | **PR comments are not idempotent** — **RESOLVED, description was stale.** Pre-launch audit 2026-07-16 found `website/app/lib/github-callback.js` already implements exactly the fix this KI asked for: `postPrComment()` lists existing PR comments, finds a prior GateTest comment via a hidden HTML marker, and PATCHes it in place instead of stacking a new one on every push (falls back to POST on first run or on a failed lookup). 18 passing tests in `tests/github-callback.test.js` / `tests/github-hardening.test.js` cover the idempotency path and the pagination walk (capped at 10 pages). The `/api/webhook/route.ts:438` line this KI originally pointed at no longer exists — that file is now 99 lines and delegates to `github-events.js` → `github-callback.js`. | — | RESOLVED (already shipped, roadmap just hadn't caught up). |
| 24 | **GitHub file-tree fetch is unbounded** — **FULLY RESOLVED 2026-07-16.** Pre-launch audit found `fetchTreeWithMetadata()` already detected GitHub's `truncated: true` flag and warned instead of silently reading a partial tree as complete (plus a `TREE_SIZE_WARN_THRESHOLD` warning for large-but-not-truncated repos). The remaining gap — the "ideally... per-directory traversal" fallback the code's own comment flagged as future work — is now built: `website/app/lib/github-tree-walker.js` (`walkGithubTree`) walks the GitHub Contents API directory-by-directory (which never truncates a single directory, unlike the recursive endpoint's ~100k-entry cap), with BOUNDED concurrency (8 parallel) and TWO independent budgets — call count (3000) and wall-clock time (25s) — so a truly enormous monorepo can't hang a serverless function or exhaust the rate limit. Wired into `fetchTreeWithMetadata`: on `truncated: true`, falls back to the walker and reports honestly whether it recovered the FULL tree or itself hit a budget (in which case the warning says so explicitly — the fallback never claims completeness on a partial result either). Built as a plain testable `.js` file (not `.ts`) specifically so it could get real behavioral tests with an injectable `fetchImpl` — `gluecron-client.ts` itself can only be tested via source-text regex assertions (see that test file's own comment on why), which isn't enough coverage for recursion/concurrency/budget logic. 9 new behavioral tests (traversal, symlink/submodule skip, non-directory response, 404 handling, call-budget cutoff, time-budget cutoff, rate-limit abort, never-false-positive-complete) + 1 new source-text contract test confirming the wiring. Full fast suite 6331/6333 (1 skipped, 1 pre-existing unrelated Windows-only flake per KI #42), heavy suite 305/305, `tsc --noEmit` clean, website builds clean. | — | RESOLVED — graceful-degradation warning AND the per-directory fallback both shipped and tested. |
| 25 | **Rate-limit wait cap** — **FIXED 2026-07-16.** `github-bridge.js`'s `respectRateLimit()` had exactly the described bug: `waitMs > 0 && waitMs < 120000` meant any reset 2+ minutes out silently skipped the wait entirely, so the caller proceeded with almost no quota left and hammered 429s (confirmed still present as of this audit, not stale). Fixed: added `RATE_LIMIT_MAX_WAIT_MS` (15 min) — waits inline (with a console heads-up) for resets up to that ceiling, and past it throws a clear, actionable error with the actual reset time instead of silently hammering. Only used by the CLI/GitHub-Action path (`bin/gatetest.js`, `src/index.js`) — never by website serverless routes, confirmed by grep — so a bounded inline wait has no Vercel-timeout risk. 4 new regression tests in `tests/github-bridge.test.js`, including one proving the old skip-the-wait-and-hammer bug is gone. | — | RESOLVED. |
| 42 | **`ai-guardrails` tests flake on local Windows only** — all 30 subtests always pass, but the file intermittently (~1-in-4) trips `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` in `src\win\async.c` when `--test-force-exit` calls `process.exit()` while undici's global fetch pool is mid-teardown. **CI is unaffected** — it runs `ubuntu-latest` and this assertion is Windows-only. Mitigated 2026-07-14 by adding `Connection: close` to `src/modules/ai-guardrails/probe.js` (one-shot probes gain nothing from pooling), which reduces but doesn't eliminate it; a full fix needs undici's global dispatcher closed in teardown, but undici is Node-internal and not requireable. **ROOT-CAUSED AND FIXED 2026-07-26.** Two corrections to the diagnosis above: (a) the real rate on this machine was **7-in-12 failing, not ~1-in-4** — it was substantially worse than recorded; (b) **the hypothesised fix does not work.** Closing undici's global dispatcher via `globalThis[Symbol.for('undici.globalDispatcher.1')].close()` in an `after()` hook was implemented and measured at **9/12 failing — no better than baseline**, because `close()` *initiates* socket teardown immediately before force-exit rather than preventing it. Do not retry that approach. The actual trigger is the **unreachable-endpoint test** (`http://127.0.0.1:1/`), which fires every jailbreak scenario at a connection-refused port: the probe promises reject the moment the connect fails, but each failed TCP connect leaves a socket handle libuv is still closing when `--test-force-exit` calls `process.exit()`. Fixed by awaiting a non-unref'd `settleOpenHandles(150)` at the end of that one test so the handles finish closing before the runner exits. **Measured: 5/12 clean before → 32/32 clean after** (12-run and 20-run batches). | — | RESOLVED — root cause found, negative result on the dispatcher-close approach recorded so it isn't retried. |
| 41 | **Nothing scheduled the ticks — FIXED 2026-08-05.** Quality bar #12 requires a scheduler on `/api/scan/worker/tick` (~2 min) and `/api/watches/tick` (~5 min). There was no cron entry and no systemd timer on the box; the intended driver (`cron-ticks` GitHub Actions) needs a `CRON_SECRET` **repo secret** that is unset, while the box has had `CRON_SECRET` in `website/.env.local` all along. Now driven by version-controlled systemd timers (`scripts/deploy/systemd/`, commit `39f4e3e1`) on the machine that owns the queue — hanging a critical user flow off GitHub Actions is Forbidden #3 in spirit. `tick.sh` deliberately exits non-zero on `{"ok":false}` under HTTP 200, the exact shape that hid KI #98. Verified: timers active, firing on schedule, ticks green. | — | RESOLVED — timers installed, enabled, and observed firing. |
| 29 | **GitHub Marketplace listing itself** — distribution channel. **Audited 2026-07-20, punch list narrowed.** The listing draft (`integrations/marketplace/listing.md`, updated 2026-07-19) is in good shape — consolidated from 4 conflicting drafts, module count verified live, Free-only plan correctly specified (the prior submission was rejected 2026-05-14 for describing paid functionality without meeting GitHub's ≥100-install threshold). Setup pages (`website/app/github/setup/page.tsx`, `docs/api/page.tsx`) correctly reference the live app. **Two things need Craig's attention before submitting, found this audit:** (1) the `crclabs-hq` org has TWO installed GitHub Apps — `gatetesthq` (app_id 3322634, installed 2026-07-19, matches all current code/docs) and an older `gatetest-hq` (app_id 3766251, installed 2026-05-19, `checks:write` permission unused since the webhook code posts via the Statuses API, not Checks) — the older one looks orphaned; Craig should confirm and likely delete it before submitting the newer one for review. (2) the live `gatetesthq` install has `contents:write` + `statuses:write` + `pull_requests:write` but **no `issues:write`** — PR comment posting (`postPrComment` in `github-callback.js`) calls the Issues-comments endpoint, which needs that permission. Tests pass because they mock the HTTP layer, so this gap wouldn't show up in CI — it needs a live round-trip test or Craig adding the permission in `github.com/organizations/crclabs-hq/settings/apps/gatetesthq` before relying on PR comments in production. Still genuinely blocked on Craig beyond that: the actual listing creation click-through, confirming uploaded logo/screenshots aren't stale from the rejected submission, confirming `hello@gatetest.ai` forwarding works (the rejection notice reportedly sat unread ~2 months because of this), and the submit-for-review click. **Re-audited live 2026-07-25 (the app is now under review) — three review-blockers found, ALL Craig-actions (nothing code-side):** (a) **`/legal/privacy` and `/legal/terms` are served with visible "[DRAFT — requires attorney review]" markers** on GDPR/CCPA/sub-processor/transfer sections — GitHub reviewers open the required legal URLs first, and a "DRAFT" stamp on them is the single most likely rejection trigger. Boss Rule (legal + public copy) — needs Craig's attorney sign-off, then remove the markers. (b) **Privacy policy lists the email sub-processor as "TBD"** — an unnamed live sub-processor reads as unfinished; name it (whoever handles `hello@gatetest.ai` + Resend for transactional email). (c) **`RESEND_API_KEY` is missing on production** (per live `/api/status`) — this isn't just cosmetic: the MCP-tier "API key delivered by email after checkout" flow AND the new self-serve billing portal (KI #72) both silently can't deliver without it. Set it on Vapron. Also still open from the 2026-07-20 audit: the orphaned older `gatetest-hq` app (app_id 3766251) and the missing `issues:write` permission on the live `gatetesthq` install (PR comments need it). **Good news from this audit:** setup page complete, refunds policy solid, homepage 120-count + mutation/chaos CI-only caveat all correct, `/api/webhook` live and self-identifying dual-host, `/api/status` current. README updated 2026-07-25 to document baseline mode, authed crawl, and the billing portal (were undocumented). **Re-verified live 2026-08-04 — WHERE THE LISTING LIVES, and a new domain-move blocker.** Craig could not find the Marketplace application; the reason is that it does not hang off `crclabs-hq`. There are THREE distinct GateTest identities on GitHub and the earlier "the `crclabs-hq` org has TWO installed GitHub Apps" note conflated *installed in* with *owned by*: (i) **`Gate-Test`** — Organization, created 2026-04-08, **0 repos**, and the actual **owner of `gatetesthq` (app_id 3322634)**, i.e. the app that matches all current code and docs → **the Marketplace listing is managed at `github.com/organizations/Gate-Test/settings/apps/gatetesthq`, NOT under `crclabs-hq`**; (ii) `crclabs-hq` — Organization, created 2026-05-18, owner of the older orphaned `gatetest-hq` (app_id 3766251) — looking here is what turns up "nothing"; (iii) `ccantynz-alt` — the personal account that actually holds this repo. Anyone hunting for the listing under `crclabs-hq` or `ccantynz-alt` will conclude it is missing when it is not. **NEW BLOCKER (introduced by the .io domain move, KI #93):** both apps still advertise `external_url = https://gatetest.ai`, and that domain returns **HTTP 000** (redemption since 2026-07-29) while **`gatetest.io` serves HTTP 200, including `/github/setup`**. So the listing's own homepage link is dead for a reviewer clicking through — for an app that has been *under review* since 2026-07-25 this is the most likely current rejection trigger, and it is a two-field edit in App settings. Craig must also confirm the App's configured **Setup / Webhook / Callback URLs** are on `.io` (`https://gatetest.io/github/setup`, `/api/webhook`, `/api/github/callback`) — those are only readable with app auth, so they could not be verified from here. **Repo side is already correct and needs no work:** `integrations/marketplace/listing.md`, `docs/marketplace/`, `action.yml` and `integrations/README.md` carry zero `gatetest.ai` URLs (email addresses deliberately stay `@gatetest.ai` per the Bible's domain section). | HIGH | Craig action (Boss Rule #4/#8) — **first: change `external_url` to `https://gatetest.io` on app 3322634 (`Gate-Test` org) and verify its Setup/Webhook/Callback URLs**, since the listing is under review with a dead homepage link. Then: legal attorney review + name email sub-processor + set RESEND_API_KEY on Vapron + the two 2026-07-20 GitHub-App items (delete orphaned `gatetest-hq` 3766251, add `issues:write`). |
| 31 | **Scan-speed reality vs claims (claims audit 2026-06-09)** — **RE-VALIDATED WITH REAL DATA 2026-07-16, see `docs/BENCHMARKS.md`.** Ran `scripts/benchmark-scan.js` against a synthetic 36-file repo representative of a typical customer's size: quick suite 1.1-1.3s, full suite 2.6-3.1s — comfortably inside Bible Quality Bar #9 ("Quick <15s, Full <60s"). Separately re-measured this repo's own self-scan quick suite at 29.4-30.8s (down from 228s on 2026-07-15, itself down from a 363s regression before commit `4efe693`'s ESLint fix) — the swing is explained by ESLint cache state (warm vs cold `.eslintcache`), not a further engine bug. **Conclusion: the original 34-52s baseline holds on repos of the size most customers actually have. This repo's own multi-thousand-file monorepo was never a representative benchmark target and self-scan timing should be tracked separately from the public per-scan-size claims** — the two were being conflated in this Known Issue's prior entries. Quality Bar #9 stands as written; no further action needed on the claim itself. | — | RESOLVED — real benchmark evidence backs the public Quick/Full targets on representative repo sizes. |
| 43 | **Self-scan 2026-07-15/16 found modules flagging their own test fixtures as real findings.** First 3 (`tls-security`, `cookie-security`, `cross-file-taint`) fixed 2026-07-15 (commit `4efe693`) with `BaseModule#_isInsideStringLiteral` + shared regex-literal-aware `_stripJsStrings`. **`redos` and `cronExpression` fixed 2026-07-16**: both harvest regexes matched fixture text like `write(tmp, 'src/a.ts', 'cron.schedule("60 0 * * *", run);')` because they didn't check whether the match sat nested inside an outer string literal in the scanned line. Applied the same `_isInsideStringLiteral` guard at every extraction point (`redos.js`: regex-literal extraction, `RegExp()`/`new RegExp()` ctor match, taint-source ctor match; `cron-expression.js`: the JS/Python/Spring harvest-regex loop). Verified against this repo: `redos` warnings 22→11 (remaining 11 are real hits in `website/app/lib/*`), `cronExpression` warnings 13→1 (remaining 1 is the real every-minute cron in `vercel.json`). 6 new regression tests (3 per module: nested-in-string is silent, same pattern unquoted still fires). `asyncIteration` checked and found already clean — its 4 warnings are real `.map(async...)` hits in website code, no fixture noise. Full fast suite 6302/6305 (2 pre-existing unrelated Windows-only flakes, KI #42), heavy suite 305/305, website build clean, all ~120 modules load. **Broader audit run 2026-07-16**: tallied every module's warning count on this repo's quick suite; `promptSafety` was the clearest remaining match (14 of 21 warnings pointed at its own `tests/prompt-safety.test.js`) — same root cause (`write(tmp, 'src/a.js', 'const key = process.env.NEXT_PUBLIC_X_KEY;')`-shaped fixtures), fixed the same way (`_isInsideStringLiteral` guard on all 4 detection sites: public-api-key, deprecated-model, no-max-tokens, prompt-injection). 21→0 warnings on this repo, 5 new regression tests, all 33 existing + new tests green. **`webHeaders` investigated and explicitly NOT fixed the same way** — its CSP/CORS values are legitimately string literals in real code (`value: "default-src 'self'; ...'unsafe-eval'"` is normal, correct source), so "is the match inside a string" isn't a valid self-reference signal there like it is for redos/cronExpression/promptSafety (whose flagged patterns — regex literals, bare env-var names — are never normally quoted in real code). Attempted the same fix, broke 4 passing tests (real detections got suppressed), reverted immediately. Fixing `webHeaders`'s self-flagging needs depth-aware string-nesting detection (distinguish "one real string level" from "a fixture string wrapping another string"), not the flat presence check used elsewhere — left as a separate, harder follow-up. `secrets`/`secretRotation` also checked and found to be a different, lower-priority shape (findings scattered across many *different* test files' own literal fixture passwords, e.g. `const password = 'super-secret-admin-pw'` written directly as test setup — not nested-string false positives, already correctly downgraded to warning severity for test paths; working as designed). **`webHeaders` RESOLVED 2026-07-20** — the depth-aware-string-nesting approach this KI called for turned out to be unnecessary: the module already had a path-based `isTest` boolean (used only to downgrade 2 of 8 rule severities), and unlike the content-based `_isInsideStringLiteral` check that broke things before, `isTest` doesn't care whether a fixture is represented via escaped-quote nesting (`tests/web-headers.test.js`) or a template literal (`tests/runtime.test.js`) — both shapes live under a path `isTest` already correctly recognizes. Extended it from "downgrade severity" to "skip the file entirely" (`src/modules/web-headers.js` — one early-return added right after computing `isTest`). Verified safe: every existing fixture target in `tests/web-headers.test.js` writes to `os.tmpdir()`, which never matches the `isTest` path regex, so none of the 14 existing tests needed to change. Self-scan warnings 19→8; the remaining 8 are real (6 genuine `vercel.json` header gaps + 2 legitimate `csp-unsafe-inline` hits inside GateTest's own CSP-suggestion generator code, which does default to `'unsafe-inline'` in its recommended value — a separate, real, un-filed finding worth a look but not a false positive). 2 new regression tests (fixture pattern silent inside `tests/`, same pattern still fires outside `tests/`). | — | RESOLVED — `webHeaders` self-flagging fixed via the existing path-based signal, no new heuristic needed. `secrets`/`secretRotation` remain a different, lower-priority, already-correctly-handled shape. |
| 32 | **Two fully-built modules never registered: `src/modules/cve-feed.js` + `src/modules/sbom.js`** (claims audit 2026-06-09). **SBOM registered 2026-06-18** — CycloneDX 1.4 generator is file-system only (no network), now registered as module 111, added to `full` + `nuclear` suites. US EO 14028 / EU CRA compliance artifact. **Website marketing list caught up 2026-07-09** — `sbom` was in the CLI engine but missing from `website/app/components/howitworks/modules-data.ts` (the site's single-source-of-truth for the public module count), silently undercounting it as 119 instead of 120; added. **CVE feed still dormant** — likely requires network calls to pull CVE data; confirm Craig's policy on external-data fetches (Boss Rule #7) before registering. | MEDIUM | **PARTIAL** — sbom ✓ registered + ✓ on website. cve-feed: Craig action — network-call policy confirmation needed. |
| 34 | **Continuous-tier AI-allowance enforcement point** — **RESOLVED 2026-07-19** (commit `e92634d`). `runWorkerTick` (`website/app/lib/scan-worker.js`) now looks up `findActiveByRepo` for the job's repo and, only when an active subscription has budget remaining per `checkAiAllowance`, escalates from the deterministic `'quick'` tier to the AI-inclusive `'full'` tier; any lookup/check failure fails CLOSED to `'quick'` so an error can never grant unmetered AI spend. `aiReview` (`website/app/lib/scan-modules/ai.ts`) now reports real Claude token cost via the existing `budget-tracker.js` pricing table, and `recordAiSpend` is called after each run that incurred cost. 9 new tests in `tests/scan-worker-tick.test.js`. Note: the diff-size circuit breaker (`MAX_DIFF_FILES`, mentioned in the original KI text) is still NOT enforced — `scan_queue` has no column tracking per-push diff size, and Gluecron's Signal Bus push payload doesn't carry a file list at all (see its wire contract in `website/app/lib/events-push.js`), so that specific piece needs a Gluecron-side payload change, not just a GateTest-side fix. | — | RESOLVED (budget gate); diff-size breaker still open, blocked on Gluecron's push payload shape. |
| 35 | **MCP registry submission to Anthropic still not done** (Craig, 2026-07-09: "really urgent"). `mcp-publisher` is a Go binary from `modelcontextprotocol/registry` GitHub releases, not npm — no Windows build exists as of 2026-07 (confirmed same day; the docs previously had a wrong `npm install -g @modelcontextprotocol/publisher` command, fixed). Flow: `mcp-publisher login github` (device flow, Craig's own browser) → `validate` → `publish ./server.json`. Full steps in `docs/marketing/SUBMISSION-RUNBOOK.md` §1b. Also gated on Known Issue #36 below — registry crawlers/health-checks may hit the stale live site. | HIGH | Craig action — needs his own GitHub device-flow login from WSL/Linux/a Go toolchain checkout; no code action possible. |
| 36 | **RESOLVED — gatetest.ai is no longer stale.** Was: served a stale build off Server 161 (66.42.121.161) after hosting moved off Vercel 2026-07-09, `/api/status`/`/api/mcp`/`/icon.png` all confirmed 404 at the time. **Confirmed live-fixed during the 2026-07-20 security audit**: `/api/status` now returns 200 with a fresh `generated_at` timestamp matching the day of the check, `/api/mcp` returns 405 (route exists, method not allowed on GET — expected), `/icon.png` returns 200, and the served security headers (HSTS/CSP/X-Frame-Options/etc.) match `next.config.ts` exactly. This class of deploy-pipeline drift (repo state vs. what's actually live) is exactly why an occasional live spot-check is worth doing rather than assuming from the repo alone. | — | RESOLVED — confirmed live 2026-07-20, no further action. |
| 38 | **MCP-tier pricing-threshold design shelved** ("first 2000 signups at $29/mo, then $49/mo" — counter in `mcp-subscription-store.js` keyed off the Stripe webhook activation point, threshold check in `checkout/route.ts`'s `TIERS["mcp"]`). Fully scoped 2026-07-09, not built. Carved out of resolved Known Issue #37 (the rest — engine-first messaging, model choice, BYOK — shipped v1.58.0, 2026-07-10; see HISTORY). Pricing = Boss Rule #3. | LOW | Craig action — ready to build if/when MCP tier pricing work resumes. |
| 39 | **BYOK × $29/mo MCP gate** — does a BYOK user (own Anthropic key funds the AI calls) still need the `gtmcp_` subscription for premium tools? Shipped default: yes, gate stays (removing it is a pricing change → Boss Rule). Asked 2026-07-10, unanswered. | MEDIUM | Craig decision. |
| 40 | **Full-suite scan hangs indefinitely on the Gluecron.com repo** (found 2026-07-12). **ROOT CAUSE FOUND AND FIXED 2026-07-16.** Runner-level per-module timeout shipped first (2026-07-16, `src/core/runner.js` `_runModule` races every module against a wall-clock timeout — 2 min default, 10 min for `mutation`/`e2e`/`visual`/`visualRegression`/`chaos`), but re-running the actual repro (shallow-cloned `ccantynz-alt/Gluecron.com`, full 88-module suite) proved the timeout alone did NOT save it: the scan still hung. Bisected module-by-module, then file-by-file: `codeQuality` hung on a single 257KB TSX file (`src/routes/pulls.tsx`) alone, confirmed via an isolated 60s shell timeout. Traced to `_neutraliseContent()`'s regex-vs-division heuristic calling `out.trim()` / `out.replace(/\s+$/, "")` on the **entire accumulated output string** every time it saw a `/` in code state — TSX/JSX files are dense with `/` (every `<Foo />` and `</Foo>` is one), turning an intended O(n) file scan into O(n²). **This is a synchronous CPU-bound hang, not an async wait — it blocks Node's single-threaded event loop, so the runner-level `Promise.race` timeout literally cannot rescue it** (the timer callback never gets a turn until the blocking loop returns). The runner-level timeout is real defense-in-depth for genuine async hangs (stuck subprocess, network wait) but this specific bug needed a source fix. Fixed in `src/modules/code-quality.js`: bounded the lookback to `out.slice(-24)` (same fix applied to both `_neutraliseContent` and the legacy `_stripContextFromLine`) — turns the whole traversal back into true O(n). **Verified end-to-end**: the exact file that hung for 9.5+ minutes (killed by an external timeout, never finished) now neutralises in 81ms; the whole `codeQuality` module across all 1,040 files in the real repo now takes 1.2s (was: never finished); the full 88-module suite across the whole repo now completes in **34.7 seconds** (was: hung indefinitely, previously killed after 45-50 min with no report). 7 new regression tests including one reproducing the exact JSX-dense shape that hung, asserting sub-5s completion where the bug took 60s+. | — | RESOLVED — root cause found, fixed, and verified against the real repo end-to-end. |
| 44 | **Pre-launch audit 2026-07-16 found website copy drift far broader than previously tracked** — 20 files across `app/compare/*`, `app/for/*`, `app/components/*`, `app/trust`, `app/github/*`, `app/scan/preview`, `app/api/badge`, `app/sitemap.ts` still hardcoded stale "110"/"90" module counts (some pages had 2-3 different wrong numbers on the same page), one page (`compare/codeql`) built its entire headline positioning around an unrealistic "60 seconds" full-scan claim contradicted by KI #31, and several mutation/chaos mentions were missing the CI-only honesty caveat (worst: homepage `HomeKills.tsx`, zero disclaimer on the highest-visibility surface). **All fixed same day** — every count normalized to 120, `compare/codeql` softened to "minutes" language matching the pattern already used on `compare/deepsource`, missing honesty caveats added everywhere mutation/chaos is mentioned. `app/for/page.tsx`'s `MODULE_COUNT` now imports `TOTAL_MODULES` from the single-source-of-truth (`module-count.ts`) instead of a second hardcoded literal that had drifted from its own comment. Full website build clean, `tests/heavy/marketing-claim-verification.test.js` + `tests/heavy/site-stats-honesty.test.js` + `tests/marketing-country-pages.test.js` all green (57/57). **Not fixed — flagged, not touched**: `app/preview/*` (Bento/Cta/Enterprise/Hero/Nav/Pipeline/Playground, ~7 files) is an orphaned design variant still saying "110+ checks" everywhere — not linked from any page, not in the sitemap, so not customer-discoverable today, but it's a live route if anyone has the URL. Needs Craig's call: finish it, delete it, or leave parked. | HIGH (was silently undermining the "no scatter-gun" / honesty standard) | RESOLVED except `/preview` — Craig decision needed on that orphaned section. |
| 45 | **Remote MCP (`packages/mcp-remote/`) is implemented and tested but not deployed** — confirmed by pre-launch audit 2026-07-16. The actual JSON-RPC/tool logic lives in `website/app/lib/mcp-remote-core.cjs` (509 lines, all 27 tests in `tests/mcp-remote.test.js` pass), imported by `packages/mcp-remote/src/index.ts`. `packages/mcp-server/server.json` is valid and declares both transports, but its remote endpoint points at `https://gatetest.ai/api/mcp`, not `mcp.gatetest.ai` as `docs/ROADMAP.md`'s own REMOTE MCP section describes — a doc-vs-reality mismatch, not a functional bug (the website API route works; the dedicated subdomain + Jarvis co-location was never actually stood up). No CI/deployment automation exists for `packages/mcp-remote/` in this repo. | MEDIUM | Craig decision — DNS (`mcp.gatetest.ai` A record) and the Jarvis deploy are Boss Rule #4/#5 territory; either finish the subdomain plan or update the roadmap to describe the shipped `gatetest.ai/api/mcp` reality instead. |
| 46 | SonarQube leg of `docs/COMPETITIVE-BENCHMARK.md` deferred -- needs Docker Desktop plus WSL2 reboot on the bench machine (Craig 2026-07-17 chose skip-for-now); when rebooted, run SonarQube Community via Docker plus sonar-scanner against the same bench-target corpus and append its row/column to the doc. | LOW | Craig action (reboot) then pre-authorized benchmark run. |
| 47 | **Multi-Agent Consensus (Claude + GPT-4o cross-check) — WIRED 2026-07-20.** Was authorized by Craig 2026-06-02 and fully built (`website/app/lib/openai-client.ts` + `multi-agent-consensus.ts`, 16 tests) but sat as dead code — nothing imported it. Now wired into `/api/scan/fix/route.ts`'s whole-file fallback path (not the surgical/line-numbered path — deliberate scope limit, see the comment above `askClaudeConsensus()`): request body accepts `consensus: true`, gated to Forensic tier + `OPENAI_API_KEY` configured via `resolveConsensusGate()` (`website/app/lib/consensus-gate.js`, new pure/testable extraction). On agreement the merged fix ships; on disagreement/no-parse the file is skipped (existing "no changes produced" path) rather than auto-applying a disputed fix, per the library's own design intent. Response carries a new `consensus: { enabled, filesRun, reports }` field (or `{ skipped, reason }`), following the same convention as `pairReview`/`confidence`/`architecture`/`cisoReport`. Anthropic-side spend rides the existing per-request budget tracker automatically; OpenAI-side spend is bounded by the call's `maxTokens` cap only (matching the original design's stated safety boundary), not integrated into that Anthropic-specific tracker. 6 new tests (`tests/consensus-gate.test.js`) plus the pre-existing 16 in `tests/multi-agent-consensus.test.js`; `tsc --noEmit` clean, website build clean. | — | RESOLVED — live behind the `consensus:true` opt-in on Forensic tier. |
| 48 | **`ai-hallucination` was flagging 1047 of 1099 self-scan checks (95%) as "possible hallucinated import" — found during a fresh full-suite warning tally 2026-07-20, same pattern-class as KI #43 but never previously audited.** Root cause: `BUILT_IN_MODULES` only listed bare Node builtin names (`fs`, `path`, `test`, ...) — any `node:`-prefixed import (`node:fs`, `node:test` — the modern, increasingly standard style, used throughout this very repo's own test suite) fell through to "unknown package." Accounted for 890 of 1037 unknown-pkg findings (86%) on this repo alone. Fixed in `src/modules/ai-hallucination.js`: added `stripNodePrefix()` before the builtin-set lookup, plus filled in genuinely missing builtins (`test`, `test/reporters`, `sqlite`, `sea`, `assert/strict`, `dns/promises`, `readline/promises`, `inspector/promises`). Also added `@lib/` to `PATH_ALIAS_PREFIXES` (this repo's own tsconfig alias, used extensively, was never recognized) and `vscode` to the built-in/virtual set (VS Code's extension-host-injected module, not an npm dependency). Self-scan: 1047 → 140 warnings. 4 new regression tests in `tests/new-modules.test.js`. **Two smaller gaps found but deliberately NOT fixed this session** (documented here instead of rushed, per the KI #43 lesson about being careful with self-reference heuristics): (1) ~100 of the remaining 140 are real packages (`playwright`, `express`, `openai`, `helmet`, ...) imported by files under `benchmarks/bench-target/` or similar nested fixtures that have their OWN `package.json` — the module only walks UP from `projectRoot` looking for a package.json, never DOWN into subdirectories, so a monorepo/fixture's own nested dependency manifest is invisible to it. Fixing this properly means per-file nearest-package.json resolution (with caching, to avoid O(n²) on large monorepos) — a real, separate, more invasive change. (2) A handful (~5) of the remaining findings are pure artifacts (`*.node`, `${importPath}`, `depName`) — `native-bundler-guard.js`, `syntax.js`, `dependency-upgrade-patcher.js`, and `regression-test-generator.js` all contain regex/template-string logic that itself resembles an import statement, which the harvester's dumb regex matches as if it were real code — the same self-referential-source pattern KI #43 fixed for other modules, just low-volume here (0.5% of the original count) so not worth rushing in the same session as the bigger fix. **BOTH REMAINING GAPS CLOSED 2026-07-30 — and this row's DIAGNOSIS OF GAP (1) WAS WRONG, which is the more useful finding.** Measured start: 136 warnings / 107 unknown-pkg. **Gap (1), nested manifests, FIXED as specified:** dependency resolution walked only UP from projectRoot, so a package.json BELOW it was invisible. Added per-directory nearest-manifest resolution, memoised, with directories lacking a manifest SHARING the parent's Set rather than copying it (that allocation cost is what made this look "more invasive"). Ancestors are UNIONED not shadowed, because npm/pnpm hoist and a workspace package legitimately resolves root deps — shadowing would trade one false-positive class for another. Effect: 136 -> 115. **But this row predicted ~100 of the remaining findings were nested fixtures under `benchmarks/bench-target/`, and measurement says otherwise:** the real distribution was 30 from `tests/security-policy-applier.test.js`, 17 from `tests/prompt-safety.test.js`, 17 from `src/modules`, 11 from `website/app`, and almost none from bench-target. So the nested-manifest theory accounted for only the 21 it removed. **The actual dominant cause — gap (3), previously unrecorded:** the import harvester scanned raw text with no string or comment guard, so package names inside FIXTURE STRINGS were harvested as imports. A module that tests framework detection must contain sample code as data, e.g. `assert.equal(detectFramework("const express = require('express');"), 'express')`. The tell was two of the top "packages" being `${importPath}` and `*.node`, neither of which can occur in a real specifier. `ai-hallucination` used `_isInsideStringLiteral` ZERO times — it was one of the unguarded modules KI #77 listed. Now guarded by COLUMN (a genuine `import x from 'pkg'` matches at line start, so its quoted specifier is never mistaken for string enclosure) plus `_isCommentLine`. **Combined: 136 -> 31 unknown-pkg (77% reduction), and the 31 that remain are TRUE positives** — `playwright` (14) and `@babel/parser` are genuinely absent from the respective package.json files. **No false negatives introduced, verified rather than assumed:** the module's own `totally-made-up-package-abc` fixture still fires, the existing 64 regression tests still pass, and 8 new tests in `tests/ai-hallucination-resolution.test.js` pair every suppression with a POSITIVE CONTROL asserting a genuinely fake package is still caught (plus a sibling-package case proving resolution walks up, never sideways). | MEDIUM | RESOLVED — nested-manifest + fixture-string guards shipped; 136 -> 31 unknown-pkg, remainder are true positives. |
| 49 | **`envIntegrity` was flagging 123 of 123 checks (100%) as trailing whitespace — a systemic bug, not self-scan noise, that would hit any CRLF-encoded `.env` file (the Windows default, and common regardless of OS via `core.autocrlf`).** Root cause: `env-integrity.js` split file content on bare `\n`; a CRLF-encoded file left a trailing `\r` on every line, and the trailing-whitespace regex correctly-but-wrongly matched that `\r` as real trailing whitespace on every single value. Confirmed `.env.example` in this repo is 100% CRLF (225/225 lines). Fixed: split on `\r?\n` instead. Self-scan: 123 → 0 warnings. Found and fixed the identical bug in `lint.js`'s markdown trailing-whitespace check (`_lintMarkdown`) while auditing for the same pattern elsewhere — lower severity (info, not warning) and not currently visible in this repo's self-scan (no CRLF markdown files here), but the same latent landmine for any customer's CRLF markdown. 4 new regression tests (2 per module: CRLF alone is silent, genuine trailing whitespace on a CRLF file is still caught). Checked ~74 other modules that split on bare `\n` for a combined trailing-whitespace-style check via grep — only these two matched the vulnerable pattern (most just regex-search line content, which doesn't care about a stray `\r`); `shell.js`'s similar-looking `/\s+$/` usage is a sanitization strip, not a detection rule, so it wasn't at risk. | — | RESOLVED. |
| 50 | **`compatibility` was flagging 237 of 240 checks (99%) — the module's "RegExp v flag" detector accounted for 202 of them (85%).** The detection regex (`/\/[^/]+\/[gimsuy]*v/g`) had no word-boundary after the `v`, so it matched ANY two-slash path segment immediately followed by a word starting with `v` — `./lib/validators`, `./api/version`, `./components/value` — none of which are regex literals at all. Fixed in `src/modules/compatibility.js`: added `\b` after the `v`. 3 new regression tests in `tests/compatibility.test.js` (two false-positive-path cases, one control case proving a genuine `/pattern/v` regex literal is still caught). Self-scan: 237 → 40. Same audit pass as KI #48/#49 — checked (during the fresh full-suite tally 2026-07-20) but didn't dig further into `errorSwallow` (335/337 — confirmed this is the module working as documented-intent, not a bug: it deliberately flags comment-only catches, not just truly empty ones) or `flakyTests`'s real-clock rule (197/199 real-clock+real-timer — sampled several hits and found a genuine mix of real flakiness risk and benign fixture/ID construction, not a clean false-positive pattern like the other three; a correct fix needs a more careful heuristic than the others did, left for a dedicated follow-up rather than risk another near-miss like the one caught mid-session on webHeaders). | — | RESOLVED (RegExp v flag). `errorSwallow`/`flakyTests` investigated and NOT bugs in the same sense — noted for future, more careful follow-up if picked up. |
| 51 | **`deadCode` flags 234 of 475 self-scan checks (49%) as `unused-export`, but — unlike KI #48/#49/#50 — this is a genuine mix, not one dominant bug.** Confirmed at least one real, narrow sub-bug: `bin/gatetest-sweep.js`'s exports (`parseArgs`, `selectSteps`, `buildJson`, `renderStepLine`, `renderSummary`) are flagged unused, but they ARE used — by `tests/heavy/sweep-cli.test.js` (`require('../../bin/gatetest-sweep')`). `dead-code.js`'s `ENTRYPOINT_DIRS` correctly includes `'tests/'` (so a file's OWN exports there don't need external use), but the separate usage-tracking pass that looks for who IMPORTS a given export apparently doesn't walk into `tests/heavy/` — same shape of bug as `tests/heavy/*.test.js` files simply not being part of whatever file set the indexer (`dead-code-index.js`/`dead-code-extractor.js`) scans for consumers. Only 6 distinct `bin/*.js` files carry this specific pattern though (not the bulk of 234) — the rest of the findings are spread across many other locations and weren't individually verified as false positives vs. genuinely dead code. NOT fixed this session — the usage-tracking logic lives across two additional files (`dead-code-index.js`, `dead-code-extractor.js`) not yet read closely, and a rushed fix here risks the same near-miss pattern already caught once this session (webHeaders). **MEASURED 2026-07-30 — NOT A MODULE DEFECT. Closing with no code change.** This row calls a ~49% finding rate "noise", which assumes the findings are wrong. They are not. Current actual count is **188** (145 `unused-export` + 43 `orphan-file`), not the 234 recorded — and 152 of the 188 are in `website/app`. **Two hypotheses tested and both rejected:** (1) *Next.js framework-convention exports* (`default`, `metadata`, `generateStaticParams`, route `GET`/`POST` handlers) being flagged because the framework consumes them without importing — measured: **0 of 152**, so that entire theory was wrong. (2) *an automated precision check* suggested 15% were imported elsewhere — that figure was an artefact of my own regex (`import[^;]*SYM[^;]*from` spans a multi-line import block), and every case I then verified BY HAND was a true positive. **Verified true positives:** `useToast`, `ToastProvider`, `AdminButton`, `ScanRecord` each appear only in their own defining file and are imported nowhere; `website/app/lib/scan-modules/infra.ts` exports ~20 scanner functions and the sibling `index.ts` never references `infra` at all; `bin/gatetest-promote.js`, `scripts/mine-mutations.js` and `editors/vscode/lib/map-findings.js` are not imported by anything in the repo, so their `main` / `walk` / `parseArgs` / `toVscodeSeverity` exports really are unconsumed. **So deadCode is working, and the count is a fact about this codebase rather than about the module.** Deliberately NOT touched: quieting an accurate detector is the one change that would make it useless, and it is the failure mode every other false-positive fix this session was careful to avoid (each paired with a positive control). **The real finding, worth its own attention:** ~145 genuinely unused exports concentrated in `website/app` — the same built-but-never-wired pattern KI #84 records for `try-fix.js` (5-layer orchestrator, zero production callers) and KI #74 for the shipped-rules loader. That is a codebase-cleanup question for Craig, not a scanner-tuning one. | LOW | RESOLVED — not a defect. Precision verified by hand; findings are true positives. Reframed as a real dead-code backlog in website/app (~145 unused exports), which is a cleanup decision, not module tuning. |
| 52 | **`src/modules/links.js` fetches its own module config (`config.getModuleConfig('links')`) but reads none of it — `checkExternal`, `checkInternal`, `timeout`, `retries`, `concurrency`, `excludePatterns` (all defined with real defaults in `src/core/config.js`) are dead schema.** Found 2026-07-20 while clearing a root ESLint `no-unused-vars` finding on the discarded `linksConfig` local. The module is 100% static-analysis today (regex-parses href/src/to/markdown-link patterns, resolves internal file references against disk) — there is no HTTP layer at all, so `checkExternal`/`checkInternal`/`timeout`/`retries`/`concurrency` describe a live external-link-checking feature (with retry + concurrency-pool semantics) that was apparently planned but never built. `excludePatterns` is the one field a static-only implementation could plausibly use today, but its matching semantics (glob? regex? substring?) aren't specified anywhere else in the codebase, so guessing at them risked shipping the wrong behavior. Deliberately NOT built this session — implementing real network link-checking is a separate, substantial feature (network calls, retry/backoff, a concurrency pool, new tests), not a fix that belongs inside an ESLint-config PR; a half-fix (e.g. wiring only `excludePatterns`) would itself be the kind of half-finished implementation the Bible warns against. | MEDIUM | Needs a scoped decision: build live external-link-checking for real (biggest lift, matches the config's evident original intent), or shrink `src/core/config.js`'s `links` schema down to what the module actually does (`excludePatterns` + maybe drop the rest) and stop advertising a feature that doesn't exist. |
| 54 | **CRITICAL — `/api/scan/run` and `/api/scan/fix` had NO server-side payment verification. Found 2026-07-20 during a full production-readiness audit (5 parallel research agents: pricing/Stripe, website-vs-tooling, PR/fix workflow, security posture, competitive research).** `sessionId` was optional on both; when omitted, `/api/scan/run` skipped its (already-weak) idempotency-check-that-doubled-as-verification block entirely and ran `scanRepo(owner, repo, tier \|\| "quick")` unconditionally — any tier, including Forensic ($399). `/api/scan/fix` had no payment-related code at all: no `sessionId` field on its input type, no Stripe lookup, nothing — the entire AI auto-fix-PR pipeline (real Anthropic spend, real GitHub PR) ran for free for anyone who knew the endpoint shape, throttled only by a 3-req/min IP rate limit. **Fixed**: `scan/run/route.ts` now requires `sessionId` for non-admin requests and verifies the Stripe payment_intent status is `"succeeded"` before running — a missing session, unpaid session, or Stripe lookup failure all now reject (402/503) instead of falling through. `scan/fix/route.ts` gained a `verifyFixPayment()` helper (same verification shape, mirrors scan/run's now-fixed pattern) called before any clustering/budget/AI work; the paid tier from Stripe metadata overrides the client-supplied `tier` (prevents claiming a higher tier than paid for). `scan/status/page.tsx`'s `runFix()` was silently omitting `sessionId` from its request body even though it was already in scope (used elsewhere on the same page) — now sends it. Verified the admin bypass (`isAdminRequest`, cookie or `x-admin-token` header) still works for the two legitimate internal callers that don't go through Stripe: `watches/tick/route.ts`'s scan/run call already sent the admin token; its **scan/fix** call did not (a genuine pre-existing inconsistency in the same file, fixed alongside this). **Side effect, not separately fixed**: `/api/scan/fleet` (bulk org-scan endpoint) calls `scan/run` with neither `sessionId` nor an admin token and is itself unauthenticated/unrated — confirmed unlinked from any page (dead/unused surface), so it now correctly gets rejected by the new gate rather than being wired up to bypass it; flagged here in case anyone revives it as a real feature later, since it would need its own auth story. 14 new source-text regression tests (`tests/payment-gate-scan-routes.test.js`) — full HTTP-level testing isn't possible in this sandbox (no real Stripe test-mode keys), so this was verified by code review + tests asserting the gate logic is structurally present and ordered correctly, not by an actual live checkout. **Cannot claim live end-to-end verification — flagging explicitly rather than overclaiming.** | CRITICAL | RESOLVED (code + tests). Live checkout flow not verified end-to-end in this sandbox — recommend Craig runs one real test-mode checkout → scan → fix cycle before/soon after this deploys. |
| 55 | **HIGH — mutation/chaos testing was actually running on the paid website Full/Forensic scans, contradicting the Bible's explicit "GitHub Action only" honesty commitment.** Found 2026-07-20 (same audit as KI #54). `CLAUDE.md`'s pricing section states plainly that mutation testing and chaos/fuzz pass "are NOT part of the website-only flow... they ship via the GitHub Action... because they need a CI runner." But `suites.full`/`suites.nuclear` in `src/core/config.js` still list `'mutation'` (and `nuclear` also lists `'chaos'`), and `website/app/lib/cli-engine-runner.js` — which runs the real CLI engine for Full/Scan+Fix/Forensic website scans — passed the suite straight through to `runSuite()` with no filtering. Since the `/tmp` workspace this runs in never gets `npm install`'d, `mutation.js`'s baseline `npm test` step would fail before doing anything useful, meaning a paying Full/Forensic customer likely saw a confusing, factually-wrong "tests must pass before mutation testing can run" failure finding rather than the feature being cleanly absent (not confirmed via a live paid scan, inferred from the code path — the mechanism is unambiguous either way). **Fixed**: `cli-engine-runner.js` now calls `runSuite(suite, { skipModules: ['mutation', 'chaos'] })` — reusing the engine's existing `skipModules` option (`src/index.js`), the exact mechanism CI's own `gatetest-full` job already uses (`--skip-module mutation`) for a related reason. One-line fix, no new mechanism. 1 new source-text regression test. | HIGH | RESOLVED. |
| 56 | **MCP `scan_repo` tool contradicted its own documentation — description and quick-start prompt said "free, no account needed," but `GATED_TOOLS` in `bin/gatetest-mcp.mjs` charged $29/mo for it.** Found 2026-07-20. The adjacent code comment (`// Free: check_health, list_modules, get_badge, scan_url, scan_repo, ...`) already correctly listed it as free — only the `GATED_TOOLS` set itself was wrong. Doubly confirmed as the right call: the underlying endpoint `scan_repo` proxies (`/api/playground/scan`) is itself already free/unauthenticated, so removing the paywall doesn't open any new free-compute surface — it just stops the MCP wrapper from paywalling something already open. The parallel "remote MCP" implementation (`website/app/lib/mcp-remote-core.cjs`, used by `packages/mcp-remote/`) already had `scan_repo` correctly in `FREE_TOOLS` — only the packaged stdio server (`bin/gatetest-mcp.mjs`) had the bug. **Fixed**: removed `'scan_repo'` from `GATED_TOOLS`. Updated CLAUDE.md's MCP free-tools list to include it. 2 new/updated tests. | — | RESOLVED. |
| 57 | **Security hardening batch — real vulnerabilities found during the 2026-07-20 audit's dedicated security-posture pass, none touching money/pricing.** (a) **SSRF in `/api/web/scan` and `/api/wp/scan`**: both had duplicated hostname-string blocklists (4 total copies across route.ts + stream/route.ts variants) with no DNS resolution and no redirect re-validation — a domain the attacker controls could resolve to a private/metadata address (DNS rebinding), or a URL that passed the initial check could 302 to one and `redirect: "follow"` would silently chase it. **Fixed**: new shared `website/app/lib/ssrf-guard.js` (`resolveAndValidateUrl`) reused across all 4 call sites — hostname-shape check (borrows the already-more-thorough `pentest/dns-verify.js` validator) plus real DNS resolution with every returned address checked against private/reserved/metadata ranges (IPv4 + IPv6, including IPv4-mapped IPv6). `website/app/lib/reliability/url-prober.js`'s `probeUrl()` switched from `redirect: "follow"` to a manual redirect loop (capped at 5 hops) that re-validates each `Location` header through the same guard before following. 23 + 6 new tests. (b) **No rate limiting on `/api/web/scan`/`/api/wp/scan` (+ stream variants)** despite being at least as expensive as the already-rate-limited `scanRun`/`scanFix` (full CLI-engine workspace + headless-browser dispatch) and completely unauthenticated — free-compute-abuse/DoS surface. **Fixed**: new `PRESETS.webScan` (3/min, matching `scanRun`), wired into all 4 routes. (c) **Command injection in `src/app-server.js`'s `cloneAndScan`**: `execSync(\`git clone --branch ${branch} ...\`)` shell-interpolated an attacker-controlled PR branch name (git ref-name rules forbid whitespace but not `$ ( ) ; & \| \`\`\`). Confirmed not reachable from the live SaaS today (superseded by the Next.js `/api/webhook` route; not what `Dockerfile`'s production `CMD` runs) but shipped, documented, `npm run app`-runnable. **Fixed**: `execFileSync('git', [...])` array-args form, no shell. 1 new regression test. (d) **`/api/fixes` POST auth used `!==` instead of `crypto.timingSafeEqual`** — inconsistent with every other secret check in the codebase. **Fixed.** (e) **`isAuthorisedTick` (`scan-worker.js`, gates `/api/scan/worker/tick` + `/api/watches/tick`) failed OPEN when `CRON_SECRET` was unset** — a deliberate "first-deploy/local-dev lenient" design that was confirmed, via the live `/api/status` endpoint, to have been left on indefinitely in production, meaning these endpoints currently accept unauthenticated ticks. **Fixed to fail closed**, matching every other secret-gated route in the codebase — **but this requires `CRON_SECRET` to already be set in the Vercel production environment before/immediately after this deploys, or continuous-scan tick processing stops firing entirely.** 2 updated tests (the old "fails open when unset" test now asserts the opposite). (f) Deleted `ContinuousScanning.tsx` — confirmed unimported by any live page, and its "Full automated penetration testing against staging" bullet contradicted the correct, live `PentestComingSoon.tsx` ("coming soon" + waitlist). **Not fixed, logged as follow-ups**: Next.js is behind on patches including a real SSRF advisory in the framework itself (`GHSA-c4j6-fc7j-m34r`) — a real version jump needing its own regression pass, deliberately not bundled into this security batch. **Good news confirmed during this audit and needing no action**: the "penetration tested" marketing claim Craig was specifically worried about was already correctly handled (`PentestComingSoon.tsx` already says "coming soon" with a waitlist, dated 2026-07-14 — a prior instance of this exact directive), and `/trust` already honestly states GateTest is not SOC2-certified yet. | HIGH | RESOLVED except the Next.js bump (tracked separately below) and the `CRON_SECRET` production env var, which only Craig can set (Vercel dashboard access). |
| 59 | **Next.js is behind on security patches, including a real SSRF advisory in the framework itself** (`GHSA-c4j6-fc7j-m34r` — "Next.js vulnerable to server-side request forgery in applications using WebSocket upgrades") plus a middleware/proxy-bypass chain and a high-severity DoS. Found during the 2026-07-20 security audit's `npm audit` pass on `website/` (9 vulnerabilities: 1 low, 7 moderate, 1 high). Current pinned version is `next@16.2.4` (`website/package.json`); `npm audit fix --force` reports the fix needs `next@16.2.10`+, "outside the stated dependency range." Deliberately NOT bundled into the same session as the SSRF fixes in KI #57 — a framework version bump is a bigger, riskier change needing its own regression pass, not something to rush alongside payment-flow and route-level SSRF fixes. Root `npm audit` separately found 2 vulnerabilities (1 moderate, 1 high) in transitive deps (`hono`, `qs`) — lower priority, `npm audit fix` (no `--force`) should clear those without a major bump. **RESOLVED 2026-08-21 — Craig scheduled the dedicated session this row asked for.** Two things had drifted since the 2026-07-20 audit: next was already at 16.2.10 (the version the SSRF advisory needed — bumped by an intervening session without closing this KI), but npm audit had grown to 14 vulnerabilities in `website/` (7 high: brace-expansion DoS ×3, postcss path traversal, sharp/libvips CVEs) and 8 at root (hono, qs, js-yaml et al.). Fixed: next 16.2.10 → 16.3.1 (minor, exact-pinned — pulls patched postcss + sharp), plain `npm audit fix` in both workspaces (no `--force`, no major bumps, no new deps — stays inside the approved stack). **Both workspaces now report 0 vulnerabilities.** Regression pass on the new framework: fast 7190/7190, heavy 329/329, `next build` clean with zero warnings, 121 modules load. | — | RESOLVED 2026-08-21 — next@16.3.1, `npm audit` clean in website/ and root. |
| 58 | **Website-vs-tooling consistency fixes, same 2026-07-20 audit.** `website/app/wp/page.tsx` said "94-module QA gate" (stale; now wired through the `TOTAL_MODULES` single-source-of-truth instead of a fresh hardcoded literal). `website/app/preview/_components/Nav.tsx` had 2 remaining "110 Checks Architecture" references missed by the 2026-07-19 partial fix (commit `b71ea2c`) for KI #44 — corrected to 120; the underlying finish/delete/park decision for the orphaned `/preview` route itself is still open (see KI #44). `website/app/lib/stripe-checkout.js` had drifted from the real checkout route (missing `scan_fix`/`nuclear`/`continuous`/`mcp` tiers, stale "all-84" module count on `full`) despite its own header comment mandating it mirror `route.ts` — root cause was two hand-written copies of the same `TIERS` table; fixed by extracting a single zero-dependency `website/app/lib/checkout-tiers.ts` that both `checkout/route.ts` and `stripe-checkout.js` import, closing the drift permanently instead of re-syncing it once more (confirmed a direct `require()` of `checkout/route.ts` itself doesn't work outside the Next.js build — it pulls in `next/server`, unresolvable via plain Node — hence the standalone file rather than exporting from the route). Homepage pricing section previously said "Quick and Full scans are free via the open-source CLI" directly above cards charging $29/$99 for the same tiers — reframed (Craig's chosen direction) to make explicit that the engine is free if self-hosted, and the paid tiers buy the same scan hosted with zero setup. Updated 2 existing test files (`tests/pricing-consistency.test.js`, `tests/heavy/marketing-claim-verification.test.js`) whose source-text price/tier assertions were scraping literals directly out of `checkout/route.ts` and broke once those literals moved to `checkout-tiers.ts` — both now correctly assert against the new architecture. | — | RESOLVED. |
| 60 | **The /web and /wp URL-scan paywalls have NO purchasable path** — found 2026-07-23 (launch-readiness audit, website-parity pass). The "Unlock for $29/$19" PaywallCard CTA links to `/api/checkout?tier=...` as a GET, which returns a JSON tier listing (checkout only works via POST); worse, the /wp CTA references `tier=wp_health`, which does not exist in `checkout-tiers.ts`, and even the POST path requires a github.com/gluecron.com repo URL — which a URL-scan customer doesn't have. The server-side fulfillment DOES exist (`resolveFullReportAccess` verifies a paid Stripe session and unlocks the full report) — what's missing is the checkout entry: a URL-scan tier (and the $19 WP price point) in Stripe + `checkout-tiers.ts`, a `url` metadata field, and a success_url that returns to /web//wp with the session id. Deliberately NOT built 2026-07-23: creating a new $19 price point and new Stripe tier structure is Boss Rule #3/#6. Everything else on these pages works — the free preview funnel converts into a dead end at the exact moment of purchase intent. | — | RESOLVED 2026-07-23 (overnight session, Craig's full-authority directive: "you have my full authority... change any code, do anything you need to do"). Built end-to-end: `web_scan` $29 + `wp_health` $19 tiers in `checkout-tiers.ts` (inline price_data, prices exactly as the pages already advertised), checkout route accepts `url` for URL tiers with success_url returning to /web / /wp carrying session_id+url, UrlScanFlow auto-runs the scan with the paid session attached, existing `full-report-auth` verifies paid server-side. PaywallCard CTAs now point at the working /checkout page. NOT yet live-tested against real Stripe test mode — that's on Craig's morning checklist. |
| 61 | **`/api/scan/status` fabricates per-module progress while a scan runs** — hardcoded module names with synthesized check counts (`checks: 5 + (i * 3)`) and fake durations (`website/app/api/scan/status/route.ts:88-120`). Contradicts the honesty standard the rest of the product now holds (and the README's "we don't tell you it's fixed, we show you the green"). Needs a real progress channel (worker-reported module completions) or an honest indeterminate state ("scanning — 88 modules queued") with no invented numbers. Found 2026-07-23. | — | RESOLVED 2026-07-23: animation module list now sourced from `scan-animation-modules.js` (drift-tested against the engine's real full suite minus mutation/chaos in `tests/scan-animation-honesty.test.js`), fabricated check counts/durations removed (stay 0 until real results land), payload self-identifies `estimated: true`. A real mid-scan progress channel remains future work but nothing invented is displayed anymore. |
| 62 | **The public composite Action (`action.yml`) is not diff-aware** — it scans the whole repo on every PR event, surfacing pre-existing findings on unrelated PRs (the classic SAST adoption killer). The engine already supports `--diff` / `--pr` / `--since` (src/core/runner.js) and the internal `integrations/github-actions/gatetest-gate.yml` template already uses `--diff` on PRs; the public Action never passes any of them. Fix shape: add a `diff-only` input (default true on pull_request events) that appends `--pr`. Found 2026-07-23. | — | RESOLVED 2026-07-23: `diff-only` input added (default true), on pull_request/pull_request_target events fetches the base ref and passes `--pr`; engine falls back to full scan if the diff fails. action.yml suite 28/28. |
| 63 | **npm `@gatetest/cli` is one release behind the repo (1.58.1 vs 1.59.0)** — the flywheel/telemetry/noise-model features marketed on the site aren't in the published CLI. History: (a) bare `node --test` hang in publish.yml FIXED 2026-07-23 (`--test-force-exit`); (b) dead `NPM_TOKEN` → Craig created a granular token 2026-07-25 (npm no longer offers classic tokens) → auth WORKED but publish died on **EOTP** (account 2FA mode demands a one-time code from any token auth; provenance signing succeeded, so only the final PUT failed). **Pivot 2026-07-25: publish.yml converted to OIDC trusted publishing** — no token, no expiry, no OTP: `npm install -g npm@latest` step added (Node 22's npm 10.x predates trusted publishing; needs ≥ 11.5.1), all `NODE_AUTH_TOKEN` usage removed, failure guidance rewritten with the exact Trusted Publisher form values (GitHub Actions / crclabs-hq / GateTest / publish.yml / no environment). **RESOLVED 2026-07-25: Craig completed the Trusted Publisher setup and dispatched the workflow — `@gatetest/cli@1.59.0` is LIVE on npm** (verified via `npm view`: dist-tag latest = 1.59.0, published 2026-07-24T21:15Z). Publishing is now zero-maintenance: every future `git push --tags` (or manual dispatch) publishes via OIDC with no credential to rotate. Residual housekeeping (low stakes): delete the now-unused `NPM_TOKEN` repo secret + revoke the 2026-07-25 granular token on npmjs.com; if `@gatetest/mcp-server` ever publishes from this workflow, configure the same trusted publisher on that package. NOTE: 1.59.0 predates today's crawler-auth work — the next version bump + tag will carry it. | — | RESOLVED — npm is current at 1.59.0, publish pipeline is token-free. Minor cleanup optional. |
| 70 | **Live crawler could not authenticate — authed sections (/dashboard/*) were silently untested.** Reported 2026-07-25 by the Fable agent working on Vapron: `--crawl` had no cookie/header/session support, so it hit the login redirect and the agent drove the dashboard with token-injection instead of GateTest — the exact "agents bypass the tool because the tool can't do the job" failure. **FIXED 2026-07-25**: `modules.liveCrawler` now accepts `headers` / `cookie` / `storageState` (Playwright storage-state JSON); CLI flags `--crawl-header "Name: value"` (repeatable), `--crawl-cookie`, `--crawl-storage-state` on both `--crawl` and `--crawl-loop`; values support `${ENV_VAR}` expansion so secrets stay out of committed config. Security invariant (`src/modules/live-crawler-auth.js`): auth material is same-origin-gated everywhere — HTTP engine merges headers only for the target origin (redirects to another origin drop auth), browser engine uses `context.route` interception instead of `setExtraHTTPHeaders` so third-party requests never see the token, and cookies go through `addCookies` (domain-scoped natively). New checks: `crawl:auth` (session active), `crawl:auth-wall` (warning: pages redirected to login, no session configured — with the fix commands in the suggestion), `crawl:auth-rejected` (error: auth configured but still bounced — expired token), `crawl:auth-config` (missing storageState file). Also fixed en passant: `--crawl` used to clobber file-based `modules.liveCrawler` config entirely; now merges. 14 tests in `tests/live-crawler.test.js` incl. end-to-end auth-wall/leak tests against local servers. **Hosted half SHIPPED same day (Craig-authorized 2026-07-25 — "shouldnt it be availble for both local and off"):** `/api/web/scan` accepts optional `auth: { headers, cookie }` (validated: ≤10 headers, name/value caps, CR/LF-injection rejected, malformed auth 400s rather than silently scanning unauthenticated); threads into `modules.liveCrawler` engine config (same-origin gating engine-side) and into `url-prober`'s live probe (new `authHeaders` option, dropped on any cross-origin redirect hop). Tokens are per-request only — never stored, never logged (test-enforced), never echoed. Response carries `authenticatedScan` honesty flag. **Still open:** the Vapron runtime browser worker does NOT receive the session (forwarding customer tokens to the worker tier needs its own design) — response `runtime.note` says so honestly; wp/scan + stream variants not yet auth-enabled. Tests: `tests/web-scan-auth.test.js`. | — | RESOLVED (CLI/engine + hosted web/scan). Runtime-worker session forwarding + stream/wp variants = open follow-up. |
| 71 | **CRITICAL-class silent degradation: the hosted URL scan ran the entire engine `web` suite with NO targetUrl — `GateTestConfig` never had the `.set()` method (or `.data` field) that `/api/web/scan/route.ts` guarded on, so both injection branches silently fell through.** Every URL-driven module in the web suite (liveCrawler, seo, links, performance, apiHealth, ...) no-op'd with "no URL configured" on every paid and free /web scan; the findings customers saw came only from the separate `url-prober` live probe and the Vapron runtime callback. Found 2026-07-25 while wiring hosted authed crawls (the auth config had to thread through the same dead path). **Fixed**: real dot-path `set(keyPath, value)` on `GateTestConfig` (write twin of `get()`, creates intermediate objects), route updated to use it for targetUrl/webUrl + crawl auth. 3 regression tests in `tests/web-scan-auth.test.js`. Not live-verified against production Stripe/paid flow — recommend a live /web scan spot-check after next deploy confirms module findings (not just probe findings) appear. | — | RESOLVED (code + tests). Post-deploy spot-check recommended. |
| 72 | **Self-serve billing portal — SHIPPED 2026-07-25 (Craig-authorized: "lets do this i dont have team to sort through dispute resolutions").** Customers had no way to upgrade/downgrade/cancel/update card without emailing support — refund-dispute risk. Built: `POST /api/billing/portal` { email } → looks up `stripe_customer_id` across continuous + mcp subscription stores → creates Stripe billing-portal session(s) (capped 3/request) → EMAILS the link via Resend (`sendBillingPortalEmail` in digest-mailer.js) — the link never travels in the HTTP response and the response is identical whether or not the email matched (no subscription-existence oracle; test-enforced). Rate-limited 3/min (`PRESETS.billingPortal`). `/billing` page (email form, checkout-page house style) + footer "Manage Subscription" link. Lib: `website/app/lib/billing-portal.js` (13 tests, `tests/billing-portal.test.js`). **Craig action before it works live: activate the Customer Portal once in the Stripe dashboard (Settings → Billing → Customer portal → Save) in BOTH test and live mode — without that saved configuration Stripe rejects portal-session creation.** Also requires `RESEND_API_KEY` + `STRIPE_SECRET_KEY` env on Vapron (route 503s honestly if missing). | — | RESOLVED (code + tests). Craig: one-time Stripe dashboard portal activation + env vars on Vapron. |
| 73 | **CRITICAL — the GitHub App install flow never posted commit statuses for third-party installers (broke the free-tier's core promise AND would fail Marketplace review).** Found 2026-07-25 by a repo-side audit while prepping the under-review Marketplace submission. `website/app/lib/github-callback.js`'s `resolveGitHubToken()` only read env PATs (`GATETEST_GITHUB_TOKEN`/`GITHUB_TOKEN`); `sendGithubCallback` was called from `scan/worker/tick/route.ts` with NO token, so for a customer who installed the App (the entire free flow) but set no PAT, it logged "no GitHub token configured — skipping feedback" and posted nothing. With Craig's own PAT set it was worse — a status POST to a stranger's repo 404s. The App-capable resolver (`github-app.ts:resolveGithubToken(owner,repo)` — JWT → `/repos/{o}/{r}/installation` → mint installation token) already existed and was used correctly by the CI-fix route, just never wired into the status/comment callback. **Fixed**: `sendGithubCallback` accepts an explicit `opts.token` (App installation token) that takes precedence over env PATs; `dispatchCallback` in worker-tick mints the per-repo App token via `resolveGithubToken` and passes it (falls back to PAT/skip when the App key isn't configured — self-hosted path unchanged). 2 new regression tests (App token posts with no PAT; App token beats env PAT). A Marketplace reviewer installing on their own repo will now actually see a status. Requires `GATETEST_APP_ID` + `GATETEST_PRIVATE_KEY` set on Vapron (already needed for the App to function). Also fixed same audit: the stale PAID-playbook framing in `integrations/marketplace/README.md` (would have recreated the 2026-05-14 rejection if followed — header/short-desc/setup-step corrected to free-only, deferring to canonical `listing.md`), screenshot guide's inaccurate "120 modules" App-check + "Nuclear" tier name, and listing.md's undeclared `workflow_run` webhook event + Issues-permission-scope verification callout. | CRITICAL | RESOLVED (code + tests). Craig: verify live commit-status posting on a test-repo install; confirm live App permission set matches listing.md. |
| 69 | **`/api/scan/worker/tick` only accepted the custom `x-vercel-cron-secret` header — which nothing but our own internal kicks send. Vercel cron invocations authenticate with `Authorization: Bearer <CRON_SECRET>`, so the moment Craig set CRON_SECRET in production (the KI #57e deploy prerequisite), every real cron tick would have 401'd and queue processing would have silently died — the exact outage the fail-closed fix was supposed to prevent, from the opposite direction.** Found 2026-07-23 while preparing Craig's CRON_SECRET runbook (the sibling `watches/tick` route already read the bearer header correctly, and `ci-fix/route.ts` even documented bearer as "same as /api/scan/worker/tick" — describing what the route should have done but didn't; `x-vercel-cron-secret` had exactly one reference in the codebase: the reader). Fixed: route accepts both the bearer token (Vercel cron) and `x-vercel-cron-secret` (internal kicks from `events-push.js`/`github-events.js`). 3 new source-text contract tests in `tests/scan-worker-tick.test.js`. NOTE for a future hardening pass: `watches/tick`'s `authorizedTick` also short-circuits on `x-vercel-cron: 1` — safe only if Vercel strips inbound `x-vercel-*` headers from external traffic; worth verifying and removing if not. | — | RESOLVED — CRON_SECRET can now be set safely. |
| 64 | **MCP-tier copy sold software Craig made free — fixed 2026-07-23 (launch-readiness audit #2, same day as KI #60-#62).** The 2026-07-23 repositioning (KI #39 close-out: local stdio server 100% free, $29/mo sells the HOSTED endpoint) had flipped `tools-data.ts` to all-free and emptied `GATED_TOOLS`, but the surfaces that SELL the tier were never updated: `Pricing.tsx`'s mcpPlan still listed "Eyes/Ears/Hands + full-suite local scans" (all free via `npx @gatetest/mcp-server`) as the $29/mo value, README's tier table said "unlocks the premium Eyes/Ears/Hands tools", and `/mcp/page.tsx` was internally incoherent — its own tool table rendered FREE on all 24 rows while the hero/FAQ/metadata/three CTAs around it sold those same local tools for $29/mo ("Everything else requires a key"). A paying customer discovering the free npx path would rightly feel scammed. All rewritten to sell what the code actually gates: hosted endpoint for claude.ai web/mobile + locked-down machines, hosted scans, hosted AI fix/diagnose, hosted scan history; free local path now shown FIRST on /mcp. **Residual (Craig or API edit): the published GitHub release v1.1.3 body carries the same stale "Premium tools unlock with a gtmcp_ key" copy — editing a published release is public comms.** | — | RESOLVED in repo; v1.1.3 release body still stale (Craig action). |
| 65 | **Marketplace/WordPress doc bundle still carried 67/39/90-module and "$199/month" claims — the KI #44 sweep missed non-website surfaces.** Fixed 2026-07-23: `integrations/marketplace/README.md` (plan table said "$199/month" for one-time Scan+Fix, "all 67 modules"; Step 6 rewritten around the Free-only-plan decision that the canonical `listing.md` already records — the 2026-05-14 rejection was for describing paid plans), `screenshots.md` (5× "67 modules"/"800+ checks"), `PUBLISHING.md` ("all 90 modules"), `integrations/wordpress/gatetest/readme.txt` (67/39 counts, "$99 includes AI fix PR" — wrong, that's the $199 tier; "no subscriptions" — false; "first scan free" — unverified claim, removed), `website/app/admin/tabs/RepoScanTab.tsx` ("Quick (39 modules)" → 41), and the CI assertions `.github/workflows/ci.yml` ("at least 90 module-list lines") + `publish.yml` ("Verify all 90 modules load") → 120, so a module-count regression below 120 actually fails CI now. NOTE: `website/app/scans/page.tsx` and README's proof bullets deliberately keep "39 modules" — they honestly label HISTORICAL scans run on the April-2026 engine; do not "fix" those. | — | RESOLVED. |
| 66 | **No repo-wide baseline mode ("only fail on NEW issues") — the single biggest adoption-killer gap vs 2026 table stakes.** Competitive + demand research (2026-07-23, three parallel agents) is unanimous: first-scan noise is why teams abandon scanners in week one, and only-new-code enforcement is now standard (Sonar "clean as you code", diff-aware everything). GateTest has `--diff`/`--pr` (changed-files scoping) and the Action defaults to diff-only on PRs, but there is NO "snapshot existing findings as a baseline, then only fail on regressions repo-wide" — a dev onboarding a mature repo can't run the full suite without eating the whole backlog. Fix shape: `gatetest --baseline` writes a fingerprint file (module:rule:file:content-hash), subsequent runs subtract baselined findings from the gate decision (still visible, like suppressedChecks); `--init` offers to capture it. Also: console reporter has no top-N cap — first-run output on a mature repo is hundreds of findings dumped raw (the plain-summary recap exists but sits below the flood). **SHIPPED, and the documented guarantee was not actually held — fixed 2026-07-30.** Both halves exist: `gatetest --baseline` writes `.gatetest/baseline.json`, the runner suppresses matched findings with `suppressReason: "baseline"` (visible, not blocking) and reports "N pre-existing finding(s) baselined", and the console reporter does cap output (`BLOCKING_SHOWN` + "N more finding(s) not shown — gatetest --all"). Verified end-to-end on a scratch repo: 2 blocking → `--baseline` → 0 blocking with "Nothing NEW is blocking", and a new finding in a NEW file still blocks. **The defect:** the fingerprint is `module::rule::file` with numeric segments stripped (deliberate — line moves must not un-grandfather), so every finding of one rule in one file shares it. The matcher compared `currentCount <= allowed` PER CALL, which only works for modules that aggregate instances into one check (secrets, links). Most modules emit one check per finding, so two empty catches in a baselined file each asked `1 <= 1` independently and BOTH were grandfathered — a baselined file became a permanent dumping ground for new findings of an already-baselined rule, in the files most likely to be edited. Measured before the fix: adding a second empty catch kept the gate GREEN while reporting "7 pre-existing finding(s) baselined" against a baseline recording 6. **README.md:132 already promised the opposite** ("the count is tracked per file, so adding a second secret to a file that already had one baselined re-blocks the gate — you can not sneak a new problem in behind an old one"), so this was documented behaviour the code did not deliver. `has()` now tallies what each fingerprint has claimed across the run and stops at the allowance; which instance blocks is arbitrary (the count carries the meaning, not the identity) and that is stated in the code. 7 regression tests. **Website sync (Craig 2026-07-30):** the capability had ZERO customer-facing coverage despite being, per this KI, 2026 table stakes — added to `/quickstart`. | HIGH | RESOLVED — baseline mode shipped, the count bug fixed, website synced. Reopen only if first-run noise is reported again. |
| 67 | **Website has no configuration/suppression docs page** — `/docs` contains only `docs/api`. `.gatetest.json` (dozens of options in `src/core/config.js`), `.gatetestignore` syntax, `--noise`, and auto-softening are undocumented on the site; README now covers suppression (fixed this session) but the site should too. The false-positive story is a genuine differentiator per competitive research — it deserves a page. **RESOLVED 2026-07-30 — /docs/configuration shipped.** Covers the full .gatetestignore grammar (all five forms, taken from src/core/ignore-file.js rather than paraphrased), the suppressed-is-not-hidden guarantee, why a path-scoped ignore does NOT feed auto-softening (excluding a fixture dir must not teach the engine to distrust an accurate module), gatetest --noise with real output, auto-softening, baseline mode, .gatetest.json + --init, --report-only vs --strict, and a which-one-should-I-use table. Every feature was run before being documented (--noise and --init verified working, baseline verified end-to-end in KI #66) so the page describes behaviour rather than intent. Linked from /quickstart and added to the sitemap. | MEDIUM | RESOLVED — page shipped, linked, in the sitemap. |
| 68 | **Two competing unshipped VS Code extension trees** (`editors/vscode/` JS + `vscode-extension/` TS, publisher GateTestHQ) with no canonical choice; site honestly says no extension ships yet (`how-it-works/page.tsx`). Also: `--ci-init gitlab\|circleci` generators exist (`src/core/ci-generator.js`) but are documented nowhere — README/site only show GitHub Actions. **CI-GENERATOR HALF DONE 2026-07-30.** `--ci-init github\|gitlab\|circleci` was verified working before being written up (all three exit 0 and emit conventional paths: `.github/workflows/gatetest.yml`, `.gitlab-ci.yml`, `.circleci/config.yml`; the generated configs are substantial, not stubs — quick diff-scoped scan on merge requests, full scan on main, separate security stage, JUnit+SARIF wired to native test reporting). Documented in README (new "Wire it into CI" section) and on `/quickstart`, whose GitLab/CircleCI answer had been telling people to hand-write a pipeline that a generator already writes for them. **Extension-tree half left for Craig:** choosing between `editors/vscode/` (JS, 6 files) and `vscode-extension/` (TS, 5 files) and deleting one is a product-direction decision about an unshipped surface, not cleanup — I am not deleting someone else's unshipped work on a LOW-severity row. | LOW | Craig decision — which VS Code extension tree survives. CI-generator documentation half is done. |
| 53 | **RESOLVED same session — root-level ESLint coverage added.** `src/`, `bin/`, `lib/`, `integrations/` (~71k lines, 237 files) had zero lint coverage — only `website/` had ESLint. Craig-authorized the new root `eslint`/`@eslint/js` devDependency 2026-07-20. Added `eslint.config.mjs` (CommonJS + Node globals for the four dirs, a browser-globals override for the 13 files that embed `page.evaluate()` callbacks, `no-control-regex`/`no-misleading-character-class` off since this codebase's whole job is matching control-character/homoglyph obfuscation patterns) plus `npm run lint`, a `lint` sweep step (`lib/sweep-steps.js`), and a CI `pre-merge-sweep` step. Fixed all 206 findings the first pass surfaced — mechanical (47 `no-useless-escape`, all genuinely redundant escapes with identical matched-character-set before/after) plus 53 `no-unused-vars`, most deleted as dead code but several were real, unrelated bugs the lint pass incidentally surfaced and fixed: `compatibility.js` computed `hasViewportMeta`/`hasPolyfill` but never acted on either (now both gate real checks); `cross-file-taint.js` computed a `prefix` distinguishing cross-file vs same-file taint but hardcoded the cross-file string into every check ID regardless (now uses `prefix`), and dropped its own `totalCrossHops` counter on the floor at summary time (now reported); `security.js`'s `.gitignore` completeness check computed `exists` (whether the file/pattern is even present in the project) specifically to gate flagging, then ignored it — unconditionally flagging `node_modules`/`.env` regardless of whether they existed, and **never once checking `.pem` keys at all** (now gated on `exists`, extended to cover `.pem`). Separately found and fixed a pre-existing Windows bug while wiring the new lint step into the sweep: `spawnSync('npx', ...)` throws `ENOENT` on Windows without `shell:true`, which was already silently breaking the `build` sweep step locally (not just the new `lint` step) — fixed via a shared `spawnNpx()` helper that builds one pre-joined command string for `shell:true` on win32 (avoids Node's `DEP0190` unescaped-args-array warning) and the plain array form elsewhere. Also had to broaden `eslint.config.mjs`'s `ignores` list after discovering GateTest's own pre-existing `lint` module (`src/modules/lint.js`) auto-detects any root `eslint.config.*` and runs bare `eslint .` — which, before the fix, walked into `tests/`, `editors/`, etc. (untouched by this config's `files` patterns) and false-positived on legitimate CommonJS top-level `return` under ESLint's module-mode parse default. Full fast suite 6418/6418, heavy suite 311/311, website build clean, 120/120 modules load, self-scan clean except the long-standing `benchmarks/bench-target/` fixture corpus (KI #31, unrelated). | — | RESOLVED. |
| 74 | **CRITICAL / HONESTY — the flywheel is marketed by name but is inert in the shipped product.** Found 2026-07-26 audit. `website/app/how-it-works/page.tsx` and `FlywheelTable.tsx` sell auto-distill to customers, and `src/shipped-rules/*.json` ships `"promotedFromCustomers": 3-7` + `winRate` 0.85-0.99 with an identical `promotedAt` across all 8 files — **fabricated provenance for a product with zero customers**, all committed in one commit (`dbf1cbd`, 2026-05-17) and never regenerated since. The code behind it does not run: (a) `recipePath` is never passed by either production caller, so `distillRecipes` is unreachable (`cli-fix-orchestrator.js:341` is a permanently-false branch); (b) `flywheel-playback-engine.js:45-51` loads `auto-distill`/`fix-telemetry` by relative path into `website/`, which `.npmignore` excludes from the published package — every `npm i -g` user silently gets `null` via a swallowed catch; (c) `distillRecipes` calls `distillClaudeFix` with the WRONG signature (passes `fixedContent`/`ruleKey`/`modelId`; it expects `issue`/`patchedContent`/`originalModel`) then returns `{distilled:true}` for a write that never happened; (d) the `scan_findings` table has zero SELECTs anywhere — write-only; (e) `GATETEST_RECIPE_STORE_URL` is unset so the cross-customer store never activates. Same honesty standard KI #61 was raised and fixed under. **The fabricated `promotedFromCustomers` values and the marketing copy are Boss Rule #8 (public-facing communication) — Craig must rule before either changes.** **PARTIALLY FIXED 2026-07-30 — three of the five code breaks closed, and the audit found two MORE.** **(b) FIXED, and it was the expensive one:** `auto-distill.js`, `fix-telemetry.js` and `recipe-store-remote.js` lived under `website/`, which `package.json` `files` (`bin/ src/ lib/`) and `.npmignore` both exclude — so in the PUBLISHED package they did not exist, and `_loadAutoDistill`'s `_safeRequire` turned "not installed" into "found nothing". All three moved to `src/core/` with one-line re-export shims at the old paths (the `datadog-client.js` pattern), and the loader now requires them directly at module load so a future packaging regression throws instead of silently disabling the flywheel. **(c) FIXED, and it was three bugs not one:** `distillRecipes` passed `fixedContent`/`ruleKey`/`module`/`fileExt`/`modelId` to a function whose contract is `{issue, originalContent, patchedContent, recipeStorePath, originalModel}` — so `issue` and `patchedContent` were always `undefined` and it bailed at the first guard; the success check was `if (!result)`, but every failure path returns the truthy object `{written:false, reason}`, so rejections were reported as `{distilled:true}`; and the id was read from `result.id` when the recipe is at `result.recipe.id`. Now verified by tests that actually write: a templatey diff lands on disk with the right ruleKey/module/fileExt/originalModel, and a non-templatey diff can no longer report success. **Why it survived:** all five pre-existing `distillRecipes` tests stopped at a guard, and the one that did reach the bug asserted only `typeof r.distilled === "boolean"`, which cannot fail. **(a) still open** — nothing passes `recipePath`, so `cli-fix-orchestrator.js:343` remains a permanently-false branch. **NEW #74f — the promotion deadlock, which no amount of fixing (a)-(e) resolves.** A freshly distilled recipe is written `confidence: "low"`. `executePlaybackSimulation` asks for stable recipes only (`includeLowConfidence: false`). Promotion to stable happens in `incrementApplicationCount` at `applicationCount >= 3`, and the only production caller of that is the playback path — which will never return a low-confidence recipe to apply. So: distil → low → never replayed → never counted → never promoted → never replayed. **A locally distilled recipe can never be used, ever.** Pinned by two tests that isolate the broken link: playback correctly skips the unproven recipe, and the SAME recipe replays once promoted by hand — proving the replay machinery is sound and only promotion is missing. Deliberately NOT fixed: the options are "replay unproven recipes" (auto-applying an uncertified patch to customer code — product risk, not a bug fix) or "count independent re-derivations" (the `duplicate` branch currently discards the evidence that Claude produced the same fix again — my recommendation) or "promote via the remote store" (needs `GATETEST_RECIPE_STORE_URL`, unset). **Craig's call — it decides when cached fixes get applied to customer code.** **NEW #74g — the same did-not-ship bug class in FOUR MORE shipped binaries, found by the new tripwire:** `bin/gatetest-doctor.js` (2 requires), `bin/gatetest-promote.js`, `bin/gatetest-reliability.js` and `bin/gatetest-train.js` all load from `website/…`. The first three are BARE top-level requires, so they **crash with MODULE_NOT_FOUND for every `npm i -g` user** — and `gatetest-reliability` is one of the three `bin` entries `package.json` declares, i.e. an advertised command that cannot start. Not fixed in this pass because each needs its own dependency tree moved (`ci-doctor/`, `recipe-promotion`, `reliability/cli-runner`, `session-telemetry`) and a half-moved tree is worse than a listed one; held as a shrink-only `KNOWN_DEBT` list in `tests/lib-shims.test.js` that fails if anyone adds to it OR forgets to remove a fixed entry. **Also fixed:** `lib/ai-ci-fixer-claude.js` and `lib/ai-ci-fixer-diagnose.js` required `../website/app/lib/prompt-injection-guard` — the anti-injection preamble and output leak scan, i.e. the module whose silent absence is least acceptable — while an identical shipping copy sat beside them at `lib/prompt-injection-guard.js`. One-line fix each. **New guard:** `tests/lib-shims.test.js` now asserts no shipped file requires across the `website/` boundary at all, which is what nothing was checking. **Marketing copy deliberately untouched:** this row already reserves it for Craig, so `how-it-works/page.tsx:177` still claims "Next time the same shape appears, the recipe layer wins and Claude is never called" — false while #74f stands. Recommended wording once he rules: *"records a recipe in your local store; a recipe replays, with Claude never called, once it has been confirmed enough times to be promoted."* Line 164 also names `website/app/lib/try-fix.js` as "our orchestrator" while KI #84 records it has zero production callers, and `src/core/shipped-rules.js` is consumed ONLY by that dead file — so the shipped recipe set is currently read by nothing that runs. **#74g THREE OF FOUR FIXED 2026-07-30.** `gatetest-doctor`, `gatetest-promote` and `gatetest-train` now load `ci-doctor/` (6 files), `recipe-promotion` and `session-telemetry` from `src/core/`, and all three start cleanly (`--help` exit 0, previously MODULE_NOT_FOUND for every npm user). **No shims needed, unlike auto-distill:** nothing under `website/app/` imported any of them — they were misfiled engine code, referenced only by `bin/` and tests. **Moving them into src/ exposed pre-existing defects that website/ eslint config had been hiding:** a dead `DEFAULT_AUTH = {}` constant, an unread `finding` parameter in `buildRecipeContext`, two inert `global-require` disables, and a bare `console.warn` in library code (now `process.stderr.write`, matching src/core convention). The `buildRecipeContext` doc comment also claimed it "merges sensible defaults per class" — it derives only `jobName`; corrected rather than left overstating. **Still open: `bin/gatetest-reliability.js`**, the most urgent of the four since it is a declared `bin` entry, and the deepest: `reliability/` is 9 files pulling in `ssrf-guard` → `pentest/dns-verify`, and `ssrf-guard` is live in four website API routes, so it needs shims rather than a straight move. Held as the single remaining `KNOWN_DEBT` entry in `tests/lib-shims.test.js`. **#74g CLOSED 2026-07-30.** `bin/gatetest-reliability.js` — the last of the four, a declared `bin` entry — now loads `reliability/` (9 files) from `src/core/`, with `ssrf-guard.js` and `pentest/dns-verify.js` moved alongside it. All four binaries start (`--help` exit 0). Shims left ONLY where `website/app/` imports the path live: `ssrf-guard.js` (four API routes) and `reliability/url-prober.js` (api/web/scan); both added to the identity assertions, which matters most for an SSRF guard — two copies would mean two different ideas of which hosts are safe to reach, and only one gets patched. `KNOWN_DEBT` is now empty and kept as an empty set so the guard stays armed. Two more defects surfaced by src/ eslint on the way in: a dead `require("path")` in cli-runner.js and an inert `global-require` disable in code-scanner-adapter.js. **MARKETING COPY + FABRICATED PROVENANCE RESOLVED 2026-08-21 — Craig authorized the honesty sweep.** how-it-works:226 now uses this row’s recommended wording (recipe recorded in the local store; replays, Claude never called, only once confirmed enough times to be promoted); the cost-trend claim is reframed as a design goal; and all 8 `src/shipped-rules/*.json` had the fabricated `promotedAt`/`promotedFromCustomers`/`winRate` fields STRIPPED, replaced with `"origin": "curated"` (fields are optional per validateShippedRule — loader verified, 8/8 still load). | HIGH | Craig decision still open on #74f only — how a recipe earns promotion (recommendation on file: count independent re-derivations via the `duplicate` branch). Code: (a) recipePath wiring still open; (b)+(c) FIXED; #74g CLOSED. |
| 75 | **The reliability corpus is not run by anything and cannot fail a build.** Found 2026-07-26. `grep -rin reliabil .github/` returns **zero matches** across all 13 workflows, despite `reliability-corpus/README.md:4-7` claiming "the nightly workflow runs every case here… opening a PR when drift is detected." `bin/gatetest-reliability.js` is not in `package.json`s `bin` map (unreachable for customers) and `cli-runner.js:129` hardcodes `exitCode: 0` ("never exit non-zero on case failures"). Real case count is **1 known-good + 3 known-bad**, not the 41 files the README implies (~30 are checked-in scan reports). Net: **nothing in CI can catch a false-positive regression** — which is why KI #43/#48/#49/#50 were each found by hand, one at a time. Highest-leverage single move for the zero-false-positives goal. **RESOLVED 2026-07-26 — and it needed four fixes, not the one this KI described.** (1) The runner treated a scanner ERROR as "found nothing": scanCode() returns {findings:[],error} rather than throwing, and runCase()'s try/catch only caught throws — so report-not-found/timeout/codeRoot-not-found all became a PASS on known-good cases. Measured: a fixture with 8 real errors + 16 real warnings reported passed:true with zero totals. (2) --project was passed RELATIVE while the child cwd was set to that same dir, so it resolved twice against itself and scanned a doubly-nested path that did not exist (it left a stray reliability-corpus/known-good/<case>/reliability-corpus/ tree, which is how it was found). (3) findLatestReport() accepted reports from PREVIOUS runs, so the current engine could be graded against stale output. (4) exitCode was hardcoded 0 — now --strict. Plus the actual FP guard: known-good now defaults to ZERO WARNINGS as well as zero errors, because all three floods (#48/#49/#50) were warning-severity and an error-only ceiling would have passed every one. New fixture known-good/modern-node-idioms reproduces all three shapes (node: imports, CRLF .env.example, ./validators + ./version). **Proven, not assumed:** reintroducing the #49 bare-split takes it 10→13 warnings and fails with "total warnings 13 > expected atMost 10", exit 1. Wired as .github/workflows/reliability-corpus.yml; gatetest-reliability added to package.json bin. The fixture .env.example was also silently excluded by the root .gitignore .env* rule — force-included, with .gitattributes -text so git cannot normalise away the CRLF the guard depends on. **Caveat for the first CI run:** the ceilings were calibrated on Windows; ubuntu may produce a small delta. If CI goes red on the first run, compare the reported counts against the knownNoise itemisation in the manifest before changing anything. | — | RESOLVED — corpus can now fail the build, verified against a real regression. |
| 76 | **The noise model is dead code in production — no writer exists for either input.** Found 2026-07-26. `noise-model.js:92-101` requires `runs>=3 AND dismissCount>=3 AND fireRate>=0.5`; `dismissCount` reads `false-positives.json` (written only by `memory.js` `dismiss()`, which has **zero callers** — there is no `--dismiss` flag) and `stats.suppressions` (written only by `persistent-memory.recordSuppression`, **zero callers** outside its own test). So `computePenalties()` always returns `{}` and `gatetest --noise` always prints "No modules softened yet." When it does fire it softens findings with no disclosure in default scan output (only a `flywheel-softened` entry in `confidenceSignals`). Website dismissals reach Postgres but the npm CLI never contacts it — **a dismissal changes nothing about the next scan, on any machine, ever.** **RESOLVED 2026-07-27 — and it took three fixes, not the one this KI described. Two of them were invisible to unit tests and only surfaced in a real CLI run.** (1) **The missing writer.** `.gatetestignore` suppressions now feed the store: putting a rule in that file IS the user declaring it a false positive, so it is the dismissal signal `recordSuppression` always wanted. `runner._buildSummary` emits `suppressedRules` (distinct `module:ruleKey` pairs, deduped per scan so one noisy file can't inflate the count), `scan-telemetry.recordScanFindings` persists them via a new batched `persistent-memory.recordSuppressions` (one load/save per scan, called AFTER `recordScan` because it only increments modules that already exist). Deliberately **excludes** `suppressReason === 'baseline'` — a baselined finding means "real, fix later", and counting it would soften modules for being accurate. (2) **Suppression was erasing its own evidence.** A suppressed finding is excluded from `errors`/`warnings`, so the module read as quiet and its `fireRate` COLLAPSED — measured on a live run, 1.0 → 0.2 — dropping it under the `fireRate >= 0.5` gate. The act of declaring a module noisy made it permanently un-softenable, so fix (1) alone would still have produced `{}` forever. `recordScan` now counts a suppressed finding as the module firing, carried by a new `suppressedChecks` count on `TestResult.toJSON()`. (3) **The store key was per-file-per-line, not per-rule** — real observed key `hardcodedUrl:hardcoded-url:localhost:src\cfg.js:1`, so the map grew without bound. New `runner._ruleIdentity()` strips the embedded file and trailing line. **Verified end-to-end, not just unit-tested:** a fixture repo with two offending files, five real CLI runs → `fireRate` holds at 1.0, one rule-level key, `computePenalties()` returns `{hardcodedUrl: 0.75}`, and `gatetest --noise` prints `hardcodedUrl 100% (5/5) 5 softened (×0.75)` — the first time that feature has ever produced output. 16 tests in `tests/noise-model-wiring.test.js`. **Still open (unchanged by this):** softening is disclosed only via a `flywheel-softened` entry in `confidenceSignals`, not in default scan output; and website dismissals still never reach the npm CLI. | — | RESOLVED (CLI/local path). Website→CLI dismissal sync remains a separate gap. |
| 77 | **False-positive guards are opt-in and only ~19% adopted (25 of 129 modules).** Found 2026-07-26. `BaseModule` offers `_isInsideStringLiteral` (used by **8** modules) and `_stripJsStrings` (**3**); there is **no shared test-path helper** — `TEST_PATH_RE` is copy-pasted verbatim into **20** module files, i.e. the KI #43 pattern institutionalised as copy-paste rather than fixed once. Also **135 bare `.split(LF)` calls across 74 module files**; only `env-integrity.js` and `lint.js` split CRLF-safely, so the KI #49 bug class can recur on any Windows/`core.autocrlf` checkout, with no lint rule preventing it. Highest-risk unguarded modules by blast radius: `auth-bypass` (no guards at all, highest severity class), `dead-code`, `web-headers`, `flaky-tests`, `retry-hygiene`, `n-plus-one`, `data-integrity`. Note `warningChecks` is never confidence-gated and 76 modules emit `severity: warning` — so most FP noise sits entirely outside the confidence system. **CRLF HALF ADDRESSED 2026-07-27 — and the severity claim did NOT survive measurement, so read this before repeating it.** Shipped: `src/core/text-lines.js` (`splitLines` / `detectEol` / `joinLines`), a mechanical conversion of **98 bare splits across 52 analysis-only modules** to `split(/\r?\n/)`, and a ratchet guard (`tests/crlf-safety.test.js`) that blocks any new bare split in a converted module and keeps the remaining debt an explicit, shrink-only list. The **22 unconverted modules are unconverted for a specific reason, not laziness**: each also contains `.join('\n')`, so if the split feeds an edit that is re-joined and written back, converting the split alone would strip `\r` and silently rewrite the customer's line endings — they need split and join converted together, by hand, with the write path checked. `joinLines()` exists for exactly that. **The honest result: this fixed no observed defect.** A LF-vs-CRLF parity scan over a fixture repo (quick suite, both line endings) produced **identical findings before and after** the conversion. Root cause of the non-effect: most `$`-anchored rules here are written `/...\s*$/`, and `\s` matches `\r`, so they were already CRLF-tolerant by accident; `dockerfile.js` similarly does `raw.replace(/\s+$/, '')` before `endsWith('\\')`. So this is **preventive hardening and a uniform invariant, not a bug fix** — KI #49 proves the class is real, but the current rules had largely dodged it. Recording this rather than claiming a win, for the same reason KI #78's overstatement had to be corrected. **TEST_PATH_RE HALF RESOLVED 2026-07-28 — and this one WAS a live bug, unlike the CRLF half.** The KI said "`TEST_PATH_RE` copy-pasted verbatim into 20 files", which undersold it: there were **6 materially different bodies**, so whether a path counted as test code depended on which module asked. Underneath that sat a real, provable defect: **`path.relative()` returns `tests\helper.js` on Windows and every one of those regexes required `/`**. Eight modules tested the raw value — `async-iteration`, `env-vars`, `hardcoded-url`, `import-cycle`, `openapi-drift`, `race-condition`, `resource-leak`, `ssrf` — so on **any Windows checkout** a file under `tests/` went unrecognised unless its NAME also carried `.test.`/`.spec.` (the extension branch is separator-independent, which is why this hid for so long). Findings in `tests/helper.js`, `tests/setup.js`, `spec/support/*.js` were reported at full severity; for `ssrf` that is a gate-**BLOCKING** error rather than an info. Fixed by `BaseModule._isTestPath(relPath)` — one canonical pattern (the union of all 6 variants) that normalises separators *inside the helper*, since call sites forgetting to normalise was the entire bug. 19 modules and 26 call sites migrated. **Measured, not assumed:** on GateTest's own repo (a Windows checkout) warnings went **820 → 813** — seven real false positives removed — with errors unchanged and the gate still passing. 29 tests in `tests/test-path-canonical.test.js`, including a ratchet that no module may re-declare its own copy. Deliberately left alone: `claude-compliance.js` (its variant also matches `mocks/examples/docs` — a broader "not shipped code" question; folding that in would start suppressing `docs/` findings for all 20 modules), and `dead-code.js` / `flaky-tests.js` / `typescript-strictness.js`, whose `TEST_FILE_RE`s are legitimately different questions (already `[\\/]`-safe, basename-only, and TS-extension-only respectively). **WARNING-CONFIDENCE HALF RESOLVED 2026-07-28, and it exposed a bug in the KI #76 fix shipped hours earlier.** First, a correction to this KI's wording: warnings ARE confidence-scored — `addCheck` runs `scoreFinding` for `WARNING` as well as `ERROR`, and applies the flywheel penalty. What was missing is that **nothing downstream ever read the number**, so for the 76 warning-emitting modules the score (and the noise model's softening with it) was computed and discarded. Errors have had a confident/soft split since confidence shipped; warnings never did. Added `softWarningChecks` + `flywheelSoftenedChecks`, surfaced in the summary and the console reporter: `Warnings: 814 (127 low confidence)` plus a `Softened: N finding(s) down-weighted from your .gatetestignore history` line. **These are reported sub-counts, not filters** — every warning still appears. Silently dropping warnings is the false-NEGATIVE direction, which a security tool must never take on its own initiative; it also closes the disclosure gap KI #76 left open. **Then the disclosure immediately paid for itself.** The first run reported **555 findings softened** on a repo whose `.gatetestignore` has five lines — because `benchmarks/bench-target/**` and `reliability-corpus/**` are **path-scoped** excludes, and the KI #76 wiring counted every suppression as a module-noise dismissal. So GateTest was down-weighting `secrets`, `codeQuality`, `deadCode` and 5 other **accurate** modules to ×0.5 for firing inside a deliberately-bad fixture corpus. Fixed: `ignore-file` now exposes `matchKind()` (`'moduleRule'` vs `'path'`), the runner records `check.suppressKind`, and only module-scoped entries reach the noise model — a bare `path/**` says "this directory is not real code" and implies nothing about any module's accuracy. Measured after the fix: softened **555 → 0**, and low-confidence warnings **593 → 127** (the 73% figure was an artifact of the bug, not a real noise reading). E2E-verified both directions: a path-only ignore yields `{}` penalties, a `hardcodedUrl:localhost` ignore still yields `{hardcodedUrl: 0.75}`. **Still open:** `_isInsideStringLiteral` at 8 modules / `_stripJsStrings` at 3, and the 22 CRLF rewrite-risk conversions. **CRLF HALF CLOSED 2026-07-28 — and the recommendation is DO NOT do the remaining 22 conversions.** Measured before acting, three independent ways: (1) a hand-built fixture repo, quick suite — identical findings LF vs CRLF; (2) `src/core` (54 real files) copied to LF and CRLF twins, full suite — **87 module runs, byte-identical output**; (3) `website/app/lib` the same way — **87 module runs, identical**. Root cause of the non-effect, now understood rather than guessed: the `$`-anchored rules here are nearly all written `/...\s*$/` and `\s` matches `\r`; the `endsWith` checks call `_trimEnd`/`trim` first; and most anchored patterns match **file paths**, which never carry a carriage return. The codebase is accidentally CRLF-tolerant. The 22 remaining modules each pair their split with a `.join('\n')`, so converting them risks silently rewriting a customer's line endings — a real downside against zero measured upside. Shipped instead: `tests/crlf-parity.test.js`, which asserts the property that actually matters (same bytes, different line endings, same findings) across 10 line-scanning modules including 7 of the unconverted 22 — plus a **negative control** that runs a deliberately CRLF-sensitive module through the same harness and fails if the harness doesn't notice. Without that control the parity assertion would have been unfalsifiable, since every real module already passes. Runs in ~180 ms. **Still open:** `_isInsideStringLiteral` at 8 modules / `_stripJsStrings` at 3 — note `_isCommentLine` was added in KI #88 as the companion guard. **STRING-GUARD HALF ADDRESSED 2026-07-28 — measured, and the worst offender was `security`.** Rather than adopting `_isInsideStringLiteral` module-by-module on faith, the gap was located by scanning a deliberately **all-inert** fixture: a file whose every dangerous token (`eval()`, `.innerHTML =`, `document.write()`, `child_process.exec()`, a password, a localhost URL) sits inside a doc string or a comment, so any finding on it is by construction a false positive. Result: **13 BLOCKING errors on a file where nothing executes.** The dangerous-pattern loop in `security.js` had no string or comment guard at all, and reported at full confidence because it passed no `column`, leaving the scorer nothing to judge (its whole-line fallback does not match `evalWarning: "…"`, which starts with an identifier). False positives that stop the gate are the worst kind — Bible Forbidden #25. Fixed: the loop now skips whole-line comments via `_isCommentLine` (KI #88) and matches via `_isInsideStringLiteral` at the match column, and carries `column` on every finding so the scorer can judge position instead of guessing. It also switched from `test()` to `exec()` with explicit `lastIndex` resets — the same trap that had silently disabled the secrets module's placeholder allow-list. Verified both directions: the four inert FPs are gone (13 → 10 blocking, and the 10 are project-level findings plus patterns that legitimately live in strings, e.g. a secret or URL inside a string is how those genuinely appear), while real `eval(u)` / `el.innerHTML = u` / `document.write(u)` in executing code still block — including code on a line carrying a trailing comment, since `_isCommentLine` is whole-line by design. 9 tests in `tests/security-inert-patterns.test.js`. **Second inert sweep 2026-07-28, broader fixture (SQLi, path traversal, prototype pollution, weak hash, insecure deserialisation, SSRF, CORS, JWT-none, cookie flags, TLS-off, money-float, await-in-loop — all inside strings or comments):** exactly **one** false positive across all 120 modules, `data:sql-injection` from `dataIntegrity`, and it was a **blocking error**. That module's check runs the regex over the whole file, so it had no position to guard on. Fixed by testing the position of the match START — in real code `query(` IS code, in a handbook string it is inside a string literal — which beats dropping to a line-by-line scan, since that would have lost the multi-line form. Findings now also carry a line number and column, which they previously did not. **And verifying that turned up a false NEGATIVE the KI never mentioned: the multi-line query form was never detected at all.** The pattern demanded `SELECT` immediately after the opening quote, so the extremely common ``db.query(`⏎  SELECT … ${id}⏎`)`` shape was invisible. Confirmed against the pre-change code rather than assumed, then closed with a `\s*`. 7 tests in `tests/data-integrity.test.js` covering doc-string, comment, single-line, multi-line and parameterised cases. Note the CRLF ratchet from the same KI caught a bare `split('\n')` introduced by this very fix — the guard working on its author. **Still open:** `_stripJsStrings` adoption (3 modules) and the remaining `_isInsideStringLiteral` call sites; two inert sweeps have now found no further FPs, so they stay unconverted until a measurement says otherwise. **FOURTH INSTANCE OF THIS CLASS FOUND 2026-08-29, in the website rather than the engine — and this one was live in production.** `website/app/api/scan/preview/route.ts` carried a PRIVATE copy of `parseDetail` while the canonical extractor (`website/app/lib/issue-extractor.ts`, used by the scan-status page and the admin Command Center) had already fixed the same shapes. The private copy required a `:<line>` segment before it would attribute a file; measured across `website/app/lib/scan-modules/*.ts`, only **15 of 86 detail templates emit a line number** while **57 emit `path: message`** with none. Result, confirmed by curling live production: **every finding from the free preview returned `"file": null, "line": null`** with the path still glued to the front of the message. Fixed by delegating to the canonical extractor and moving the helpers to `website/app/lib/preview-finding.ts` — a Next `route.ts` may only export handlers, so anything defined there is by construction untestable, which is exactly how it survived. **The same file also had severity decided by the file path**: the keyword heuristic ran against the raw string (which starts with the path), so `examples/error/index.js: uses legacy 'var'` was an **error** while `examples/auth/index.js: uses legacy 'var'` was a **warning** — and errors sort first, so inflated findings led the free preview. Inversely, **10 of the 13 shapes the `secrets` module can emit — including a live Stripe key, a GitHub PAT and a private key block — were classified `warning`**, the false-negative direction on our highest-severity module. Severity now follows explicit prefix -> emitting module -> keywords on the path-stripped message. Measured after: secrets-not-error 10 -> 0, and the seven path variants of one lint finding agree. 25 tests in `tests/preview-finding.test.js`, including a ratchet asserting exactly two `parseDetail` definitions exist under `website/app`. **The lesson generalises beyond helpers: a copy that no test exercises is not a copy, it is a fork.** | HIGH | Code work — pre-authorized. CRLF closed by measurement + parity guard; security + data-integrity guards shipped. |
| 89 | **Command injection was undetectable in its most common form, and two vulnerability classes have no detector at all.** Found 2026-07-28 by **inverting** the inert-fixture technique: instead of scanning inert code to find over-reporting, scan genuinely-vulnerable code to measure what is MISSED. A 10-vulnerability fixture (SQLi via concat and via template, command injection, path traversal, weak password hash, predictable token, SSRF, prototype pollution, hardcoded credential, money-as-float) was caught 6/10. **Fixed: command injection.** The only shell-exec rule was `/child_process.*exec\s*\(/`, which requires `child_process` on the SAME LINE — so it caught the inline `require('child_process').exec(...)` and missed the ordinary two-line shape (`const cp = require('child_process')` … `cp.execSync('ls ' + req.query.dir)`). New rule matches `exec`/`execSync` whose argument contains `+` or `${`, deliberately scoped: `execFile`/`spawn` take an argv array and are the SAFE alternative, so flagging them would punish the correct fix, and a static command string is not flagged. Verified all five negatives plus the doc-string case, and our own gate still passes. 6 tests. **STILL MISSING, no detector exists anywhere in the 120 modules — logged rather than half-built at 06:45:** (a) **weak password hashing** — `crypto.createHash('md5')` / `sha1` for passwords; `grep -rln md5 src/modules/` returns **nothing**, and every competitor (SonarQube, Snyk, Semgrep) ships this. (b) **prototype pollution** — `target[req.body.key] = req.body.value`; no `__proto__` detection anywhere. (c) **path traversal** — only `.createReadStream(req.` is matched, so the far more common `fs.readFileSync(path.join(root, req.query.file))` is missed. These are genuine competitive gaps against the Bible's "if a competitor does something we don't, that's a GateTest bug" standard, and each is a new detector rather than a fix to an existing one. **(a) WEAK PASSWORD HASHING — SHIPPED 2026-07-28.** New `_checkWeakPasswordHashing` inside the existing `security` module (deliberately a check, not a new module, so the Bible-tracked "120 modules" count is untouched — Forbidden #17). The hard part was never detecting md5; it was not drowning customers in false positives, since md5/sha1 are legitimate and ubiquitous for cache keys, ETags, content addressing and checksums. A rule flagging every `createHash('md5')` would fire constantly on correct code and train people to ignore the module. So it fires **only on credential context** within a tight ±2-line window. Two matching classes, and the distinction is load-bearing: the unambiguous words (`password`, `passwd`, `passphrase`, `secret`, `credential`, `api_key`) match as **substrings**, because `\bpassword\b` does **not** match `hashPassword` — that boundary silently broke the first version of this rule and camelCase is exactly how the vulnerable code is written in the wild; the ambiguous short words (`pwd`, `pin`, `token`, `salt`) stay word-bounded so `spinner`/`tokenizer` cannot drag an innocent hash in. The suggestion names bcrypt/scrypt/argon2id and says explicitly that switching to SHA-256 does **not** fix it, since the defect is speed, not collision resistance. 8 tests covering both directions: password/secret hashing caught; cache key, ETag, asset fingerprint, `spinnerTokenizerKey`, doc-string mention and sha256-for-password all correctly ignored. Our own gate still passes. **(b) PROTOTYPE POLLUTION — SHIPPED 2026-07-28.** New `_checkPrototypePollution`, again a check inside `security` rather than a new module (count still 120). Same precision problem, harder: `obj[key] = value` is one of the most common lines in JavaScript and is almost always fine, so a rule flagging dynamic assignment generally would bury the customer. It therefore requires the key to trace **directly** to request input (`req\|request\|ctx\|event` × `body\|query\|params\|payload`), which is the shape that actually lets an attacker set `__proto__` and poison Object process-wide. It stands down when the author has visibly defended the sink — a nearby `__proto__`/`constructor`/`prototype` denylist, `Object.create(null)`, `hasOwnProperty`, `new Map()`, or `freeze(` — using a wider ±6-line window than the weak-hash check, because a key denylist normally sits a few lines above the assignment rather than beside it. 7 tests, again weighted to negatives: denylisted merge, `Object.create(null)` sink, ordinary `obj[key] = value` with a local key, `Map`-based store, and the same sink quoted in a doc string all correctly ignored; `req.body`/`req.query`/`req.params` keys all caught. No new findings on our own repo or the inert fixture. **(c) PATH TRAVERSAL — SHIPPED 2026-07-28. KI #89 is now closed.** New `_checkPathTraversal`, again a check inside `security` (count still 120). The trap worth catching is that **`path.join` looks like a fix and is not one**: it normalises `..` rather than rejecting it, so `fs.readFileSync(path.join('/data', req.query.file))` with `?file=../../etc/passwd` escapes `/data` cleanly — and plenty of code is written exactly that way believing it is safe. Covers `readFile`/`writeFile`/`appendFile`/`createReadStream`/`createWriteStream`/`unlink`/`rm`/`open` (sync and async, plus `fs.promises`) and `res.sendFile`/`res.download`. Stands down on the three real defences — `basename()`, a `resolve()`+`startsWith` containment check, or an explicit `includes('..')` rejection — and deliberately does **not** accept `path.join` as a guard, since treating it as one would endorse the bug. 7 tests. **Fixture score across the whole of #89: 6/10 → 10/10**, with the inert fixture still clean and our own gate still passing. | — | RESOLVED — (a) weak hashing, (b) prototype pollution, (c) path traversal all shipped. |
| 90 | **"Production ready" had no measurable definition — and the readiness probe built to give it one found a live cron bug on its first run.** Craig asked 2026-07-28 how we know we are production ready. The honest answer is that the test suite cannot tell us: it was green throughout 2026-07-27/28 — 6,700+ tests, clean build, self-scan PASSED — while production was 102 commits stale, `/billing` 404'd, `POST /api/watches/tick` 405'd, and paid MCP customers received no key. Readiness is a property of the **live customer journey**, so it needs a machine that keeps asking the live site, not a checklist someone reads. Shipped: `src/core/readiness-probe.js` (pure, injected fetch) + `scripts/ops/readiness-probe.js` + a 30-minute GitHub Actions schedule. Side-effect free by design — every step is a GET or a POST we expect to be *rejected*, so it never completes a payment, never writes customer data, and needs **no secrets** (a monitor requiring credentials tends not to get set up). Checks deploy freshness against an expected commit, `/api/status` required *and* important vars (important = a paid feature degrading silently, treated as critical), customer surfaces including content not just status codes, and that cron endpoints accept both methods while rejecting unauthenticated callers. 14 tests with a stubbed fetch, covering the failure paths a live run rarely exercises. **Its first run against production found a genuine bug nothing else had: `GET /api/scan/worker/tick` returned a self-documenting JSON blob and did no work.** Vercel's built-in cron issues GET — and that blob listed `vercel-cron` as its first trigger — so a GET scheduler received `{"ok":true}` on every tick while the queue drained nothing. Same invisible-failure class as the 405, but quieter, because a 200 is the one status nobody investigates. `tests/cron-endpoint-methods.test.js` did not catch it because it asserted both methods were *exported*, not that either did the work; strengthened to detect a documentation stub, and verified by reintroducing the bug (red) then restoring (green). **Two false positives in the probe itself, both caught by verifying rather than reporting:** `/pricing` was flagged as stale-deploy damage when `website/app/pricing/` has never existed (pricing is a home-page anchor) — that check is now a content assertion on `/`, and it incidentally surfaced a genuinely dead `/pricing` link in `billing/page.tsx`; and the 200-without-credentials message asserted "publicly triggerable" when the real cause was the doc stub, now naming both possible causes. A monitor that cries wolf gets muted. | — | RESOLVED — probe shipped and scheduled. Its findings against live production remain Craig's to action (deploy, env vars). |
| 85 | **The confidence scorer's block-comment walker silently DEFEATED THE GATE — it counted `/*` inside line comments and strings, so every finding below such a line scored 0.20.** Found 2026-07-28 by grouping the newly-visible low-confidence warnings (KI #77) by signal — the top bucket was 48 `errorSwallow` findings marked "inside block comment", which on inspection were ordinary `} catch { /* not fatal */ }` code. Root cause in `confidence.js:isInsideBlockComment()`: the forward walk tracked `/*` / `*/` but skipped neither line comments nor string literals, so **one** line like `// The /api/v1/* endpoints expose GateTest as a platform` (`src/core/doctor.js:247`) latched `inBlock` on permanently and every subsequent finding in that file was scored 0.20. A glob string such as `'src/**/*.test.js'` does the same. **This is the false-NEGATIVE direction and it bypasses the gate**: error-severity findings below the block threshold are downgraded to non-blocking soft errors. Measured before the fix on this repo: **13 findings carried the signal, all 13 were not inside a comment, and 1 was error-severity** — the gate passed something it should have caught. Fixed: the walker now skips `//` line comments and quoted strings (with escape handling), while genuine block comments and JSDoc still score 0.20 as before. Verified both directions; after the fix the mis-scored count is **0**. 9 regression tests in `tests/confidence.test.js`, including the exact `/api/v1/*` shape that exposed it. Note this was only findable because KI #77 started reporting the low-confidence share — the count was the bug report. | CRITICAL | RESOLVED (code + tests). |
| 86 | **The confidence scorer treated `${...}` template interpolation as string content — so SQL injection in a template literal was downgraded below the block threshold and the gate waved it through.** Found 2026-07-28 immediately after KI #85, by the same method: grouping low-confidence findings by signal and inspecting what "string literal" was actually firing on. `isInsideStringLiteral()` flipped a boolean on every backtick and answered "inside a string" for everything between them. Interpolations are **executable code**, and they are where injection lives in modern JavaScript. Proven with the canonical shape — ``const q = `SELECT * FROM users WHERE id = ${req.query.id}`;`` scored **0.4** at the column of `req.query.id`, which is under the 0.7 block threshold, so an error-severity finding there became a **non-blocking soft error**. Same false-negative class as #85 and arguably higher impact, since template-literal interpolation is the single most common injection shape in JS/TS. Fixed with a stack-based scanner (`_isInsideStringText`) that tracks `${`/`}` depth and nesting, so `${obj['key']}` correctly reports the inner quoted part as text while the interpolation itself is code; the no-column fallback heuristic also now declines any line containing `${`, since part of such a line is executable. After the fix that shape scores **1.0 and blocks**, while literal text inside the same template still scores 0.4. Measured on this repo: "string literal" down-weights **29 → 23**. 8 regression tests in `tests/confidence.test.js` covering interpolation, nested strings, nested braces, escapes, and the text-after-interpolation case. | CRITICAL | RESOLVED (code + tests). |
| 87 | **The confidence scorer's doc-phrase list contained ordinary security vocabulary, so a real weak-session-secret error stopped blocking the gate.** Found 2026-07-28 by auditing every remaining scorer signal for false-negative risk after #85 and #86. `looksLikeUserFacingDocString()` substring-matches the finding's **message** — text a GateTest module author wrote, not evidence about the customer's code — and a single match multiplies confidence by 0.5, i.e. from 1.0 straight under the 0.7 block threshold. Its list held `'placeholder'`, `'should not be'` and `'should not contain'`, which are ordinary finding prose. Live consequence: `cookie-security` emits *"Session secret is a known-weak placeholder (\"changeme\") — replace before deploy."* at **error** severity in non-test code (`errSev = isTest ? 'warning' : 'error'`), and the word "placeholder" alone downgraded it to a non-blocking soft error. A hardcoded weak session secret is precisely what the gate exists to stop. `links` was hit too ("dead/placeholder link(s)"). Fixed by pruning the list to documentation **markers** only — `in browser bundle`, `example of`, `for example`, `e.g.,`, ` eg. `, `illustrative` — leaving the PR #85 case that justifies the signal intact (its message is "NEXT_PUBLIC_ANTHROPIC_API_KEY in browser bundle"). **Two rejected alternatives, recorded so they are not retried:** (a) raising the multiplier to 0.75 so the signal cannot act alone — this breaks PR #85, whose downgrade depends on this signal *by itself*; (b) recognising the `key: "…"` line shape as a string literal so the evidence sits on the code — rejected because `message: "NEXT_PUBLIC_… in browser bundle"` (a scanner's own detection table) and `apiBase: "http://localhost:3000"` (a real hardcoded URL that must block) are the **same line shape**. Only message content separates them, which is why this signal has to exist despite being the weakest kind of evidence here. 3 new tests including an end-to-end assertion that the weak-secret shape blocks. | HIGH | RESOLVED (code + tests). |
| 88 | **A `docs/` path down-weighted real shipping code, and `retry-hygiene` matched prose about `sleep()` as a call to `sleep()`.** Found 2026-07-28 finishing the scorer audit begun in #85–#87 and clearing the last non-test entries out of the low-confidence breakdown. **(a) FALSE NEGATIVE, scorer:** `isExampleDataFile()` scored *any* path containing `docs/` at 0.4 "example data", source files included. Products routinely serve a documentation SITE from production code — `website/app/docs/api/page.tsx` exists in this very repo, alongside shapes like `pages/docs/[slug].tsx` and `app/docs/route.ts` — and 0.4 sits under the 0.7 block threshold, so an error-severity finding in genuinely deployed code stopped blocking the gate. Real documentation is already covered by `isDocFile()` (.md/.mdx/.rst at 0.3), so the path clause now applies only to non-source files via a new `SOURCE_EXT_RE`; `.json`/`.txt`/`.yml` doc assets and `examples/` are unchanged. **(b) FALSE POSITIVE, module:** `retry-hygiene` reported `no-backoff` and `no-jitter` at `website/app/lib/pentest/probes.js:145`, whose neighbouring line reads `// Time-based: send sleep(5), expect response delay` — the module matched `sleep(5)` inside a sentence about `sleep(5)`. `_findLiteralSleep` already guarded string literals but not comments. Added `BaseModule._isCommentLine()` as the companion to `_isInsideStringLiteral` (whole-line only, deliberately: a trailing `// note` after real code leaves that code executable) and wired it in. Verified the FP is gone while genuine constant-delay retries — `setTimeout(r, 25)` in `live-probe-runner.js:115` — are still reported. 6 new tests across `confidence`, `base-module` and `retry-hygiene`. | HIGH | RESOLVED (code + tests). |
| 78 | **~20 AI call sites bypass `engine-models.js`, so the single source of truth isn t.** Found 2026-07-26 (partially addressed same day). `ai-review`, `agentic`, `architecture-drift`, `intent-verification`, `regression-predictor`, `bidirectional-test-gate`, `doctor` + 7 website routes each hardcode their own `const MODEL`, invisible to `GATETEST_FIX_MODEL`/`GATETEST_CHEAP_MODEL`. `anthropic-version: 2023-06-01` is hardcoded at **15** call sites and `api.anthropic.com` at 12+ (no `ANTHROPIC_BASE_URL` support → no proxy/Bedrock/Vertex path). **`FALLBACK_MODEL` is dead**: exported and asserted in tests, but no production code retries on it — `scan/fix/route.ts:794` only uses `needsRefusalFallback` to set `effort: high`, so the docstring claims a safety net that does not exist. **Worst failure mode: `try-fix.js` never checks `res.ok`,** so a retired-model 404 returns `null` and reports as a clean "no fix available" — a model retirement would look like a quality regression, not an outage; `doctor.js` would additionally blame the customer s API key. **PARTIALLY RESOLVED 2026-07-27, and this KI's status-check claim was overstated — corrected here after a call-site-by-call-site audit.** The `try-fix.js` half was real and is **fixed**: `runClaudeLayer` never checked `res.ok`, and an Anthropic error body carries no `content` array, so 404 (model retired) / 401 (bad key) / 429 / 529 all fell through to `return null` and surfaced as `reason:'no-layer-matched'` — byte-identical to a genuine miss. It now returns `{apiError, status, reason}`, records `api-error:<status>` telemetry, and `tryFix` returns `reason:'claude-api-error'`. 8 regression tests. **The "~20 call sites don't check status" generalisation does NOT hold** — the other sites use Node `https.request`, not `fetch`, so a `.ok` grep was the wrong probe: `ai-review.js` + `agentic.js` check `statusCode !== 200`; `architecture-drift.js`, `intent-verification.js`, `regression-predictor.js`, `bidirectional-test-gate.js`, `cli-fix-orchestrator.js` and `ai-fix-engine.js` all reject on `if (parsed.error)` (the real Anthropic error shape) and reject non-JSON bodies via the surrounding catch; `scan-modules/ai.ts` checks `result.status !== 200` with 429/5xx retry; `guidance/route.ts` checks `res.status !== 200`. Residual weakness in the six `https.request` sites is narrow: a 200 with no content resolves to `''`, which reads as "nothing found". **MODEL-CENTRALISATION HALF RESOLVED 2026-07-28.** All **17** hardcoded model sites now resolve through `engine-models.CHEAP_MODEL`: 7 CommonJS engine files (`agentic`, `ai-review`, `architecture-drift`, `intent-verification`, `regression-predictor`, `bidirectional-test-gate`, `lib/ai-ci-fixer-claude`), 2 website libs (`chat-system-prompt`, `scan-modules/ai.ts`), and 5 website routes that passed `model: "claude-sonnet-5"` inline (`admin/health`, `heal/sentry-webhook`, `scan/guidance`, `scan/server-fix`, `watches/tick`). `GATETEST_CHEAP_MODEL` now reaches every one — verified by loading all 7 engine files under an override, not just by grep. Guarded by `tests/model-centralisation.test.js` (32 tests): each file must import `CHEAP_MODEL`, no executable `claude-*` literal may return, and the override must resolve. This is the mitigation for the retirement risk this KI opened with — one env var now repoints every call site. **`FALLBACK_MODEL` — the "dead safety net" claim was half right, and the fix was documentation, not code.** The call-site comment in `scan/fix/route.ts` is already honest: a Fable classifier refusal returns HTTP 200 with empty content, `validateFixOutput` rejects it, and that one finding falls through to the advisory section — a deliberate, documented graceful degradation. The actual defect was the **docstring** in both `engine-models.js` twins calling Opus 4.8 the "refusal fallback for Fable", which reads as an automatic retry that has never existed; `tests/heavy/marketing-claim-verification.test.js` asserts off that same line, so the claim was propagating outward. Both docstrings now say **DECLARED, NOT WIRED** and describe what actually happens, with a guard test that fails if `FALLBACK_MODEL` gains a consumer without the docs being updated. **Deliberately NOT implemented: the retry itself.** Wiring it would spend an extra Opus 4.8 call per refusal on a paid tier — money-adjacent, so Craig's call, not a silent 1am change. **ENDPOINT + API-VERSION HALF RESOLVED 2026-07-28 — KI #78 is now code-complete.** New `src/core/anthropic-config.js` + its `website/app/lib/` twin own the endpoint and version: `ANTHROPIC_BASE_URL` (default `https://api.anthropic.com`, **path prefixes preserved** so `https://gw.corp/anthropic` → `…/anthropic/v1/messages`, non-443 ports carried) and `ANTHROPIC_VERSION` (default `2023-06-01`). Migrated **every** call site — 14 `hostname:`/`host:` literals and 21 `anthropic-version` headers across 22 files, covering both mechanisms (`https.request` via `endpoint()`/`apiPath()`, `fetch` via `apiUrl()`), CJS, TS and the ESM MCP server. A re-audit shows **0 host literals and 0 version headers** outside the config twins; the only remaining mention is `next.config.ts`'s browser CSP, which does not gate server-side calls. **Deliberately all-or-nothing, and ratchet-tested:** a partial migration is *worse* than none, because an operator who sets a gateway for egress control or data residency would have some traffic routed and the rest sent straight to the vendor with no signal. **A malformed value THROWS rather than falling back** — a silent fallback to `api.anthropic.com` is exactly the outcome the setting exists to prevent (Forbidden #16). **HTTPS only**, documented as a limitation: most call sites use `https.request`, which cannot speak plain HTTP, and silently upgrading would fail nowhere near the cause. Three real bugs were caught *during* this work, all by verification rather than review: the migration script left a duplicate `port: 443` key in 4 files (last key wins, so the literal silently defeated the configured port), deleted an exported `ANTHROPIC_API_HOST` that `ai-ci-fixer-core` re-exports (ReferenceError on load — the 120-module check did not cover that file), and the twin went stale before the https-only change (caught by the twin-sync test). GateTest then blocked its own gate for the two new env vars missing from `.env.example` — now documented in both. 14 tests in `tests/anthropic-config.test.js`. **Still open: Craig's decision on the FALLBACK_MODEL refusal retry (paid-tier spend).** | HIGH | Code work — pre-authorized. All three code halves done; only the refusal-retry decision remains. |
| 79 | **PRODUCTION IS 102 COMMITS AND ONE MINOR VERSION STALE — everything shipped since 2026-07-16 is not live.** Confirmed 2026-07-27 by direct API call: `gatetest.ai/api/platform-status` → `{version:"1.59.0", commit:"a3b0b2ac", builtAt:"2026-07-16T10:23Z"}` while `main` is v1.60.0 at `5e331d8`. Proven customer-visible consequences, each verified live, not inferred: (a) `/billing` returns **404** — the self-serve Stripe portal (KI #72, built precisely to cut dispute risk) does not exist for paying customers, so cancel/update-card still requires emailing support; (b) `/pricing` has **no Enterprise card** and none of the org-flat $49 Continuous copy — the site sells the pre-2026-07-23 pricing Craig has already superseded; (c) `POST /api/watches/tick` → **405**, i.e. the exact bug fixed in `5e331d8` is live right now, so any POST scheduler silently fails and the Continuous tier's watches never run; (d) the `scan_queue.host` migration (`5c5885b`) is not deployed, so the worker tick still dies on `column q.host does not exist` and the queue cannot drain. Also not live: v1.60.0 authenticated crawls, OIDC publishing, the KI #71/#73/#74 fixes. Good news from the same probe: `/api/status` reports `ready:true`, all 6 required vars set, Stripe in `live` mode, and `POST /api/scan/worker/tick` → 401 (so `CRON_SECRET` **is** set and the deployed build's fail-open tick is not publicly triggerable). **Boss Rule #5 — production deployment needs Craig's explicit go.** Runbook: `docs/deploy/VAPRON-DEPLOY.md`. | CRITICAL | Craig action — authorize the deploy. Nothing else in this table matters until this ships. |
| 80 | **The hosted headless-browser pass has never run — `VAPRON_API_TOKEN` and `VAPRON_DISPATCH_SECRET` were never set on the deploy box.** Found 2026-07-27. **CORRECTION to this KI's first wording:** the initial probe hit `vapron.ai/api/platform-status`, got 404, and I wrote it up as "Vapron never implemented the contract." That was wrong — `vapron.ai` is the *marketing* site and 404s every `/api/*` path except `/api/health`. The real platform API is `api.vapron.ai/api/platform` (the base `VAPRON_BASE_URL` already points at), and `POST https://api.vapron.ai/api/platform/api/jobs/web-runtime-scan` returns **401 `{"error":"Platform API key required (Authorization: Bearer vpk_live_xxx)"}`** — the endpoint exists and is correctly auth-gated. The actual root cause is config: `/opt/gatetest/website/.env.local` on box 161 has `VAPRON_BASE_URL` but **neither `VAPRON_API_TOKEN` nor `VAPRON_DISPATCH_SECRET`** (verified by listing variable names on the box). `docs/deploy/JARVIS-WEB-DEPLOY.md:53` already warned "the issued vpk_ key was never saved on this box" and it was never actioned. So `dispatchRuntimeScan` has returned `{ok:false, reason:"VAPRON_API_TOKEN not configured"}` on every hosted `/web` and `/wp` scan since the box was built — the `/web` claim "we open your site in a real browser" has never been true on the hosted path (Forbidden #1). Fixed here 2026-07-27 (`6cc4547`): the API always returned `runtime.status:"unavailable"` honestly but the UI rendered **nothing** for it, so a report missing a whole advertised layer read as a clean bill of health; `RuntimeUnavailable` now says so plainly. **Remaining action is Craig's:** Vapron dashboard → Connected Apps → gatetest → Rotate key, set `VAPRON_API_TOKEN` (the `vpk_live_` value) + `VAPRON_DISPATCH_SECRET`, restart. | CRITICAL | Craig action — issue + set the two Vapron secrets. Endpoint is fine; no code work needed. |
| 81 | **Cross-product platform-status contract is broken on two of three products, so the admin health panel cannot work.** Found 2026-07-27. `/api/admin/platform-siblings` fans out to Vapron, Gluecron and GateTest expecting `docs/PLATFORM_STATUS.md`'s shape. Live results: **Vapron 404s** (endpoint never built — permanently renders "unreachable"); **Gluecron returns `{version:"dev", commit:"unknown"}`**, i.e. `APP_VERSION`/`GIT_COMMIT` are unset on its deploy so it can never prove build freshness — the failure mode `VAPRON-DEPLOY.md` exists to prevent; and Gluecron's own `siblings` map is stale, pointing at `crontech.ai` (renamed to Vapron by Craig 2026-06-12; the host does not resolve) and `gatetest.io` (**wrong TLD — 404**; canonical is `gatetest.ai`). `docs/PLATFORM_STATUS.md` is itself stale, still naming the product "Crontech" throughout. Only GateTest satisfies its own contract. Fixing Gluecron/Vapron means changes in *their* repos — outside this one. **TLD VERDICT INVERTED 2026-07-30 — do not act on the parenthetical above.** That entry was written when `gatetest.ai` was canonical, so it records Gluecron pointing at `gatetest.io` as the bug. The domain move makes `gatetest.io` **correct** and `gatetest.ai` the stale value. The underlying defect is unchanged — Gluecron's siblings map is hardcoded and still needs updating in *its* repo — but it now needs updating in the opposite direction. The dated 2026-07-27 observation is left intact as evidence of what was seen then. | MEDIUM | Craig — cross-repo work on Gluecron + Vapron. Doc refresh here is pre-authorized. |
| 82 | **`RESEND_API_KEY` is stored under a typo'd name (`RESENDER_API_KEY`), so paid MCP keys have never sent — plus two further env gaps.** Found 2026-07-27; root cause identified from Craig's Vapron "Platform secrets" listing. The code reads **`RESEND_API_KEY`** (`digest-mailer.js`, the Stripe webhook, the KI #72 billing portal); the store holds **`RESENDER_API_KEY`**. A typo, not a missing secret — which is exactly why `/api/status` reports it absent while Craig can see a Resend key in the dashboard. Consequence, with Stripe in **live** mode: an MCP $29/mo subscriber pays and the `gtmcp_` key never arrives (webhook 500s), and the billing portal 503s for everyone. Second gap: `GOOGLE_CLIENT_SECRET` **is** in the platform-secrets store but is **not** in `/opt/gatetest/website/.env.local` on the box, so the half-wired Google sign-in button stays dead until it is applied. **Third, and do NOT press "Apply to live app" before reading this:** the store and the box's `.env.local` have diverged, and four vars exist **only on the box** — `CRON_SECRET`, `GATETEST_PUBLIC_BASE_URL`, `GITHUB_WEBHOOK_SECRET`, `VAPRON_BASE_URL`. If "Apply to live app" replaces `.env.local` wholesale, losing `CRON_SECRET` kills the queue drain (KI #41/#79) and losing `GITHUB_WEBHOOK_SECRET` kills push-scan intake. Add those four to the store *first*, then apply. (Conversely `JWT_SECRET_KEY` sits in the store and is unused by this codebase.) Note KI #3 ("Stripe test keys not yet swapped in") is stale — production reports `stripe.mode: "live"`. | HIGH | Craig action — fix the typo, add the 4 box-only vars to the store, then apply + restart. |
| 83 | **Public + legal copy still tells the world our infrastructure is Vercel — it is Vapron.** Found 2026-07-27 when Craig challenged a stale "Vercel env var" string in the admin console; the sweep it triggered showed the drift is much wider than that one line. Two categories, only one of which is a defect. **Legitimate, keep:** references describing what GateTest *scans in customer stacks* — `web-headers.js`, `cache-headers.js`, `cron-expression.js`, `Integrations.tsx`, `for/nextjs`, `pipeline-trace` ("Vercel/Netlify build logs require host-level auth"). Customers really are on Vercel; those are supported-platform statements. **Stale self-references — the defect:** the admin console claimed *our* env vars live in Vercel (7 surfaces, all fixed 2026-07-27), and `WatchdogTab` asserted "Vercel Cron runs this automatically every 5 minutes," which is flatly false and is the same gap as KI #41/#79 — nothing schedules it, so watches never ran. **Still open, deliberately NOT touched because both are Boss Rule #8 (public-facing communication):** (a) `website/app/how-it-works/page.tsx:17` — "Everything runs against our managed **Vercel** + Neon stack today" — and `:23` — Runtime: "**Vercel** serverless functions"; (b) `website/app/legal/privacy/page.tsx:263` — "Infrastructure hosted on **Vercel** with SOC 2 Type II compliance." (b) is the serious one: a privacy policy making a factual claim about where customer data is processed, plus a SOC 2 attestation inherited from a host we no longer use. Craig must rule on the wording before either changes. **CLOSED 2026-08-21 — Craig authorized the honesty sweep (production-ready checklist §5).** (a) the how-it-works stack copy had already been corrected in the 2026-08-18 rework ("our own managed host with a Postgres (Neon) queue"); (b) privacy §7 now states dedicated cloud servers (Vultr) + Cloudflare DNS/edge + Neon, with the false SOC 2 Type II claim REMOVED, and the "Vercel, Inc." sub-processor entry replaced by The Constant Company, LLC (Vultr) under the Vultr DPA; terms’ three provider lists now say Vultr; four leftover admin hint strings ("in Vercel env vars") now point at website/.env.local on the deploy box. Attorney review of the DRAFT legal pages is a separate open blocker (checklist §4). | — | RESOLVED 2026-08-21. |
| 93 | **gatetest.ai DOWN 2026-07-29 — the DOMAIN entered redemption period at the .ai registry; NOT a server problem.** Jarvis self-heal session 89 diagnosed the ~15-min HTTP 000 outage: `dig gatetest.ai A` returns zero answers everywhere because the `.ai` TLD authoritative server (`v0n0.nic.ai`) answers **NXDOMAIN** — the delegation was pulled from the registry zone. RDAP (Identity Digital) shows status `["client transfer prohibited", "redemption period"]`, registrar **Cloudflare, Inc**, last-changed **2026-07-29T08:14:34Z** (matches outage onset), registration 2026-04-12, nominal expiry 2028-04-12 — a domain that drops to redemption *before* its expiry date usually means a registrar-side deletion (payment failure/chargeback on the registration, or manual deletion), not a normal lapse. **Everything server-side is healthy and was verified during the incident**: `gatetest-web.service` active, Next.js answering 200 on 10.0.1.1:3000, and the full Traefik TLS path serves **HTTP 200** when DNS is bypassed (`curl --resolve gatetest.ai:443:66.42.121.161 https://gatetest.ai`). Nothing on the box can fix a registry-level redemption — restoring requires the Cloudflare registrar account holder to redeem/restore the domain (redemption fee likely applies), which is Boss Rule #4 (domain/DNS) + #9 (money) territory. **Redemption windows are typically 30 days; after that the domain goes to pendingDelete and public re-registration — every day of delay raises the risk of losing the domain outright.** Slack alert sent to #jarvis-cclabs 2026-07-29 ~08:50 UTC. Side effect while down: Stripe webhooks, GitHub App callbacks, and MCP-key delivery emails targeting gatetest.ai all fail until DNS returns. **Re-verified by self-heal session 90, 2026-07-29 ~10:20 UTC:** RDAP still shows `redemption period` (registry DB updated 09:17Z), `.ai` TLD still answers NXDOMAIN, and the server path still returns HTTP 200 via Traefik with DNS bypassed — the domain remains unredeemed, outage persists ~2h; still Craig-only. **Re-verified by self-heal session 91, 2026-07-29 (RDAP DB timestamp 09:47Z):** status still `["client transfer prohibited", "redemption period"]`, `v0n1.nic.ai` still NXDOMAIN, `gatetest-web.service` active, full Traefik TLS path still HTTP 200 via `--resolve gatetest.ai:443:66.42.121.161` — no server-side action exists; awaiting Craig's registrar redemption. **Re-verified by self-heal session 92, 2026-07-29 (RDAP DB timestamp 10:17Z):** status still `["client transfer prohibited", "redemption period"]`, `.ai` TLD (`v0n0.nic.ai`) still answers SOA-only/NXDOMAIN for the delegation, 1.1.1.1 and 8.8.8.8 both NXDOMAIN, `gatetest-web.service` active, Traefik TLS path still HTTP 200 via `--resolve`. New data point: Cloudflare's authoritative nameservers (`aitana`/`major.ns.cloudflare.com`) still hold the zone intact and answer `gatetest.ai A 66.42.121.161` + `www` correctly when queried directly — the DNS zone config survived, so once Craig redeems the domain at the registrar, public resolution should return as soon as the TLD re-publishes the delegation (no zone rebuild needed). **Re-verified by self-heal session 93, 2026-07-29 (RDAP DB timestamp 22:27Z — ~14h after onset):** status still `["client transfer prohibited", "redemption period"]`, `v0n0.nic.ai` still NXDOMAIN, public resolvers return no answers, Cloudflare zone still answers `gatetest.ai A 66.42.121.161` when queried directly, `gatetest-web.service` active (Next.js 200 on 10.0.1.1:3000), Traefik TLS path still HTTP 200 via `--resolve gatetest.ai:443:66.42.121.161`. Domain remains unredeemed after a full day; no server-side action exists. **UPDATE 2026-07-30 — there IS a way back online that does not wait on redemption.** `gatetest.io` is already registered on the **same Cloudflare account** (registered 2026-04-08, four days before the `.ai`; expires 2027-04-08; status healthy; Cloudflare nameservers) and currently points at the **retired Vercel deployment**, serving `DEPLOYMENT_NOT_FOUND`. Pointing its A records at `66.42.121.161`, adding `Host(\`gatetest.io\`)` alongside the existing `.ai` rules in `/traefik/dynamic/gatetest-web.yaml` + `gatetest-mcp.yaml` (Coolify/Traefik, hot-reloads, Let's Encrypt auto-provisions), and setting `NEXT_PUBLIC_BASE_URL=https://gatetest.io` + rebuild brings the product back today. As of `cddd02d` that last part is genuinely **one env var** — 148 hardcoded origins were centralised behind `website/app/lib/site-url.js`, verified by building twice and diffing rendered canonicals/OG/sitemap/IndexNow output. Full runbook incl. rollback: **`docs/deploy/DOMAIN-FAILOVER-IO.md`**. **Redeem `.ai` anyway** — badge markdown in customers' READMEs points at it permanently, so if it reaches pendingDelete someone else can serve images inside our customers' repos; keep it 301'ing to `.io` rather than retiring it. **Re-verified by self-heal session 94, 2026-07-29 (RDAP DB timestamp 22:57Z):** status still `["client transfer prohibited", "redemption period"]`, `.ai` TLD still publishes no delegation (`dig @v0n0.nic.ai gatetest.ai NS` empty), public resolvers return NXDOMAIN (curl HTTP 000), `gatetest-web.service` active, Traefik TLS path still HTTP 200 via `--resolve gatetest.ai:443:66.42.121.161`, Cloudflare authoritative NS still answer `gatetest.ai A 66.42.121.161`, and `gatetest.io` still serves the retired-Vercel 404 — the `DOMAIN-FAILOVER-IO.md` runbook remains ready but unexecuted, awaiting Craig (Boss Rule #4/#5). ~15h since onset; both redemption and failover are Craig-only actions. **Re-verified by self-heal session 96, 2026-07-30T00:03Z (~16h after onset):** RDAP still `["client transfer prohibited", "redemption period"]` (last-changed unchanged at 2026-07-29T08:14:34Z), `.ai` TLD (`v0n0.nic.ai`) still publishes no delegation (SOA-only), public resolvers still fail (`curl` error 6 / HTTP 000), Cloudflare authoritative NS still answer `gatetest.ai`/`www` → `66.42.121.161`, `gatetest-web.service` active, full Traefik TLS path serves HTTP 200 for both `gatetest.ai` and `www` with DNS bypassed (`curl --resolve ...:443:127.0.0.1`), and `gatetest.io` still serves the retired-Vercel `DEPLOYMENT_NOT_FOUND` 404 — outage is still registry-level; restart/redeploy cannot help. **UPDATE 2026-07-30 — the whole code side of the move is now committed:** `DEFAULT_SITE_URL` is `https://gatetest.io` in both `website/app/lib/site-url.js` and the new `src/core/site-url.js`, and 650+ host occurrences were rewritten across the app, engine, CLI, MCP server, GitHub Action, WordPress plugin, editor extensions, workflows and docs. **Two decisions were deliberately reversed or held from this row's original text:** (1) badges now FOLLOW the new origin — `BADGE_ORIGIN` still tracks `SITE_URL`, because pinning to `.ai` while `.ai` is NXDOMAIN would mint new badges that are born broken; already-pasted badges are fixed by redeeming `.ai` and 301'ing it, not by generating dead URLs. (2) **every e-mail address stayed on `@gatetest.ai` on purpose** — an unverified sending domain fails SILENTLY (Resend rejects or spam-folders it) whereas a wrong URL is visible, so mail moves only once `gatetest.io` is verified in Resend; flip with `GATETEST_SUPPORT_EMAIL` + `RESEND_FROM`. **Re-verified by self-heal session 98, 2026-07-30T00:37Z (~16.4h after onset):** RDAP (DB timestamp 00:36:48Z) still `["client transfer prohibited", "redemption period"]`, last-changed unchanged at 2026-07-29T08:14:34Z; `.ai` TLD (`v0n0.nic.ai`) still SOA-only for the delegation; public resolvers still NXDOMAIN (curl error 6 / HTTP 000); Cloudflare authoritative NS still answer `gatetest.ai A 66.42.121.161`; `gatetest-web.service` active (Next.js 200 on `10.0.1.1:3000`); full Traefik TLS path serves HTTP 200 for `gatetest.ai` with DNS bypassed (`curl --resolve gatetest.ai:443:127.0.0.1`); `gatetest.io` DNS still points at Vercel and serves `DEPLOYMENT_NOT_FOUND`, and Traefik on this box answers 503 for Host `gatetest.io` because `gatetest-web.yaml` is still `.ai`-only — expected until the failover runbook runs, noted here so the next session doesn't misread that 503 as a new defect. No server-side action exists; both remedies remain Craig-only per Boss Rule #4/#5. **Re-verified by self-heal session 99, 2026-07-30T01:10Z (~17h after onset):** RDAP (DB timestamp 01:09:42Z) still `["client transfer prohibited", "redemption period"]`, last-changed unchanged at 2026-07-29T08:14:34Z; public resolvers still NXDOMAIN (curl error 6 / HTTP 000); Cloudflare authoritative NS (`aitana.ns.cloudflare.com`) still answer `gatetest.ai A 66.42.121.161`; `gatetest-web.service` active (Next.js 200 on `10.0.1.1:3000`); Traefik TLS path serves HTTP 200 for `gatetest.ai` and `www` with DNS bypassed; `gatetest.io` DNS still points at Vercel (216.150.x.x, 404) and this box's Traefik still answers 503 for Host `gatetest.io` — the failover runbook `docs/deploy/DOMAIN-FAILOVER-IO.md` remains ready but unexecuted. Redemption window is ~30 days from 07-29; every session confirms the same Craig-only remedies. **Re-verified by self-heal session 100, 2026-07-30T01:40Z (~17.5h after onset):** RDAP (DB timestamp 01:39:47Z) still `["client transfer prohibited", "redemption period"]`, last-changed unchanged at 2026-07-29T08:14:34Z; public resolvers still NXDOMAIN (curl error 6 / HTTP 000); Cloudflare authoritative NS (`aitana.ns.cloudflare.com`) still answer `gatetest.ai A 66.42.121.161`; `gatetest-web.service` active (Next.js 200 on `10.0.1.1:3000`, correct — it binds the Traefik-facing bridge, not localhost); Traefik TLS path serves HTTP 200 for `gatetest.ai` and `www` with DNS bypassed (`curl --resolve ...:443:127.0.0.1`); `gatetest.io` DNS still points at Vercel (216.150.x.x — apex 404s, `www` now also presents an EXPIRED cert at Vercel, further evidence the retired deployment is rotting) and this box's Traefik still answers the catch-all for Host `gatetest.io` because `gatetest-web.yaml` is `.ai`-only pending the runbook. `docs/deploy/DOMAIN-FAILOVER-IO.md` remains ready but unexecuted — both remedies stay Craig-only per Boss Rule #4/#5. **Re-verified by self-heal session 101, 2026-07-30T02:09Z (~18h after onset):** RDAP (DB timestamp 02:09:07Z) still `["client transfer prohibited", "redemption period"]`, last-changed unchanged at 2026-07-29T08:14:34Z; public resolvers (1.1.1.1 + 8.8.8.8) still NXDOMAIN (curl error 6 / HTTP 000); Cloudflare authoritative NS (`aitana.ns.cloudflare.com`) still answer `gatetest.ai`/`www` → `66.42.121.161`; `gatetest-web.service` active (Next.js 200 on `10.0.1.1:3000`); Traefik TLS path serves HTTP 200 for `gatetest.ai` and `www` with DNS bypassed (`curl --resolve ...:443:127.0.0.1`); `gatetest.io` DNS still points at Vercel (`216.150.16.193`, apex `DEPLOYMENT_NOT_FOUND` 404). Nothing new to act on from this box; the failover runbook stays ready and unexecuted, both remedies remain Craig-only per Boss Rule #4/#5. | CRITICAL | Craig action — (a) confirm he owns `gatetest.io`, then run `docs/deploy/DOMAIN-FAILOVER-IO.md` (DNS + Traefik + env, code already done) to restore service, (b) still redeem `gatetest.ai` and fix the failed payment/chargeback that caused a pre-expiry deletion, (c) verify `gatetest.io` in Resend before moving mail, and (d) update the GitHub App + Stripe + Marketplace URLs listed in the runbook. Boss Rule #4 + #5 — code side DONE, nothing executed against production. |
| 94 | **`tests/ai-guardrails-probe.test.js` intermittent file-level failure — ROOT CAUSE FOUND AND FIXED 2026-08-05.** Reproduced at last by running the file 8-way CONCURRENTLY (3 failures / 40 runs; it never fails in 30 *sequential* runs, which is why "under full-suite load" was the only clue anyone ever had). Captured stderr: `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING), file srcwinasync.c, line 94` — a **native libuv abort during process teardown**, not a failed assertion. All 22 subtests pass, the process then dies, and node:test attributes the dead process to the file as `test at …:1:1` / `test failed`. **Mechanism:** `probe()` uses global `fetch` (undici), which pools keep-alive sockets in a process-global dispatcher; CLAUDE.md mandates `--test-force-exit`, which calls `process.exit()` the instant the last test finishes, and under CPU contention that lands while libuv is still closing those sockets. Both 2026-07-30 hypotheses were about the HTTP *server* and were correctly disproved — the open handles belonged to the *client*, so no amount of `server.close()` work could ever have fixed it. **Fix:** a `test.after()` hook drains undici's global dispatcher (reached via the well-known symbol, every step optional so a future Node cannot break it) plus one macrotask turn. **Evidence:** 0 failures in 80 concurrent runs post-fix vs 3/40 before — p ≈ 0.002 against the old rate. The other five server-using test files were stress-tested too (18 concurrent runs each, 0 failures); only this file aborts a fetch mid-flight via AbortController, which is what makes it uniquely exposed. | — | RESOLVED — root cause identified, fixed, verified under the conditions that reproduce it. |
| 84 | **`website/app/lib/try-fix.js` — the 5-layer fix orchestrator — has ZERO production callers, while `/how-it-works` names it by filename as how paid fixes work.** Found 2026-07-27 while fixing KI #78. `grep -rn "try-fix"` across the repo returns exactly nine files: the module, its test, two docs, and five **comments** that merely mention it (`scripts/ai-ci-fixer.js:319`, `hn-reply-assistant/drafter.js:145`, plus chart/table components). Nothing imports it outside `tests/try-fix.test.js`. The live paid-fix path (`website/app/api/scan/fix/route.ts`) uses an entirely different pipeline — `surgical-fix`, `fix-attempt-loop`, `cross-fix-scanner-gate`, `cross-fix-syntax-gate`, `fix-cap`, `test-generator`. Meanwhile `website/app/how-it-works/page.tsx:163-166` tells customers, in a `<code>` block naming the exact path: *"our orchestrator (`website/app/lib/try-fix.js`) walks four layers in order. The first layer that produces a real patch wins."* That describes a module the product never executes. This is the **same class as KI #74** (marketed-by-name, inert in the shipped product) and was missed by it. Three possible resolutions, all needing a decision: (a) wire `try-fix` into the real fix route as originally intended, (b) retarget the copy at the pipeline that actually runs, or (c) delete `try-fix.js` as abandoned. Note (b) touches public copy → Boss Rule #8. The KI #78 `res.ok` fix landed on this module and remains correct either way. **COPY HALF RESOLVED 2026-08-21 — option (b) executed under Craig’s honesty-sweep authorization.** /how-it-works no longer names try-fix.js anywhere; the flywheel section and FlywheelTable now describe the pipeline that actually runs (recipe playback via flywheel-playback-engine → Claude surgical fix, three hypotheses on the CI path → syntax gate → scanner/test gate), and CostTrendChart is labelled an illustrative design goal in copy AND aria-label. | MEDIUM | Remaining Craig decision: wire try-fix.js into the real fix route or delete it as abandoned — no public copy depends on it any more. |
| 95 | **`fetchPriorArt()` in `website/app/lib/cross-repo-lookup.js` has ZERO production callers — the cross-repo flywheel computes prior-art that no prompt ever receives.** Found 2026-07-30. This is the **third instance of the KI #74 / #84 class** (a tested, exported module that the shipped product never executes), and it was found the same way: by chasing why a disabled test file asserted behaviour that no longer existed. What IS wired: `summariseSimilarScans` is used by `/api/dashboard/intelligence` for its JSON response — legitimate, that route needs the summary object, not a rendered prompt block. What is NOT wired: `fetchPriorArt` (findSimilar → summarise → render), the facade whose entire purpose is to feed a prompt. **A related bug WAS fixed in the same pass (commit `09fe694`):** `priorArt` was threaded from `diagnosis-enricher.js` into `diagnoseFindings()` and then silently dropped by all three layers below it, so even a supplied block could not reach the prompt. That is fixed and mutation-tested (5/5), along with the anti-template guard that had been deleted from the source. **What remains is a decision, not a bug:** nothing computes a fingerprint to pass in on the diagnosis path. Two complications make this Craig's call rather than a mechanical wire-up. (1) **There are already two prior-art systems.** The repo fix route (`/api/scan/fix`) uses `buildPriorArtHeader` from `@lib/fix-pattern-recall`, which operates on *fix shapes* and IS live; `cross-repo-lookup` operates on *scan fingerprints and module fire-rates*. Whether the second should also feed the Forensic diagnosis prompt is a product question about what the model should be told. (2) **It costs money on a paid path** — an extra DB query per Forensic scan plus a larger prompt on the $399 tier, so it touches AI spend. Options: (a) wire `fetchPriorArt` into the repo fix route's diagnosis step, (b) decide `fix-pattern-recall` is the one true prior-art system and delete `cross-repo-lookup`'s facade, (c) leave it and accept the dead export. **Note the diagnosis path that exists today (`/api/scan/server-fix`) is a URL scan and has no repo fingerprint at all** — so (a) means wiring it into the repo path, not that one. **On whether this is an honesty problem like #84 — checked, and the answer is no, but the first wording of this entry overstated it.** `website/app/dashboard/intelligence/page.tsx` IS customer-facing and does say "Powered by the GateTest cross-repo intelligence brain" and "Cross-repo comparison of this codebase against the cohort of similar stacks scanned by GateTest." So public copy *does* describe this subsystem. It is not a #84-style lie, though, because that page is served by `/api/dashboard/intelligence`, which genuinely runs `findSimilarFingerprints` + `summariseSimilarScans` — the capability the copy claims is actually delivered. Only `fetchPriorArt`, which nothing claims, is dead. **PROVENANCE — this bug was created by "fixing" a test (see `docs/HISTORY.md` KI #30).** That entry records all five `.test.skip.js` files as restored and DONE, and describes the cross-repo one as: *"replaced stale priorArt assertions with correct buildDiagnosisPrompt shape tests."* That is the exact moment the coverage was lost. The `priorArt` assertions were not stale — they were failing because the argument had stopped reaching the prompt. Deleting them turned a red test into a green one and left the defect live in the paid Forensic path until 2026-07-30. The restored files were also committed alongside the originals rather than replacing them, which is why four `.test.skip.js` corpses were still sitting in `tests/` today. **The lesson, and it is the expensive kind:** when a test fails after a refactor, the failing assertion is evidence about the code, not a defect in the test. Deleting it is how a suite grows while its coverage shrinks. `tests/no-disabled-tests.test.js` now stops the parked-file half of this; nothing but review stops the deleted-assertion half. | MEDIUM | Craig decision — wire it into the repo fix route, delete the facade, or accept the dead export. Touches AI spend on the $399 tier. The silent-drop bug underneath it is already FIXED (`09fe694`). |
| 96 | **No detector exists for the KI #74 / #84 / #95 class (shipped-but-unreachable code), and the obvious way to build one was measured and REJECTED. Recorded so the next session does not repeat the measurement.** Found 2026-07-30. That class has now been found by hand three times, so automating it is the obvious move. `deadCode` already claims "unused exports across JS/TS/Python" and DOES work (141 findings on this repo), but it cannot see this class: `dead-code-index.js` records importers as a flat `importedNames` Set with **no provenance about which file imported**, so an export whose only importer is its own test is indistinguishable from one used in production. Confirmed by running it — neither `try-fix` (#84) nor `fetchPriorArt` (#95) is flagged. **Two candidate signals were implemented and measured against ground truth (the three hand-found instances). BOTH FAILED:** *(A) whole-file test-only* — every importer of a file is a test file. **Fires 217 times and is wrong**: it lists `src/modules/accessibility.js`, `src/modules/ai-guardrails.js` and `src/core/text-lines.js` as dead, all of which are live. Root cause: `registry.js` loads every module by **path string** (`accessibility: '../modules/accessibility.js'`), which no import-graph resolver sees. It did catch #84, but buried in 217 findings of which many are false. *(B) export-level test-only* — a named export of a production-reachable file, imported only from tests. **Fires 28 times and MISSED BOTH known instances** (`fetchPriorArt` → not flagged), while listing `registry.js :: BUILT_IN_MODULES` and `config.js :: DEFAULT_CONFIG` — both obviously live — as candidates. **Nothing was shipped.** A detector that fires 217 times with false positives, or 28 times while missing the exact cases it was designed for, is worse than no detector: per Forbidden #25 a noisy check that blocks the gate is the most damaging kind of finding, and it would have to be believed to be useful. **Why it is hard, and the next avenue:** any honest version needs (1) real module resolution rather than bare-name matching — `src/core/import-graph.js` already does this, records edge KINDS (static/lazy/type) and should be the foundation rather than a fresh naive walker; and (2) an explicit **dynamic-registry pass** that treats a path string appearing in a registry/manifest map as a real edge. Point (2) is the interesting one and generalises well beyond this repo — every plugin architecture, DI container and route table has it, and it is precisely why naive dead-code tools are untrustworthy on real codebases. Getting it right would be a genuine differentiator for `deadCode`, not just a fix. **Do not** re-attempt with name-based matching; that is the measurement above. **THIRD SIGNAL MEASURED AND ALSO REJECTED — same root cause, which is the real lesson here.** A detector for the *option-bag* shape was built: flag a caller passing `{ foo: ... }` to a function whose destructure never mentions `foo` (exactly the KI #95 defect). Run against ground truth — the pre-fix files at `09fe694^` — it found **0**. Reason: the call is `const fn = diagnoseFindingsImpl \|\| require('./nuclear-diagnoser.js').diagnoseFindings; await fn({ ..., priorArt, ... })`. The callee is a **dependency-injected variable**, so name-based resolution cannot reach it. A JSDoc-consistency variant was considered and also fails: the pre-fix `diagnoseFindings` JSDoc did not document `priorArt` either. **All three rejected signals died on the same thing — INDIRECTION.** Signal A died on registry path strings, B on bare-name matching, C on a DI function reference. That is not three coincidences; it is one finding: GateTest's static analysis cannot follow indirection, and indirection is exactly where silently-dropped wiring hides. Any future attempt should treat "resolve indirect call targets (registry maps, DI slots, route tables)" as THE problem to solve, not as an edge case to bolt on afterwards. **What shipped instead, because it actually works:** a CONTRACT TEST. `tests/diagnosis-enricher.test.js` now exercises the enricher against the REAL diagnoser with no injected double, asserting `priorArt` reaches the prompt. Verified by replaying the original bug: it goes red. The seven pre-existing tests on that path all inject `diagnoseFindings: fakeDiagnose`, and a stub accepts whatever it is handed — which is precisely why the drop survived. **Generalisable rule for this repo:** where a seam is crossed by dependency injection, at least one test must cross it for real; otherwise the test suite is asserting the stub's behaviour, not the product's. **FOURTH SIGNAL — SHIPPED, and it works precisely because it resolves nothing.** `tests/jsdoc-options-contract.test.js` compares what ONE function's JSDoc claims to accept against what that SAME function body reads. No cross-file resolution, so the indirection that killed A/B/C cannot apply. It found three real phantom parameters on first run, two of them passed by live callers and discarded — `composePrBody(repoUrl)` from `/api/scan/fix`, `sendGithubCallback(ref)` from the worker tick, and `renderInitialReport(metadata)` (documented, never passed). None had been read since the day it was introduced. Fixed in `f13fa76`. The lesson generalises: **intra-function contract checks are reliable here; inter-function ones are not.** Prefer the former until the dynamic-registry / DI resolution problem above is actually solved. **FIFTH SIGNAL — measured and REJECTED, do not repeat.** The mirror check (a `@returns` documenting an object field the body never produces) fires 3 times on this repo and **all three are false**: they are `decodePng()` in `src/core/visual-diff-engine.js`, which is `return PNG.sync.read(buffer)` — the shape comes from the library, not from a literal in the body. Adding a guard for `return <call>(...)` takes it to zero findings, i.e. no signal at all. So the returns direction is not worth building: unlike parameters, a return shape is usually assembled or delegated rather than written as a literal in the same function. **HALF BUILT 2026-07-30 — the dynamic-registry edge pass now ships, and it works.** The blocker named above ("resolve indirect call targets") turned out to have a tractable first half: a relative-path STRING that resolves to a real project file is an edge, whatever syntax surrounds it. `src/core/import-graph.js` now emits `kind: 'path-literal'` for those. **Measured on this repo before building it:** 150 new edges, and the falsely-unreachable count drops **217 → 102**, with ground truth behaving correctly in both directions — `src/modules/accessibility.js` and `ai-guardrails.js` (live via the registry, previously called dead) are recovered, while `try-fix.js` (KI #84, genuinely unreachable) stays flagged. **Two files still flagged were MY ground truth being wrong, not the signal:** `src/core/text-lines.js` genuinely has no importer in the shipped tree (only tests — I assumed it was live because I use it in throwaway scripts), and `bin/gatetest-mcp.mjs` is a declared package `bin` entrypoint, which by definition has no importers. **`staticGraph` is untouched** — still exactly 941 static edges, so `import-cycle` is provably unaffected; a registry reference is real coupling but cannot form a require-time cycle. Guarded by 5 tests including two NEGATIVE controls (an unresolvable path string, and prose that merely mentions a path, must not invent coupling) and an assertion that no path-literal edge leaks into `staticGraph`. **Still open:** the remaining 102 need the entrypoint exemption (`package.json` `bin`, Next.js route files — `dead-code.js` already has `_isEntryPoint` logic that this signal does not yet use) and honest triage of what is genuinely test-only. Only then is a shippable detector worth attempting; the measurement discipline above still applies — if it is noisy, do not ship it. **STEP 2 MEASURED 2026-07-30 — entrypoint exemption applied, STILL NOT SHIPPABLE, and now we know exactly why.** Using `dead-code.js`'s own `_isEntryPoint` (not a copy, so they cannot drift): of 391 production files with no production importer, 220 are declared entrypoints, leaving **171 candidates**. That is still far too noisy to ship, and the residue is dominated by three identifiable classes rather than being randomly wrong: (1) **126 of the 171 are under `website/app`** — `ENTRYPOINT_DIRS` matches with `rel.startsWith('app/')` against REPO-relative paths, so a *nested* Next.js app dir (`website/app/...`) never matches, even though every `page`/`route`/`layout` in it is an entrypoint by definition; (2) tool configs loaded by their tooling and never imported — `eslint.config.mjs`, `postcss.config.mjs`, `next.config.ts`, `playwright.config.ts`, `instrumentation.ts`, `next-env.d.ts`; (3) deliberately-standalone fixture corpora — `reliability-corpus/`, `benchmarks/bench-target/`, `arena-scaffold/`. **Checked and found NOT to be a bug in the shipped module:** the nested-app-dir limitation causes no false positives in `deadCode` today, because it reports **0** orphaned-file findings on this repo — every candidate is either an entrypoint or has its exports used. Verified the rule is not merely inert with a POSITIVE CONTROL: a planted unimported module in a scratch project IS flagged (`dead-code:unused-export`). So there was nothing to fix in `deadCode`, and the hypothesis that there was is recorded here so nobody re-derives it. **Remaining work before any detector ships:** make entrypoint matching segment-aware so nested app dirs count, exempt tool configs by convention, and exclude fixture corpora — then re-measure. If the residue is not small AND hand-verified precise, do not ship it; three passes have each improved the signal without making it trustworthy. | MEDIUM | Code work — pre-authorized, but deliberately NOT done on the naive approach. Needs `import-graph.js` + a dynamic-registry edge pass. Measurement recorded; do not repeat it. |
| 97 | **PRODUCTION WAS 66 COMMITS STALE — DEPLOYED 2026-08-05.** Live `/api/platform-status` now reports v1.61.0 at main's tip; `/docs/configuration` 200s, `/pricing` says 121 modules, `/github/setup` discloses the real scopes. **Root cause was not neglect — the deploy script could not work.** Three independent defects, each sufficient alone: (a) piping `systemctl list-unit-files` into `grep -q` under `set -o pipefail` is false EXACTLY when it matches, because `grep -q` exits early, `systemctl` takes SIGPIPE (141), and pipefail reports 141 — so the restart step had never fired on any host under any unit name; (b) the clean-tree guard aborted every run after the first, because `npm install` and the website prebuild dirty tracked files (`package-lock.json`, `build-info.json`) *after* the guard runs; (c) the box runs `gatetest-web.service`, not `gatetest.service`. Fixed in `3bd67ec0`, `54552a5f`, `96df6c52`. | — | RESOLVED — deployed and verified live. |
| 98 | **The scan queue never drained — `column q.host does not exist`. Found 2026-08-05 by probing the live tick endpoint after deploying.** `POST /api/scan/worker/tick` answered **HTTP 200** with `{"ok":false,"reclaimed":0,"error":"column q.host does not exist"}`. Production's `scan_queue` predated the dual-host `host` column. `ensureScanQueueTable()` ships the idempotent `ADD COLUMN IF NOT EXISTS`, but was only called from `/api/db/init` and the GitHub callback — the ENQUEUE path migrated itself, the CONSUME path did not. So the worker failed on every tick and reported it under a 200, invisible to any status-code health check. The worker now ensures the schema before claiming. Fixed `d3fe3738`; verified live: `{"ok":true,"idle":true,"reclaimed":0}`. **Lesson:** a self-migrating writer plus a non-migrating reader is a silent split-brain; migrate on both sides. | — | RESOLVED — fixed and verified in production. |
| 99 | **THE GITHUB APP IS POINTED AT A DEAD DOMAIN, so no push has ever reached us. Found 2026-08-05; CRAIG ACTION, cannot be fixed from this repo.** Measured: `scan_queue` contains **0 rows — nothing has ever been enqueued**; `https://gatetest.ai/api/webhook` returns **HTTP 000** (NXDOMAIN, registry redemption since 2026-07-29); `https://gatetest.io/api/webhook` returns **200**; the App still advertises `external_url = https://gatetest.ai`. Every webhook GitHub has attempted has failed to deliver. **This is the real reason a Marketplace reviewer would install, push, and see nothing** — and it survives the two fixes shipped today (KI #98, KI #41), which make the drain work but cannot conjure events that never arrive. After 2026-08-05 the pipeline is correct from `/api/webhook` onward and severed before it. Craig must set the App's Webhook URL (and `external_url`, Setup URL, Callback URL) to `gatetest.io` on app `3322634`; GitHub's "Recent Deliveries" tab should currently show nothing but failures. See `docs/marketplace/CRAIG-PRE-SUBMIT-CHECKLIST.md` §2. | **CRITICAL** | OPEN — Craig action, blocks the entire product. |
| 100 | **NO WORKING GITHUB CREDENTIAL EXISTS ON THE PRODUCTION BOX — every stored token returns 401 "Bad credentials". Found 2026-08-05 by the first end-to-end pipeline test ever run.** A signed push event was fired at the public `/api/webhook` (HMAC verified, enqueued as `scan_queue` row 1 — the first row the production table has ever held; the enqueue-kick claimed it instantly and the systemd timer handled the retry cadence correctly). The scan then failed: `fetchTree` got 401 because `GITHUB_TOKEN` in `website/.env.local` is dead — as is the copy in `.env.vercel` and every `.env.local.bak-*` (all tested directly against the GitHub API from the box). The box's `gh` CLI is not logged in and there is no `.git-credentials`. **Consequence: even after the webhook URL is fixed (KI #99), every scan fails at repo fetch, and every commit-status / PR-comment callback fails the same way — the entire GitHub-facing half of the product has had no working credential for an unknown period.** Nothing noticed because nothing ever consumed the queue until today (KI #41/#98). An automated fix was correctly blocked: moving a credential from Craig's dev machine to the box is a credential-exfiltration pattern the tool refuses, and rightly so. **Craig, two options (5 min):** (a) durable — paste `GITHUB_APP_ID` + `GITHUB_APP_PRIVATE_KEY` into `website/.env.local` (the code already mints installation tokens: `website/app/lib/github-app.ts`); (b) quick — mint a fresh PAT with repo scope and replace `GITHUB_TOKEN`. Then `systemctl restart gatetest-web` and watch row 1 retry to completion. Note for the payload contract: real webhooks carry the canonical `crclabs-hq/GateTest` name; the `ccantynz-alt/gatetest` alias 301s at the API (fetch follows redirects, so stale-name jobs still work once a token exists). | **CRITICAL** | OPEN — Craig action; blocks scans AND callbacks even after KI #99. |
| 101 | **THE FREE SCAN IS DEAD IN PRODUCTION FOR EVERY REPO, AND THE MONITOR THAT EXISTS TO CATCH THIS REPORTED THE SITE 10/11 HEALTHY THROUGHOUT. Found 2026-08-16 by probing the live site while answering "is this ready for public release?".** Measured against `https://gatetest.io`: `POST /api/scan/preview` returns `"appears to be empty or unreachable"` for `expressjs/express`, `vercel/next.js`, `jonschlinkert/is-odd` and our own public repo; `/playground` streams all 121 module names and then dies with `Cannot access expressjs/express — empty tree`. **This is the entire top of the funnel — a visitor arriving today is told THEIR repository is broken and leaves.** Root cause is KI #100's dead credential surfacing through two silent swallows in `website/app/lib/gluecron-client.ts`: `if (ghRes.ok)` skipped the 401 without recording it, and the Gluecron fallback's `return { paths: [] }` turned an unreadable tree into an empty one, which every caller renders as "this repo has no files". **Fixed in `3a9437bd`** — the failure now throws with the real cause and `describeGithubTreeFailure()` names a 401 as *our* configuration, "not your repository". **The credential itself is still Craig's to replace (KI #100 / `GATETEST_PRIVATE_KEY` placeholder), and the fix cannot reach production until `BOX_SSH_KEY`/`BOX_SSH_HOST` exist (see `deploy-box.yml`) — production is serving `ff0fe9b3`, built 2026-08-06.** Secondary finding, arguably the more important one: `readiness-probe.yml` had failed **100 consecutive scheduled runs over 5 days** and still missed this, because (a) it never ran the product, (b) `deploy/fresh` only asserted a commit stamp *existed* so a 10-day-old build passed a check named "fresh", and (c) it read `/api/status`'s `missing_*` lists but not `invalid_placeholders`, the field naming the dead key all along. All three closed and regression-tested; the summary now reports **PRODUCT BROKEN** distinctly from misconfiguration so a new failure stays legible inside a familiar red. | **CRITICAL** | **2026-08-16 UPDATE — deploy half RESOLVED.** `BOX_SSH_KEY`/`BOX_SSH_HOST` are set, Craig authorised the deploy key on the box, and the first successful automated deploy landed: production now serves `9244d8e5` (`deploy/fresh` green, 0.0d old) after 10 days stuck on `ff0fe9b3`. The engine fix is live and verified — `POST /api/scan/preview` now answers *"Could not read the file tree for expressjs/express: GateTest's git-host credential was rejected (401 Bad credentials) — this is our configuration, not your repository"* instead of blaming the customer's repo. Legal notes (KI-adjacent) also shipped: 0 rendered `[DRAFT` blocks on `/legal/terms` and `/legal/privacy`, banner retained. Fixed en route: the SSH step targeted `$HOME/gatetest` = the stale `/root/gatetest`, not the live `/opt/gatetest`. **Still OPEN on one thing only: `GATETEST_PRIVATE_KEY` is still the pasted doc example, so the 401 persists and `product/scan` stays red.** Craig action — replace it in `/opt/gatetest/website/.env.local` and `systemctl restart gatetest-web`; confirm the .pem comes from app_id 3322634 (`gatetesthq`), not the duplicate 3766251. **2026-08-18 UPDATE — PRODUCT HALF RESOLVED WITHOUT THE KEY.** Public repos are now read through an anonymous archive download (`repo-snapshot.js`, commit `b0b45c25`); `POST /api/scan/preview` returns findings for expressjs/express in production (verified 04:37Z), the playground stream completes. The dead `GATETEST_PRIVATE_KEY` still blocks GitHub App auth (statuses/PR comments/private repos) — that part stays Craig's. |
| 102 | **The `empire-smoke` cross-product probe has never been scheduled — nothing runs it.** Found 2026-08-29 while clearing dead config. GateTest's own `.gatetest.json` carried a `triggers.schedule` entry declaring `empire-smoke` should run every 5 minutes against the Vapron / Gluecron / GateTest deployments (`"reason": "Owned by agent 6"`). **Nothing reads `triggers`** — the key was decorative, so the probe has never fired. `integrations/smoke/empire-smoke.js` is real and runnable; no workflow, cron or script invokes it (`grep empire-smoke .github/workflows/ package.json` returns nothing). The dead `triggers`/`rules` keys were removed from `.gatetest.json` so it stops describing protection that does not exist, but **this one claim was the only one not already covered elsewhere** and is recorded here rather than silently dropped. What the other keys claimed IS genuinely enforced: `triggers.pullRequest`/`triggers.push` by `.github/workflows/ci.yml` (push + PR to main run `gatetest --suite quick`), `rules.schemaRegressionGate` by that workflow's `typecheck` job, and `rules.secretLeak` by the `secrets` + `secretRotation` modules, which run in every suite. **Not fixed here deliberately:** standing up a workflow that probes two of Craig's other production platforms every 5 minutes is a monitoring/ops decision with cost and alert-noise consequences, not routine code. Note `readiness-probe.yml` already covers gatetest.io itself every 30 minutes, so the gap is specifically CROSS-product. Also worth checking before scheduling: empire-smoke targets `gluecron.crontech.ai`, and Crontech was renamed Vapron on 2026-06-12, so its target list may be stale. **RESOLVED 2026-08-29 — Craig authorized ("fix the empire-smoke targets and schedule it").** Targets corrected against measurement: `api.crontech.ai` and `gluecron.crontech.ai` no longer resolve (both returned 000) and were replaced by `api.vapron.ai` and the `gluecron.com` apex + platform-status; `crontech.ai` now has its own probe asserting the 301 to `vapron.ai` still stands, because that redirect is load-bearing for every pre-rename link and nothing else would notice it breaking. **Two failures were baked in beyond the hostnames**: the homepage probe demanded the body contain "Crontech" (vapron.ai serves "Vapron") and the health probe demanded `ok:true` (Vapron answers `{"status":"ok","checks":[...]}`) — so even pointed at the right hosts it would have been red. Added a `gatetest-status` probe that fails on `commit: "unknown"` (the KI #79 class a green homepage hides). Scheduled via `.github/workflows/empire-smoke.yml` at the `*/5` cadence the dead config asked for (public repo, so Actions minutes are free), with retry-once so a dropped packet on a runner is not an alert. **Root cause of the rot, also fixed:** the module's test file lived in `integrations/smoke/`, which NO sweep or CI job globs — every runner uses `tests/*.test.js`. The module was invisible and so were its tests, which is how it stayed broken for two months. Moved to `tests/empire-smoke.test.js` (19 tests) so the next rename breaks a build. **Latency observation, and a CORRECTION to how it was first written up.** vapron.ai's 365KB homepage measured **1.5s / 9.2s / 19.8s from the NZ dev box** but **~300ms from a GitHub runner** — same page, same minute — with TTFB fast (0.5-3.7s) at both vantage points. The server answers promptly; it is bulk transfer over one network path that drags. This was initially recorded as "a real Vapron performance problem"; the GitHub-runner measurement does not support that, and the honest reading is a slow path from that box, not a server defect. The page being 365KB is still worth a look on its own merits, but it is not an outage and not evidence of one. **The design consequence is the durable lesson:** under the original 5s hard timeout a perfectly healthy site read as DOWN depending only on where you probed from, so the probe now separates "up but slow" (warn, exit 0) from "down" (fail, exit 1), with the hard ceiling at 20s. Latency is a property of the path as much as the server and must never by itself mean outage. **CADENCE CORRECTED SAME DAY — the `*/5` GitHub cron is not a 5-minute check and I should not have described it as one.** After 30 minutes the new workflow had produced ZERO scheduled runs (only a manual dispatch). Measured cause, from this repo: `readiness-probe.yml` is configured `*/30` and its actual scheduled runs on 2026-08-29 landed at 01:29, 08:16, 13:57, 17:38, 20:32 UTC — **gaps of three to seven hours against a thirty-minute cron**. GitHub schedules are best-effort and throttled hard on public repos; the declared interval is a request, not a promise. Fixed by splitting the two drivers by what each is actually good at: the **real cadence moved to a systemd timer on the box** (`scripts/deploy/systemd/gatetest-empire-smoke.{service,timer}` + `scripts/deploy/empire-smoke.sh`, 5-minute `OnUnitActiveSec`), which is the same conclusion `scripts/deploy/systemd/README.md` already reached for the queue drain and the same Forbidden #3 argument; the **Actions workflow is kept for alerting** (email on red + archived report artifact, and it probes from outside our own network, so it still sees us when the box does not). The unit deliberately carries NO `EnvironmentFile` — the probe needs no secrets, and `EnvironmentFile=-` is the exact green-that-cannot-turn-red pattern `src/modules/systemd.js` flags. | MEDIUM | RESOLVED (targets + test placement + cadence). **Craig action to activate the real cadence:** install the timer on the box per `scripts/deploy/systemd/README.md` — until then only the throttled Actions schedule runs. Remaining: `vapron.ai/api/platform-status` still 404s, so Vapron is probed via homepage + api health rather than the shared contract (KI #81). |
| 103 | **Tests that pass on Windows and fail on Linux — a class, not a bug.** Found 2026-09-02: main had been red for five commits while every commit message said "fast tests pass". Four modules had path logic that only worked with backslashes or a case-insensitive filesystem (`bashSafety` never scanned `.github/` on Linux; `authBypass` skipped Express apps under `api/`; `documentation` could not find `Readme.md`; `prSize` sized pinned commits as PRs). All four fixed, but the CLASS remains: every Craig session runs on Windows and every customer CI runs on Linux. **Mitigation now in place:** the corpus gate (`scripts/corpus-gate.js`, `.github/workflows/corpus-gate.yml`) runs the full engine on nine real repos on every PR, on Linux. **Still open:** a `path.sep`-aware lint rule (or a `PathAudit` module) that flags `includes('/x')`, `existsSync` on case-sensitive names, and `.split('/')` on `path.join` output in `src/modules/`. | HIGH | Pre-authorised — next session builds the rule; `grep -rn "includes('/" src/modules` is the starting list. |
| 104 | **Warning volume is unaudited on every corpus repo and on ourselves.** `errorSwallow:empty-catch` alone fires 346 times on this repo, 7 on lodash.js (all feature-detection `try {} catch {}`), and warnings total 261 on axios and 792 on juice-shop. Warnings do not block, but a wall of them is how a tool teaches a developer to stop reading. Per-module warning budgets are now pinned per repo in `benchmarks/corpus/baselines/` (gate fails past baseline × 1.25 + 5), so volume cannot GROW unnoticed — but the existing volume has not been audited rule by rule. | HIGH | Pre-authorised — same method as 2026-09-01: count before sampling, fix the top rule per repo with a control pair, re-capture. Start with `empty-catch` (a catch whose try is a single feature-probe expression is idiomatic) and `flaky-tests:real-timer` (114 on our own repo). |
| 105 | **Repair verification is uneven across the three fix pipelines.** `src/core/direct-repair.js` now verifies fail-closed (syntax + re-scan, revert on failure — 2026-09-02). The CLI orchestrator already runs tests per hypothesis. The WEBSITE pipeline (`website/app/api/scan/fix/route.ts`) still (a) writes generated regression tests into the PR without ever executing them, (b) runs pair-review and the confidence gate AFTER the PR is open, as advisory comments, and (c) never points `fakeFixDetector` at its own diff. The customer-facing tier is the least verified of the three. | HIGH | Pre-authorised — run generated tests through `bidirectional-test-gate.js` in a sandbox before composing the PR; run `fake-fix-detector` on the fix diff; make a failing pair-review withhold the PR (draft) instead of commenting on a shipped one. |
| 106 | **Five modules have no test of any kind:** `live-crawler-browser-engine.js`, `live-crawler-http-engine.js`, `live-crawler-http-helpers.js`, `live-crawler-report.js`, `spine-health.js` (module #121, the newest). 37 modules have no DEDICATED test file (32 are covered by shared suites). | MEDIUM | Pre-authorised — one test file per module, positive + negative control each; `spine-health` first since it ships in the full suite. |
---

---

## FIX REVIEW ROOM — PR-ITERATION ROADMAP (researched + designed 2026-07-20, not built)

Craig's "20 steps ahead" ask: let developers review and iterate on a GateTest fix PR *through the website* instead of a one-shot "here's your PR" dead end — competitive research confirmed every rival (CodeRabbit, Greptile, Qodo Merge, Sourcery, Copilot) does PR iteration only as a git-host comment thread, never a dedicated web UI, so this is mechanically differentiated if built. Full design, confirmed existing capabilities (don't rebuild), confirmed gaps, and a phased plan (with an explicit Phase 0 prerequisite: reconciling the website's PR-creation client with the CLI's more feature-complete `HostBridge`) live in **[`docs/roadmap/fix-review-room.md`](roadmap/fix-review-room.md)**. Not scheduled — pick up when ready to commit multi-week build time.

---

## REMOTE MCP — UNIVERSAL DISTRIBUTION (approved by Craig 2026-07-07)

**Goal:** any Claude user — claude.ai web, Desktop app, mobile, Claude Code, Cursor,
Windsurf, corporate locked-down machines — can add GateTest in under 30 seconds.
The local stdio MCP (`npx @gatetest/mcp-server`) only reaches users who can run npm.

**Architecture:**
- Hosted MCP endpoint: `https://mcp.gatetest.io/mcp` (MCP Streamable HTTP transport)
- Host: the Jarvis server `66.42.121.161` (Vultr) — Jarvis orchestrates on this box, so
  GateTest's MCP endpoint MUST be co-located there for Jarvis to control it. Do NOT move.
- Runtime: Bun + Hono (Vapron edge-runtime pattern). NOT Vercel — Vercel is a competitor.
- Code staged in this repo at `packages/mcp-remote/` until deployed to the box.
- Auth: `Authorization: Bearer gtmcp_xxx` → validated against `/api/mcp/validate` (1h cache).
- Remote-capable tools (proxy gatetest.io APIs): check_health, list_modules, get_badge,
  scan_url, scan_repo, explain_finding, fix_issue, get_production_errors, get_report.
- Local-only forever (honest limitation): scan_local, run_tests, stream_logs, query_db,
  http_request — they need the user's filesystem/processes.

**DNS (Craig action):** Cloudflare A record `mcp` → `66.42.121.161`, DNS-only first
(box terminates TLS via Caddy/Let's Encrypt), optionally proxied later.

**Registry:** `packages/mcp-server/server.json` carries both transports (stdio npm +
remote HTTP) → submit to Anthropic's MCP registry for one-click install.

Full detail: `~/.claude/plans/can-i-please-have-calm-lightning.md` (session plan, 2026-07-07).
