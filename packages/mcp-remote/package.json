{
  "name": "@gatetest/mcp-remote",
  "version": "1.0.0",
  "private": true,
  "description": "GateTest hosted MCP endpoint (mcp.gatetest.ai) — MCP Streamable HTTP transport on Bun + Hono. Gives claude.ai web, mobile, and no-terminal users GateTest tools with zero install.",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
