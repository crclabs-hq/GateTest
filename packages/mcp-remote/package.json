{
  "name": "@gatetest/mcp-remote",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted wrapper for the GateTest remote MCP endpoint (the hosted one is https://gatetest.io/api/mcp) — MCP Streamable HTTP transport on Bun + Hono over the same mcp-remote-core. Gives claude.ai web, mobile, and no-terminal users GateTest tools with zero install.",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
